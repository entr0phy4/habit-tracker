---
phase: 05-visual-identity-check-in-delight
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/types.ts
  - src/domain/colors.ts
  - src/domain/colors.test.ts
  - src/domain/backupSchema.ts
  - src/domain/backupSchema.test.ts
  - src/infrastructure/habitRepository.ts
  - src/infrastructure/habitRepository.test.ts
  - src/infrastructure/backupService.ts
autonomous: true
requirements:
  - ENH-01
must_haves:
  truths:
    - Habit includes color: string hex
    - DEFAULT_HABIT_COLOR is #3fb950 and presets match D-03 (8 colors)
    - normalizeHabitColor maps missing/invalid to default
    - habitRepository create/update persist color
    - parseBackupJson accepts habits without color; import path stores normalized color
    - buildHeatmapTheme(hex) returns dark array length 5 with #21262d as level 0
  artifacts:
    - src/domain/colors.ts
    - src/domain/colors.test.ts
  key_links:
    - Habit.color used by form/UI in later plans
    - backupSchema color optional for v1 compatibility (D-18)
  prohibitions:
    - Must not bump backup version above 1
    - Must not add animation libraries
    - Must not require color in Zod (legacy backups)
---

## Phase Goal

**As a** habit tracker user, **I want** each habit to have a durable color identity, **so that** later UI can show personal accents and backups round-trip colors safely.

<objective>
Add domain color presets/helpers, extend Habit + repository + backup for color (ENH-01 foundation; D-01–D-05, D-11 helper, D-18–D-19).

Output: colors.ts (+ tests), updated types/repo/backup, passing unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-visual-identity-check-in-delight/05-CONTEXT.md
@.planning/phases/05-visual-identity-check-in-delight/05-RESEARCH.md
@.planning/phases/05-visual-identity-check-in-delight/05-UI-SPEC.md
@src/domain/types.ts
@src/domain/backupSchema.ts
@src/infrastructure/habitRepository.ts
@src/infrastructure/backupService.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — colors domain tests</name>
  <files>src/domain/colors.test.ts</files>
  <read_first>src/domain/types.ts, .planning/phases/05-visual-identity-check-in-delight/05-CONTEXT.md, .planning/phases/05-visual-identity-check-in-delight/05-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - colors.test.ts imports from ./colors and fails because module missing OR assertions fail
    - Tests cover: HABIT_COLOR_PRESETS length 8 including #3fb950
    - Tests cover: DEFAULT_HABIT_COLOR === '#3fb950'
    - Tests cover: normalizeHabitColor(undefined) and invalid → default; valid preset preserved
    - Tests cover: buildHeatmapTheme('#3fb950').dark has length 5 and dark[0] === '#21262d'
  </acceptance_criteria>
  <action>
    Create failing tests for colors domain per D-02–D-04 and D-11. Do not create colors.ts yet (RED).
  </action>
  <verify>
    <automated>npm test -- src/domain/colors.test.ts 2>&1 | grep -qE 'FAIL|Error|Cannot find'; test $? -eq 0</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — colors.ts + Habit.color + repo + backup</name>
  <files>src/domain/colors.ts, src/domain/types.ts, src/domain/backupSchema.ts, src/domain/backupSchema.test.ts, src/infrastructure/habitRepository.ts, src/infrastructure/habitRepository.test.ts, src/infrastructure/backupService.ts</files>
  <read_first>src/domain/backupSchema.ts, src/infrastructure/habitRepository.ts, src/infrastructure/backupService.ts, src/domain/backupSchema.test.ts</read_first>
  <acceptance_criteria>
    - colors.ts exports HABIT_COLOR_PRESETS, DEFAULT_HABIT_COLOR, normalizeHabitColor, buildHeatmapTheme
    - Habit interface has color: string
    - habitRepository.create defaults color to DEFAULT when omitted; update accepts color
    - habitSchema has color: z.string().optional(); after parse/import habits get normalized color on write
    - Existing backup tests still pass; add test for habit without color → ok and importable
    - npm test -- src/domain/colors.test.ts src/domain/backupSchema.test.ts src/infrastructure/habitRepository.test.ts passes
  </acceptance_criteria>
  <action>
    Implement colors.ts. Extend Habit + repository (D-01, D-04, D-05). Extend backup Zod optional color (D-18); ensure export includes color (D-19) by storing color on habits. Normalize on create/update and when importing habits in backupService. Fix any fixture breakage in touched tests.
  </action>
  <verify>
    <automated>npm test -- src/domain/colors.test.ts src/domain/backupSchema.test.ts src/infrastructure/habitRepository.test.ts</automated>
  </verify>
</task>

</tasks>

## Verification

```bash
npm test -- src/domain/colors.test.ts src/domain/backupSchema.test.ts src/infrastructure/habitRepository.test.ts
```

Create `.planning/phases/05-visual-identity-check-in-delight/05-01-SUMMARY.md` when done.
