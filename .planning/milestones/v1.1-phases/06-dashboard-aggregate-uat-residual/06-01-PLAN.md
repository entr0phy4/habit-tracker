---
phase: 06-dashboard-aggregate-uat-residual
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/stats.ts
  - src/domain/stats.test.ts
autonomous: true
requirements:
  - ENH-03
must_haves:
  truths:
    - countScheduledCompletions returns { completed, scheduled } using the same schedule/future gates as calculateCompletionRate (D-07)
    - calculateOverallCompletionRate pools Σ completed ÷ Σ scheduled across habits then Math.round — not the mean of per-habit rates (D-06)
    - Empty habit list or scheduled === 0 yields 0 (D-04 denominator case)
    - calculateCompletionRate public API remains stable (may delegate to counts)
  artifacts:
    - src/domain/stats.ts
    - src/domain/stats.test.ts
  key_links:
    - calculateOverallCompletionRate composes countScheduledCompletions per habit
    - isDueOnDate + isFutureDate + iterateDaysInRange unchanged gates
  prohibitions:
    - Must not average per-habit calculateCompletionRate percentages
    - Must not add Dexie/React imports to stats.ts
    - Must not introduce rolling 7/30 windows
    - Must not touch UI or hooks in this plan
---

## Phase Goal

**As a** habit tracker user, **I want** an honest pooled lifetime completion rate across my active habits, **so that** one Panel glance reflects how much of my scheduled work I actually did — without overweighting brand-new habits.

<objective>
Ship domain building blocks for ENH-03 with TDD: `countScheduledCompletions` + `calculateOverallCompletionRate` in `stats.ts`, covering pooled-vs-mean and zero-denominator cases.

Purpose: Lock D-06/D-07 math before hooks or Panel UI.
Output: Extended `stats.ts` + green `stats.test.ts`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dashboard-aggregate-uat-residual/06-CONTEXT.md
@.planning/phases/06-dashboard-aggregate-uat-residual/06-RESEARCH.md
@.planning/phases/06-dashboard-aggregate-uat-residual/06-PATTERNS.md
@.planning/phases/06-dashboard-aggregate-uat-residual/06-VALIDATION.md
@src/domain/stats.ts
@src/domain/stats.test.ts
@src/domain/schedule.ts
@src/domain/dates.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pooled overall + count helpers tests</name>
  <files>src/domain/stats.test.ts</files>
  <read_first>src/domain/stats.ts, src/domain/stats.test.ts, .planning/phases/06-dashboard-aggregate-uat-residual/06-CONTEXT.md, .planning/phases/06-dashboard-aggregate-uat-residual/06-PATTERNS.md</read_first>
  <acceptance_criteria>
    - stats.test.ts imports countScheduledCompletions and calculateOverallCompletionRate from ./stats
    - Test: countScheduledCompletions on daily 2 of 3 days → { completed: 2, scheduled: 3 }
    - Test: calculateOverallCompletionRate single habit matches calculateCompletionRate for same inputs
    - Test: pooled two habits — habit A 1/1 complete, habit B 0/99 complete → overall Math.round(1/100*100)=1, NOT 50 (mean of 100 and 0)
    - Test: calculateOverallCompletionRate([]) → 0
    - Test: all habits with scheduled 0 → 0
    - Suite fails RED until Task 2 implements exports (FAIL / Cannot find name / is not a function)
  </acceptance_criteria>
  <behavior>
    - countScheduledCompletions mirrors calculateCompletionRate loop gates (due only; skip future)
    - calculateOverallCompletionRate sums counts then rounds; never averages percentages
  </behavior>
  <action>
    Extend src/domain/stats.test.ts with RED tests for countScheduledCompletions and calculateOverallCompletionRate per D-06/D-07/D-04. Keep existing calculateCompletionRate and getWeekDayState tests green/unchanged in intent. Do not implement production exports yet if that keeps RED clear — or implement stubs that throw; prefer missing exports for classic RED.
  </action>
  <verify>
    <automated>npm test -- src/domain/stats.test.ts 2>&1 | grep -qE 'FAIL|Error|Cannot find|is not a function|does not provide'; test $? -eq 0</automated>
  </verify>
  <done>Failing stats tests document ENH-03 pooled domain contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement count + overall helpers</name>
  <files>src/domain/stats.ts, src/domain/stats.test.ts</files>
  <read_first>src/domain/stats.test.ts, src/domain/stats.ts, .planning/phases/06-dashboard-aggregate-uat-residual/06-RESEARCH.md, .planning/phases/06-dashboard-aggregate-uat-residual/06-PATTERNS.md</read_first>
  <acceptance_criteria>
    - stats.ts exports countScheduledCompletions and calculateOverallCompletionRate
    - calculateCompletionRate still returns same numbers as before (refactor to delegate to counts allowed)
    - Pooled-vs-mean and zero-denominator tests pass
    - npx vitest run src/domain/stats.test.ts passes
    - No React/Dexie imports in stats.ts
  </acceptance_criteria>
  <action>
    Implement countScheduledCompletions and calculateOverallCompletionRate per RESEARCH API and D-06/D-07. Optionally refactor calculateCompletionRate to use counts. Keep getWeekDayState unchanged. Ensure all stats tests green.
  </action>
  <verify>
    <automated>npx vitest run src/domain/stats.test.ts -x</automated>
  </verify>
  <done>Domain pooled overall rate ready for dashboard hook wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain pure functions | Inputs are in-memory Sets/Frequency from app code — no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-00 | Tampering | N/A domain math | low | accept | Local compute only; no new untrusted parse surface in this plan |
</threat_model>

<verification>
- npx vitest run src/domain/stats.test.ts -x — all green
- rg "calculateOverallCompletionRate|countScheduledCompletions" src/domain/stats.ts — present
- rg "from 'react'|from \\\"dexie" src/domain/stats.ts — no matches
</verification>

<success_criteria>
- ENH-03 domain math complete (D-06, D-07, D-04 zero case)
- Existing per-habit rate behavior preserved
- No UI/hook changes in this wave
</success_criteria>
