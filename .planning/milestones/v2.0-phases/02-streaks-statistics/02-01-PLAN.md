---
phase: 02-streaks-statistics
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/dates.ts
  - src/domain/dates.test.ts
  - src/domain/streak.ts
  - src/domain/streak.test.ts
autonomous: true
requirements:
  - STRK-01
  - STRK-02
must_haves:
  truths:
    - Weekly habit rest days do not break streak — only scheduled days count (STRK-01, D-14)
    - Today incomplete does not zero displayed current streak for daily habits (STRK-01, D-15)
    - First scheduled completion starts streak at 1; zero before any completion (STRK-01, D-13)
    - Longest streak is all-time maximum consecutive scheduled-day run (STRK-02, D-16)
    - Habit start date uses local date from createdAt, not UTC slice (RESEARCH Pitfall 4)
  artifacts:
    - src/domain/streak.ts
    - src/domain/streak.test.ts
  key_links:
    - calculateCurrentStreak gates every step with isDueOnDate from schedule.ts
    - calculateLongestStreak uses iterateDaysInRange forward scan
    - getHabitStartDate converts habit.createdAt via getLocalDateString
  prohibitions:
    - Must not persist streak fields on Habit records
    - Must not use toISOString().slice(0,10) for day keys
---

## Phase Goal

**As a** habit tracker user, **I want to** see streak motivation and completion stats that respect each habit's schedule, **so that** I stay motivated without breaking my chain on rest days or before today's check-in.

<objective>
Implement schedule-aware streak pure functions with comprehensive Vitest coverage — foundation for Today badge and history stat cards.

Purpose: STRK-01 and STRK-02 require correct backward/forward streak walks before any UI wiring (D-13 through D-16).
Output: `streak.ts`, date iteration helpers, passing `streak.test.ts` and extended `dates.test.ts`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-streaks-statistics/02-CONTEXT.md
@.planning/phases/02-streaks-statistics/02-RESEARCH.md
@.planning/phases/02-streaks-statistics/02-PATTERNS.md
@.planning/phases/02-streaks-statistics/02-VALIDATION.md
@src/domain/schedule.ts
@src/domain/dates.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing streak and date-iteration tests</name>
  <files>src/domain/streak.test.ts, src/domain/dates.test.ts</files>
  <read_first>src/domain/schedule.ts, src/domain/dates.ts, .planning/phases/02-streaks-statistics/02-CONTEXT.md, .planning/phases/02-streaks-statistics/02-RESEARCH.md</read_first>
  <acceptance_criteria>
    - streak.test.ts imports calculateCurrentStreak and calculateLongestStreak from ./streak and tests fail because module does not exist yet
    - MWF fixture monWedFri = { type: 'weekly', days: [1, 3, 5] } reused from schedule.test.ts pattern
    - Test: new habit no completions → current streak 0 (D-13)
    - Test: daily 5-day run, today incomplete → current 5 (D-15)
    - Test: daily 5-day run, today complete → current 6
    - Test: MWF Mon+Wed complete, evaluate Tuesday → current counts Mon+Wed only (rest day skip)
    - Test: missed scheduled day breaks current streak (D-14)
    - Test: longest &gt; current when past 10-day run followed by recent miss (D-16)
    - dates.test.ts adds getPreviousDay across month boundary and iterateDaysInRange inclusive bounds tests (fail until Task 2)
    - npm test -- src/domain/streak.test.ts fails with import/module-not-found, not assertion pass
  </acceptance_criteria>
  <behavior>
    - calculateCurrentStreak(Set, Frequency, today, habitStartDate) walks backward skipping non-scheduled days
    - calculateLongestStreak(Set, Frequency, startDate, endDate) forward-scans for max consecutive scheduled completions
    - getPreviousDay and iterateDaysInRange use noon-local T12:00:00 parse per schedule.ts convention
  </behavior>
  <action>
    Create src/domain/streak.test.ts with all edge cases from 02-VALIDATION.md Nyquist table for STRK-01 and STRK-02. Use fixed date strings (2026-07-xx) — no Date.now() in assertions.

    Extend src/domain/dates.test.ts with tests for getPreviousDay('2026-07-01') → '2026-06-30' and iterateDaysInRange('2026-07-13','2026-07-15') yielding 3 inclusive dates.

    Do not create streak.ts yet — RED commit must fail on missing implementation.
  </action>
  <verify>
    <automated>npm test -- src/domain/streak.test.ts 2>&1 | grep -qE 'FAIL|Error|Cannot find'; test $? -eq 0</automated>
  </verify>
  <done>Failing streak test suite committed; documents all D-13–D-16 and weekly-rest edge predicates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — streak.ts and date iteration helpers</name>
  <files>src/domain/streak.ts, src/domain/dates.ts</files>
  <read_first>src/domain/streak.test.ts, .planning/phases/02-streaks-statistics/02-RESEARCH.md, .planning/phases/02-streaks-statistics/02-PATTERNS.md</read_first>
  <acceptance_criteria>
    - calculateCurrentStreak implements D-15 today-incomplete skip without breaking streak
    - calculateLongestStreak returns all-time max per D-16
    - getPreviousDay, iterateDaysInRange, getHabitStartDate exported from dates.ts
    - getHabitStartDate(habit) returns getLocalDateString(new Date(habit.createdAt)) — never createdAt.slice(0,10)
    - iterateDaysInRange uses addDays/subDays with noon-local parse — no millisecond += 86400000
    - npm test -- src/domain/streak.test.ts src/domain/dates.test.ts passes
  </acceptance_criteria>
  <action>
    Extend src/domain/dates.ts with per-function date-fns imports: addDays, subDays (getPreviousDay already uses subDays). Add getPreviousDay, iterateDaysInRange generator, getHabitStartDate accepting Habit type from types.ts.

    Create src/domain/streak.ts exporting calculateCurrentStreak and calculateLongestStreak as pure functions. Import isDueOnDate from schedule.ts. No React, no Dexie imports in domain layer.

  </action>
  <verify>
    <automated>npm test -- src/domain/streak.test.ts src/domain/dates.test.ts -x</automated>
  </verify>
  <done>All streak unit tests green; date iteration helpers ready for stats module and hooks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain ← Set&lt;date&gt; | Pure functions receive only validated date strings from repository layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | streak.ts inputs | low | accept | Set built server-side from Dexie; no user string in streak math |
| T-2-02 | Denial of Service | iterateDaysInRange | low | mitigate | Bounded to habit.createdAt → today range in hooks; no unbounded user input |
</threat_model>

<verification>
- npm test -- src/domain/streak.test.ts src/domain/dates.test.ts -x — all green
- No imports of db or react in src/domain/streak.ts
</verification>

<success_criteria>
- STRK-01 streak counting rules encoded and tested (D-13–D-15)
- STRK-02 longest streak algorithm tested (D-16)
- Date helpers ready for stats and hooks in Plans 02–04
</success_criteria>

## Artifacts this phase produces

| Symbol / Path | Kind | Plan |
|---------------|------|------|
| `calculateCurrentStreak` | function | 02-01 |
| `calculateLongestStreak` | function | 02-01 |
| `getPreviousDay` | function | 02-01 |
| `iterateDaysInRange` | generator | 02-01 |
| `getHabitStartDate` | function | 02-01 |
| `src/domain/streak.ts` | file | 02-01 |
| `src/domain/streak.test.ts` | file | 02-01 |

<output>
Create `.planning/phases/02-streaks-statistics/02-01-SUMMARY.md` when done
</output>
