---
phase: 01-habit-management-daily-logging
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/habits/HabitForm.tsx
  - src/components/habits/WeekDayDots.tsx
  - src/components/habits/WeekDayDots.test.tsx
  - src/pages/HabitNewPage.tsx
  - src/pages/TodayPage.tsx
  - src/components/habits/HabitRow.tsx
  - src/hooks/useTodayHabits.ts
  - src/infrastructure/habitRepository.test.ts
  - src/domain/schedule.test.ts
  - src/components/ui/toggle.tsx
  - src/components/ui/toggle-group.tsx
autonomous: true
requirements:
  - HABT-01
  - LOG-03
must_haves:
  truths:
    - User can create a habit with specific weekdays via M-S day toggle buttons on /habits/new (D-06, HABT-01)
    - New habits default to all seven days selected (Daily, D-07)
    - After create, sonner toast "Habit created" shows and user returns to Today list (D-08)
    - Habit rows display WeekDayDots schedule indicator beside name (D-04)
    - Weekly habit not due today does not appear on Today list (D-02, LOG-03)
    - Creating two habits with the same trimmed name produces two distinct records (HABT-01 idempotency probe — allowed)
    - Empty name submit shows inline error "Enter a habit name to continue." (UI-SPEC)
    - statement: Partial day selection (1-6 days) saves as weekly frequency, not daily
      verification: backstop
  artifacts:
    - src/components/habits/HabitForm.tsx
    - src/components/habits/WeekDayDots.tsx
  key_links:
    - HabitForm maps all-7-selected to { type: 'daily' } else { type: 'weekly', days }
    - useTodayHabits calls isDueOnDate for each active habit against today's local date string
    - HabitNewPage passes onSubmit to HabitForm and shows toast.success on create
  prohibitions:
    - Must not use modal or drawer for habit creation (D-05)
    - Must not show non-due habits greyed out on Today — hide entirely (D-02)
---

<objective>
Complete habit creation UX with weekday frequency picker, schedule dots on rows, and due-today filtering for weekly habits.

Purpose: Fulfill HABT-01 frequency requirements and LOG-03 today filtering after walking skeleton proved persistence.
Output: Shared HabitForm component, WeekDayDots, toast on create, weekly schedule filtering verified by tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-habit-management-daily-logging/01-CONTEXT.md
@.planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md
@.planning/phases/01-habit-management-daily-logging/01-PATTERNS.md
@.planning/phases/01-habit-management-daily-logging/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HabitForm with day toggles and validation</name>
  <files>src/components/habits/HabitForm.tsx, src/pages/HabitNewPage.tsx, src/components/ui/toggle.tsx, src/components/ui/toggle-group.tsx, src/infrastructure/habitRepository.test.ts</files>
  <read_first>src/pages/HabitNewPage.tsx, .planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md, .planning/phases/01-habit-management-daily-logging/01-CONTEXT.md</read_first>
  <acceptance_criteria>
    - npx shadcn@latest add toggle toggle-group succeeds
    - HabitForm renders name Input maxLength 80, ToggleGroup with 7 day buttons S M T W T F S (D-06)
    - Default state has all 7 days selected (D-07)
    - Submit with empty/whitespace name blocked with inline error per UI-SPEC copy
    - Submit with valid name calls onSubmit with trimmed name and Frequency: daily when all 7 selected, weekly with days array otherwise
    - Each day toggle min 44x44px touch target (UI-02)
    - habitRepository.test.ts covers weekly frequency persistence
  </acceptance_criteria>
  <action>
    Create src/components/habits/HabitForm.tsx as shared form for create (and later edit in Plan 03). Use shadcn Input, Label, ToggleGroup per UI-SPEC HabitForm section. Day indices 0-6 matching Date.getDay() with labels S M T W T F S.

    Refactor HabitNewPage to compose HabitForm inside AppShell with Back chevron to /, heading "New habit", submit label "Create habit". On submit call habitRepository.create, toast.success('Habit created') via sonner (D-08), navigate('/').

    Add/extend habitRepository.test.ts: create weekly Mon/Wed/Fri habit persists frequency.days [1,3,5].

    Per D-05: remain on dedicated /habits/new page — not modal.
  </action>
  <verify>
    <automated>npx vitest run src/infrastructure/habitRepository.test.ts src/domain/schedule.test.ts</automated>
  </verify>
  <done>HabitForm creates daily and weekly habits with validation; toast + redirect on success.</done>
</task>

<task type="auto">
  <name>Task 2: WeekDayDots schedule indicator on habit rows</name>
  <files>src/components/habits/WeekDayDots.tsx, src/components/habits/WeekDayDots.test.tsx, src/components/habits/HabitRow.tsx</files>
  <read_first>src/components/habits/HabitRow.tsx, .planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - WeekDayDots renders 7 dots with S-S labels; scheduled days use primary green fill, unscheduled use border gray (D-04)
    - Daily frequency shows all 7 dots filled
    - HabitRow integrates WeekDayDots inline with habit name; layout matches UI-SPEC flex row
    - Dots are read-only schedule indicator — not completion status
  </acceptance_criteria>
  <action>
    Implement WeekDayDots.tsx accepting frequency prop. Map daily type to all days filled. Use 8px dots, 4px gap, Label typography for day letters per UI-SPEC.

    Update HabitRow to render WeekDayDots next to habit name. Preserve min-h-11 and existing tap toggle behavior from Plan 01.

    Write src/components/habits/WeekDayDots.test.tsx with @testing-library/react: daily frequency renders 7 filled dots; weekly Mon/Wed/Fri renders only those day letters with primary fill.

    No navigation affordance on row yet — history chevron added in Plan 04.
  </action>
  <verify>
    <automated>npx vitest run src/components/habits/WeekDayDots.test.tsx src/domain/schedule.test.ts</automated>
  </verify>
  <done>Each Today row shows which weekdays the habit is scheduled.</done>
</task>

<task type="auto">
  <name>Task 3: Weekly due-today filtering integration tests</name>
  <files>src/hooks/useTodayHabits.ts, src/domain/schedule.test.ts, src/hooks/useTodayHabits.test.ts</files>
  <read_first>src/hooks/useTodayHabits.ts, src/domain/schedule.ts, .planning/phases/01-habit-management-daily-logging/01-RESEARCH.md</read_first>
  <acceptance_criteria>
    - schedule.test.ts asserts Mon/Wed/Fri habit due only on those weekdays
    - Integration test or hook-level test: seed weekly habit not due today → not returned by today filter (LOG-03, D-02)
    - Integration test: seed weekly habit due today → appears in filtered list
    - TodayPage still shows empty state when no habits due (zero-one-many probe)
    - npx vitest run exits 0
  </acceptance_criteria>
  <action>
    Extend schedule.test.ts with fixtures for each weekday boundary. Add test file src/hooks/useTodayHabits.test.ts or extend integration test: use fake-indexeddb, create habits with different frequencies, assert filter results match isDueOnDate for mocked today date (inject today via date helper parameter or vi.setSystemTime).

    Verify useTodayHabits excludes archived and non-due habits. Do not render hidden habits greyed out.

    Run full vitest suite to ensure no regressions.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Today view lists only habits due on the current local calendar day.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → HabitForm | Name string and day selection array |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | HabitForm submit | medium | mitigate | Client-side trim + empty reject before repository; server N/A |
| T-01-05 | Denial of Service | frequency.days array | low | accept | Clamp to 0-6 integers client-side; single-user local app |
| T-01-SC | Tampering | shadcn toggle-group install | high | mitigate | Official shadcn registry only per UI-SPEC Registry Safety |
</threat_model>

<verification>
- npx vitest run — all green
- Manual: create Mon/Wed/Fri habit on Tuesday → not on Today; on Monday → appears
</verification>

<success_criteria>
- HABT-01 fully satisfied including weekday frequency
- LOG-03 filtering correct for daily and weekly habits
- D-04 through D-08 implemented
</success_criteria>

## Artifacts this phase produces

| Symbol / Path | Kind | Plan |
|---------------|------|------|
| `HabitForm` | component | 01-02 |
| `WeekDayDots` | component | 01-02 |
| `src/components/ui/toggle.tsx` | shadcn component | 01-02 |
| `src/components/ui/toggle-group.tsx` | shadcn component | 01-02 |
| `src/hooks/useTodayHabits.test.ts` | test file (if created) | 01-02 |

<output>
Create `.planning/phases/01-habit-management-daily-logging/01-02-SUMMARY.md` when done
</output>
