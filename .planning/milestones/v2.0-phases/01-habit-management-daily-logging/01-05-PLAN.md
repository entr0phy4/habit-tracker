---
phase: 01-habit-management-daily-logging
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
gap_closure: true
files_modified:
  - src/hooks/useTodayHabits.ts
  - src/hooks/useTodayHabits.test.ts
  - src/pages/TodayPage.tsx
  - src/pages/TodayPage.test.tsx
autonomous: true
requirements:
  - UI-01
  - DATA-01
must_haves:
  truths:
    - Today view shows centered error copy when IndexedDB is unavailable
  artifacts:
    - src/hooks/useTodayHabits.ts
    - src/pages/TodayPage.tsx
    - src/pages/TodayPage.test.tsx
  key_links:
    - useTodayHabits catches Dexie query failures and exposes status error to consumers
    - TodayPage branches on error status before empty/list render and shows UI-SPEC copy centered in viewport
  prohibitions:
    - Must not show blank null screen when IndexedDB fails — error copy is mandatory
    - Must not change successful loading or empty-state behavior
---

<objective>
Close verification gap: Today view must detect IndexedDB unavailability and render centered error copy per UI-SPEC.

Purpose: VERIFICATION.md gap — TodayPage currently returns null while loading and never surfaces storage failures; human UAT item depends on this fix.
Output: useTodayHabits error state, TodayPage error UI branch, automated tests proving error copy renders.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-habit-management-daily-logging/01-VERIFICATION.md
@.planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md
@.planning/phases/01-habit-management-daily-logging/01-04-SUMMARY.md
@src/pages/TodayPage.tsx
@src/hooks/useTodayHabits.ts
@src/infrastructure/db.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Surface IndexedDB failures from useTodayHabits</name>
  <files>src/hooks/useTodayHabits.ts, src/hooks/useTodayHabits.test.ts</files>
  <read_first>src/hooks/useTodayHabits.ts, src/hooks/useTodayHabits.test.ts, src/infrastructure/db.ts, .planning/phases/01-habit-management-daily-logging/01-VERIFICATION.md</read_first>
  <acceptance_criteria>
    - useTodayHabits returns a discriminated union: status loading | error | ready (not a bare array | undefined)
    - status loading when useLiveQuery result is undefined (preserves current TodayPage loading behavior)
    - status error when any Dexie habits or completions query throws (simulates IndexedDB unavailable)
    - status ready with habits array on successful query (existing behavior unchanged)
    - Existing useTodayHabits.test.ts cases updated and still pass for ready/empty/due-today paths
    - New test mocks db.habits.filter or toArray to throw and asserts status error
  </acceptance_criteria>
  <behavior>
    - Test: Dexie query throws → hook returns { status: 'error' }
    - Test: Query pending (undefined) → hook returns { status: 'loading' }
    - Test: Successful query with habits → hook returns { status: 'ready', habits: [...] }
    - Test: Successful query, no habits due → hook returns { status: 'ready', habits: [] }
  </behavior>
  <action>
    Refactor useTodayHabits to wrap the useLiveQuery async callback in try/catch. On catch, return an internal error sentinel from the query function (do not rethrow — dexie-react-hooks would leave consumers stuck on undefined). Map useLiveQuery output in the hook body:

    undefined → { status: 'loading' }
    error sentinel → { status: 'error' }
    habits array → { status: 'ready', habits }

    Export the return type (e.g. TodayHabitsState) for TodayPage typing.

    Update all four existing useTodayHabits.test.ts assertions to read result.current.status and result.current.habits instead of result.current directly.

    Add test "returns error status when IndexedDB query fails": vi.spyOn(db.habits, 'filter').mockImplementation(() => { throw new DOMException('The operation failed', 'AbortError'); }) or equivalent Dexie failure; await waitFor status error.

    Do not add IndexedDB probing outside the live query — detection happens on query failure per gap scope.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useTodayHabits.test.ts</automated>
  </verify>
  <done>Hook exposes loading, error, and ready states; Dexie failure path covered by unit test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TodayPage error UI and component test</name>
  <files>src/pages/TodayPage.tsx, src/pages/TodayPage.test.tsx</files>
  <read_first>src/pages/TodayPage.tsx, .planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md, src/components/layout/AppShell.tsx</read_first>
  <acceptance_criteria>
    - When useTodayHabits returns status error, TodayPage renders AppShell with title Today and centered error message
    - Error copy is exactly: Couldn't save your data. Try refreshing the page.
    - Error container uses flex min-h-[50dvh] flex-col items-center justify-center text-center (mirrors empty-state layout per UI-SPEC TodayView)
    - Error text uses text-sm text-muted-foreground (Body typography on secondary surface)
    - FAB and habit list are not rendered in error state
    - status loading still returns null (no regression)
    - status ready preserves existing empty state and habit list branches
    - TodayPage.test.tsx asserts error copy visible when hook mocked to error status
    - Full vitest suite passes
  </acceptance_criteria>
  <behavior>
    - Test: Mock useTodayHabits → { status: 'error' } → screen.getByText("Couldn't save your data. Try refreshing the page.") is in document
    - Test: Mock useTodayHabits → { status: 'error' } → queryByRole for Add habit FAB absent (FloatingAddButton not mounted)
    - Test: Mock useTodayHabits → { status: 'ready', habits: [] } → empty state heading still renders
  </behavior>
  <action>
    Update TodayPage to destructure useTodayHabits result. Add branch before ready render:

    if (state.status === 'loading') return null;
    if (state.status === 'error') return AppShell with centered paragraph containing exact UI-SPEC copy (Copywriting Contract row "Error state (IndexedDB failure)");

    Map state.habits for the ready branch (replace todayHabits variable).

    Create src/pages/TodayPage.test.tsx with MemoryRouter wrapper. vi.mock('@/hooks/useTodayHabits') returning controllable state per test. Use @testing-library/react render + screen queries.

    Do not add toast for this error — UI-SPEC specifies inline centered copy only on Today view.

    Run full test suite as regression gate.
  </action>
  <verify>
    <automated>npx vitest run src/pages/TodayPage.test.tsx src/hooks/useTodayHabits.test.ts && npx vitest run</automated>
  </verify>
  <done>Today view shows centered UI-SPEC error copy when storage is unavailable; automated test proves rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB → useTodayHabits | Untrusted storage availability; private browsing and quota errors surface here |
| useTodayHabits → TodayPage | Error state must not leak PII — generic user-facing copy only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Information Disclosure | TodayPage error UI | low | mitigate | Use fixed UI-SPEC copy; do not render raw Dexie/IDB exception messages |
| T-01-12 | Denial of Service | IndexedDB unavailable | medium | mitigate | Render recoverable error state with refresh guidance instead of blank screen |
</threat_model>

<verification>
- npx vitest run — all tests green including new TodayPage and useTodayHabits error cases
- Re-run gap truth: Today view shows centered error copy when IndexedDB is unavailable
</verification>

<success_criteria>
- VERIFICATION.md gap closed: IndexedDB failure detection and centered error copy on Today view
- Exact string Couldn't save your data. Try refreshing the page. in rendered output (automated test)
- No regression to loading, empty, or populated Today states
</success_criteria>

## Artifacts this phase produces

| Symbol / Path | Kind | Plan |
|---------------|------|------|
| `TodayHabitsState` | type (exported from useTodayHabits) | 01-05 |
| `TodayPage.test.tsx` | test | 01-05 |
| TodayPage error branch | UI | 01-05 |

<output>
Create `.planning/phases/01-habit-management-daily-logging/01-05-SUMMARY.md` when done
</output>
