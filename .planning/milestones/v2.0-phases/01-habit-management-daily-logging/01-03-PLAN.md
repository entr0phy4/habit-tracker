---
phase: 01-habit-management-daily-logging
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/components/habits/HabitRow.tsx
  - src/components/habits/HabitRow.test.tsx
  - src/pages/TodayPage.tsx
  - src/pages/HabitEditPage.tsx
  - src/pages/ManageHabitsPage.tsx
  - src/hooks/useHabits.ts
  - src/App.tsx
  - src/infrastructure/habitRepository.test.ts
  - src/components/layout/AppShell.tsx
autonomous: true
requirements:
  - HABT-02
  - HABT-03
  - LOG-01
  - UI-01
  - UI-02
must_haves:
  truths:
    - Mobile swipe right on habit row toggles completion; vertical scroll not hijacked (D-09, touch-action pan-y)
    - Desktop row body click toggles completion (D-10)
    - Same gesture toggles off instantly — undo without separate UI (D-12)
    - Row press shows scale(0.98) feedback; min height 44px (D-13, UI-02)
    - User can edit habit name and frequency on /habits/:id/edit (HABT-02, D discretion)
    - User can archive habit from edit page; archived habits hidden from Today (HABT-03)
    - User can restore archived habits from /habits/manage (HABT-03)
    - Hard delete from manage page requires confirm dialog and removes habit + completions (HABT-03)
    - Archiving an already-archived habit remains archived (HABT-03 idempotency probe)
    - Today header links "Manage habits" to /habits/manage (RESEARCH Open Q2)
    - document.visibilitychange refreshes today date key so midnight check-ins use correct day (Pitfall 2)
    - statement: Manage page shows active and archived sections in one scrollable page
      verification: backstop
  artifacts:
    - src/pages/HabitEditPage.tsx
    - src/pages/ManageHabitsPage.tsx
    - src/hooks/useHabits.ts
    - src/components/habits/HabitRow.test.tsx
  key_links:
    - HabitRow trailing CalendarDays button uses stopPropagation — row body toggles, icon navigates (prep for Plan 04 history route)
    - HabitEditPage loads habit via useLiveQuery by :id param
    - ManageHabitsPage lists archived habits with Restore calling habitRepository.update archived false
  prohibitions:
    - Must not navigate to history when user intended to toggle — interaction split per UI-SPEC
    - Must not hard-delete without confirmation dialog
---

<objective>
Deliver swipe/tap check-in polish, habit edit/archive/manage flows, and Today page production UX (empty state, manage link, midnight refresh).

Purpose: Complete HABT-02/03 and full LOG-01/UI-02 interaction requirements.
Output: HabitEditPage, ManageHabitsPage, enhanced HabitRow with pointer-event swipe, visibility handler.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-habit-management-daily-logging/01-CONTEXT.md
@.planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md
@.planning/phases/01-habit-management-daily-logging/01-PATTERNS.md
@.planning/phases/01-habit-management-daily-logging/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HabitRow swipe, tap, and component test</name>
  <files>src/components/habits/HabitRow.tsx, src/components/habits/HabitRow.test.tsx</files>
  <read_first>src/components/habits/HabitRow.tsx, .planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md, .planning/phases/01-habit-management-daily-logging/01-RESEARCH.md</read_first>
  <acceptance_criteria>
    - HabitRow uses Pointer Events with SWIPE_THRESHOLD 50px, touch-action pan-y, pointerType touch for swipe only (D-09)
    - Desktop onClick on row body toggles when not touch (D-10)
    - Completed state: bg-muted, line-through, muted-foreground (D-11)
    - Trailing ghost icon button CalendarDays 44x44 with aria-label includes habit name; stopPropagation on click (UI-SPEC interaction split)
    - HabitRow.test.tsx asserts min-h-11 class and click fires onToggle
    - npx vitest run src/components/habits/HabitRow.test.tsx passes
  </acceptance_criteria>
  <action>
    Upgrade HabitRow per RESEARCH Pattern 5 and UI-SPEC Interaction States: pointerdown/up tracking, swipe translate with green reveal strip at 20% opacity during drag, snap back on release. Require deltaX > threshold and deltaX > deltaY for swipe completion.

    Add trailing Button variant ghost size icon with lucide CalendarDays — onClick navigates to /habits/:id/history (route stub OK until Plan 04 implements page). stopPropagation prevents toggle.

    Write HabitRow.test.tsx with @testing-library/react: render row, fireEvent.click triggers onToggle; query for min-h-11.

    Per A1 assumption: no react-swipeable library.
  </action>
  <verify>
    <automated>npx vitest run src/components/habits/HabitRow.test.tsx</automated>
  </verify>
  <done>Mobile swipe and desktop tap both toggle; history button isolated from toggle zone.</done>
</task>

<task type="auto">
  <name>Task 2: Edit, archive, and manage habit pages</name>
  <files>src/pages/HabitEditPage.tsx, src/pages/ManageHabitsPage.tsx, src/hooks/useHabits.ts, src/App.tsx, src/infrastructure/habitRepository.test.ts</files>
  <read_first>src/components/habits/HabitForm.tsx, .planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md, .planning/phases/01-habit-management-daily-logging/01-CONTEXT.md</read_first>
  <acceptance_criteria>
    - /habits/:id/edit route in App.tsx renders HabitEditPage (HABT-02)
    - HabitEditPage pre-fills HabitForm from useLiveQuery; submit calls habitRepository.update; toast "Habit updated"
    - Archive button sets archived true, toast "Habit archived", navigate to /
    - Delete button shows destructive confirm dialog per UI-SPEC copy; confirm calls habitRepository.delete
    - /habits/manage lists active habits with edit links and archived section with Restore
    - habitRepository.test.ts covers update, archive hides from active filter, delete cascades completions
    - npx vitest run src/infrastructure/habitRepository.test.ts passes
  </acceptance_criteria>
  <action>
    Implement useHabits hook (all non-archived) and ManageHabitsPage per UI-SPEC: header Back to /, heading "Manage habits", rows tap to edit, archived section with Restore via update archived false. Empty archived shows "No archived habits".

    Implement HabitEditPage mirroring create layout: Back to /habits/manage on cancel, "Edit habit" title, HabitForm with initial values, "Save changes" submit, Archive and Delete text buttons at bottom per UI-SPEC.

    Register routes in App.tsx. Wire TodayPage header "Manage habits" link to /habits/manage (D discretion / RESEARCH Q2).

    Extend habitRepository tests for HABT-02 and HABT-03 behaviors. Archive preferred over delete (Pitfall 6).
  </action>
  <verify>
    <automated>npx vitest run src/infrastructure/habitRepository.test.ts</automated>
  </verify>
  <done>User can edit, archive, restore, and permanently delete habits.</done>
</task>

<task type="auto">
  <name>Task 3: Today page polish and midnight refresh</name>
  <files>src/pages/TodayPage.tsx, src/components/layout/AppShell.tsx, src/components/habits/FloatingAddButton.tsx</files>
  <read_first>src/pages/TodayPage.tsx, .planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - TodayPage matches UI-SPEC TodayView: header Today + Manage link, list gap-2, padding-bottom clears FAB
    - Empty state uses exact copy from UI-SPEC Copywriting Contract
    - visibilitychange listener on TodayPage forces today key refresh when document becomes visible (Pitfall 2)
    - AppShell max-w-[480px] mx-auto at md breakpoint
    - npx vitest run exits 0
  </acceptance_criteria>
  <action>
    Polish TodayPage layout per UI-SPEC spacing tokens. Implement todayKey state updated on visibilitychange to re-run useTodayHabits dependency (pass todayKey into hook or remount list).

    Ensure FAB FloatingAddButton aria-label "Add habit", 56px circle, primary color.

    Finalize AppShell responsive column. Confirm dark theme tokens unchanged (UI-01).

    Run full test suite.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Today view matches UI contract; midnight/tab-resume date refresh works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → edit form | Name updates |
| Destructive action → IndexedDB | Hard delete cascade |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | habitRepository.delete | medium | mitigate | Confirm dialog with explicit destructive copy before cascade delete |
| T-01-07 | Repudiation | archive action | low | accept | Reversible via manage screen; toast feedback |
| T-01-08 | Information Disclosure | habit name in aria-label | low | accept | Local-only; labels improve a11y |
</threat_model>

<verification>
- npx vitest run — full suite green
- Manual UAT: swipe on mobile viewport, tap on desktop, edit habit, archive and restore
</verification>

<success_criteria>
- HABT-02 and HABT-03 complete
- LOG-01 mobile swipe + desktop tap both work
- UI-02 touch targets verified by HabitRow.test.tsx
</success_criteria>

## Artifacts this phase produces

| Symbol / Path | Kind | Plan |
|---------------|------|------|
| `useHabits` | hook | 01-03 |
| `HabitEditPage` | page | 01-03 |
| `ManageHabitsPage` | page | 01-03 |
| `HabitRow.test.tsx` | test file | 01-03 |

<output>
Create `.planning/phases/01-habit-management-daily-logging/01-03-SUMMARY.md` when done
</output>
