---
phase: 06-dashboard-aggregate-uat-residual
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/pages/DashboardPage.tsx
  - src/pages/DashboardPage.test.tsx
  - src/components/habits/StatCards.test.tsx
  - .planning/phases/02-streaks-statistics/02-UAT.md
autonomous: true
requirements:
  - ENH-03
  - QA-01
must_haves:
  truths:
    - Panel shows "Tasa general" + whole-number percent above card list when active habits exist (D-01, D-02, D-03)
    - Overall summary hidden on empty ready state; shows 0% when denominator 0 with habits present (D-04)
    - Dashboard error status shows soft Spanish copy with no raw exception text; summary hidden (D-14)
    - DashboardCard unchanged — no per-card rate (D-01)
    - StatCards renders full integers >999 without K/M (D-10)
    - 02-UAT.md items #1–#4 marked passed with evidence pointers (D-09, D-10, D-15)
  artifacts:
    - src/pages/DashboardPage.tsx
    - src/pages/DashboardPage.test.tsx
    - src/components/habits/StatCards.test.tsx
    - .planning/phases/02-streaks-statistics/02-UAT.md
  key_links:
    - DashboardPage reads status/items/overallRate from useDashboardHabits
    - Summary only when status==='ready' && items.length>0
    - StatCards.test asserts 1234/5678
    - 02-UAT.md cites hook/page/StatCards tests
  prohibitions:
    - Must not add rate to DashboardCard
    - Must not put overall rate into AppShell title string
    - Must not show error.message / Dexie strings
    - Must not redesign StatCards layout — verification only for #4
---

## Phase Goal

**As a** habit tracker user, **I want** to see my overall completion rate on Panel and trust that streak/stat surfaces fail safely, **so that** one glance shows progress and Phase 2 UAT residuals are closed.

<objective>
Render ENH-03 Panel summary per UI-SPEC and close QA-01 documentation: DashboardPage wiring, StatCards large-integer assertion, update `02-UAT.md`.

Purpose: Ship user-visible overall rate + finish Phase 2 residual closeout.
Output: Updated DashboardPage (+ tests), StatCards test, `02-UAT.md` results.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dashboard-aggregate-uat-residual/06-CONTEXT.md
@.planning/phases/06-dashboard-aggregate-uat-residual/06-UI-SPEC.md
@.planning/phases/06-dashboard-aggregate-uat-residual/06-PATTERNS.md
@.planning/phases/06-dashboard-aggregate-uat-residual/06-VALIDATION.md
@src/pages/DashboardPage.tsx
@src/pages/DashboardPage.test.tsx
@src/pages/TodayPage.tsx
@src/hooks/useDashboardHabits.ts
@src/components/habits/StatCards.tsx
@src/components/habits/StatCards.test.tsx
@.planning/phases/02-streaks-statistics/02-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Panel OverallRate summary + error/empty states</name>
  <files>src/pages/DashboardPage.tsx, src/pages/DashboardPage.test.tsx</files>
  <read_first>src/pages/DashboardPage.tsx, src/pages/DashboardPage.test.tsx, src/pages/TodayPage.tsx, .planning/phases/06-dashboard-aggregate-uat-residual/06-UI-SPEC.md, src/hooks/useDashboardHabits.ts</read_first>
  <acceptance_criteria>
    - DashboardPage uses status from useDashboardHabits (loading → null; error → soft message "No se pudieron cargar los hábitos."; ready empty → existing empty state without rate; ready with items → "Tasa general" + `${overallRate}%` above ul)
    - DashboardCard props unchanged (habit + currentStreak only)
    - DashboardPage.test mocks updated return shape including overallRate + status
    - Tests: shows Tasa general and percent when populated; hides rate on empty; shows soft error copy on status error; no exception text assertions
    - npx vitest run src/pages/DashboardPage.test.tsx passes
  </acceptance_criteria>
  <action>
    Implement Panel summary and error branch per 06-UI-SPEC.md (D-01–D-04, D-14). Update DashboardPage.test.tsx mocks and cases. Keep Spanish empty-state copy unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/pages/DashboardPage.test.tsx -x</automated>
  </verify>
  <done>ENH-03 Panel UI shipped with safe empty/error handling.</done>
</task>

<task type="auto">
  <name>Task 2: StatCards large integers + close 02-UAT residual</name>
  <files>src/components/habits/StatCards.test.tsx, .planning/phases/02-streaks-statistics/02-UAT.md</files>
  <read_first>src/components/habits/StatCards.tsx, src/components/habits/StatCards.test.tsx, .planning/phases/02-streaks-statistics/02-UAT.md, .planning/phases/06-dashboard-aggregate-uat-residual/06-CONTEXT.md</read_first>
  <acceptance_criteria>
    - StatCards.test includes case current=1234 longest=5678 → visible full integers, no K/M (D-10 / UAT #4)
    - 02-UAT.md tests 1–4 result: [passed] with short evidence notes (reactivity → useDashboardHabits/toggle test + Flame/StatCards live query; #2 useStreak.test; #3 useHabitStats.test; #4 StatCards.test; note heatmap replaces obsolete dot-grid wording for #1)
    - Summary counts updated: passed 4, pending 0
    - npx vitest run src/components/habits/StatCards.test.tsx src/pages/DashboardPage.test.tsx passes
  </acceptance_criteria>
  <action>
    Add StatCards large-integer test (should already pass against String(n)). Update 02-UAT.md results and summary for QA-01 closeout (D-09, D-10, D-15). Do not redesign StatCards.
  </action>
  <verify>
    <automated>npx vitest run src/components/habits/StatCards.test.tsx src/pages/DashboardPage.test.tsx -x</automated>
  </verify>
  <done>QA-01 residual documented closed; integer display verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook error status → DashboardPage | Only soft curated copy may render |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | DashboardPage error UI | low | mitigate | Hard-coded soft Spanish string; never interpolate errors |
</threat_model>

<verification>
- npx vitest run src/pages/DashboardPage.test.tsx src/components/habits/StatCards.test.tsx -x
- rg "Tasa general" src/pages/DashboardPage.tsx
- rg "No se pudieron cargar" src/pages/DashboardPage.tsx
- rg "overallRate|rate" src/components/dashboard/DashboardCard.tsx — no overall rate prop
- rg "\\[passed\\]" .planning/phases/02-streaks-statistics/02-UAT.md — four items
</verification>

<success_criteria>
- ENH-03 user-visible on Panel
- QA-01 Phase 2 UAT #1–#4 closed in 02-UAT.md with automated evidence
- Roadmap Phase 6 success criteria 1–4 satisfiable after this plan
</success_criteria>
