---
phase: 09-pwa-install-manifest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/manifest.webmanifest
  - public/icons/icon-source.svg
  - public/icons/icon-192.png
  - public/icons/icon-512.png
  - public/icons/maskable-192.png
  - public/icons/maskable-512.png
  - public/icons/apple-touch-icon.png
  - index.html
  - src/platform/install.ts
  - src/platform/install.test.ts
  - src/platform/manifest.test.ts
  - src/test/setup.ts
autonomous: true
requirements:
  - PWA-01
must_haves:
  truths:
    - manifest.webmanifest is served with name Habit Tracker, display standalone, theme/background #0d1117, and separate 192/512 any + maskable icon entries (D-04, D-05, D-19, PWA-01)
    - index.html links manifest, theme-color meta, and apple-touch-icon before JS boots (D-20)
    - Flame streak icons use #3fb950 on #0d1117 with maskable safe-zone padding (D-01, D-02, D-03)
    - platform/install.ts exposes standalone detection, iOS branch, engagement gating, 7-day dismiss, and module-scoped beforeinstallprompt capture (D-06, D-09, D-11)
    - No service worker registration or vite-plugin-pwa dependency added in this phase
  artifacts:
    - public/manifest.webmanifest
    - public/icons/icon-192.png
    - public/icons/icon-512.png
    - public/icons/maskable-192.png
    - public/icons/maskable-512.png
    - public/icons/apple-touch-icon.png
    - src/platform/install.ts
    - src/platform/install.test.ts
    - src/platform/manifest.test.ts
  key_links:
    - index.html manifest link href must match public/manifest.webmanifest path
    - manifest icon src paths must resolve to committed PNGs under public/icons/
    - install.ts module-scope listener must load via side-effect import before React hydration
  ui_considerations:
    - empty settings-install-section — standalone hides section entirely (D-16); detection functions ready for Plan 03
    - populated install-banner — isEngaged and isDismissed gates implemented for Plan 02
    - populated ios-install-modal — shouldShowIosInstallFlow ready for Plan 02 modal
    - zero-one-many settings-install-section — isIosSafari vs Chromium branch helpers exported
    - overflow ios-install-modal — N/A this plan (UI in Plan 02)
    - long-text install-banner — N/A this plan (UI in Plan 02)
    - long-text settings-install-section — N/A this plan (UI in Plan 03)
  prohibitions:
    - Must not add vite-plugin-pwa or workbox packages
    - Must not register a service worker or add SW registration script to index.html
    - Must not call browser notification permission APIs
    - Must not add blocking first-visit install modal logic
---

<objective>
Ship static PWA foundation: manifest, maskable flame icons, index.html head tags, and the pure platform/install.ts module with unit tests.

Purpose: Chrome and Safari can discover install metadata and UI plans can import detection, engagement, and deferred-prompt helpers without duplicating platform logic.
Output: public/manifest.webmanifest, public/icons/*, updated index.html, src/platform/install.ts + tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-pwa-install-manifest/09-CONTEXT.md
@.planning/phases/09-pwa-install-manifest/09-RESEARCH.md
@.planning/phases/09-pwa-install-manifest/09-PATTERNS.md
@.planning/phases/09-pwa-install-manifest/09-VALIDATION.md
@.planning/phases/09-pwa-install-manifest/09-UI-SPEC.md
@src/index.css
@public/favicon.svg
@index.html
@src/test/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flame icons and static manifest</name>
  <files>public/icons/icon-source.svg, public/icons/icon-192.png, public/icons/icon-512.png, public/icons/maskable-192.png, public/icons/maskable-512.png, public/icons/apple-touch-icon.png, public/manifest.webmanifest</files>
  <read_first>public/favicon.svg, src/index.css, .planning/phases/09-pwa-install-manifest/09-CONTEXT.md, .planning/phases/09-pwa-install-manifest/09-UI-SPEC.md, .planning/phases/09-pwa-install-manifest/09-RESEARCH.md</read_first>
  <acceptance_criteria>
    - icon-source.svg depicts upward streak flame in #3fb950 on #0d1117 rounded square (~25% corner radius per D-03)
    - PNG exports at 192, 512 (any purpose), 512 and 192 maskable with flame in central 80% safe zone on opaque #0d1117 background (D-01, D-02, D-03, D-05)
    - apple-touch-icon.png is 180×180 with same visual (D-05)
    - manifest.webmanifest has name and short_name Habit Tracker (D-04), start_url /, scope /, display standalone, theme_color and background_color #0d1117 (D-19)
    - manifest icons array lists four entries with purpose any or maskable separately — not combined (D-05)
    - Spanish description field allowed in manifest; name stays English per D-04
  </acceptance_criteria>
  <action>
    Create public/icons/ directory. Design flame motif distinct from green-check favicon per D-01. Export committed PNGs (manual export or CLI — no new npm icon pipeline required). Write public/manifest.webmanifest as static JSON with $schema from schemastore, description in Spanish optional (e.g. Seguimiento de hábitos con rachas y progreso visual). Icon src paths use /icons/ prefix. Do not modify vite.config.ts or add PWA plugins.
  </action>
  <verify>
    <automated>test -f public/manifest.webmanifest && test -f public/icons/icon-192.png && test -f public/icons/maskable-512.png && test -f public/icons/apple-touch-icon.png</automated>
  </verify>
  <done>All icon PNGs and manifest.webmanifest exist with correct fields per D-01 through D-05 and D-19.</done>
</task>

<task type="auto">
  <name>Task 2: index.html PWA head tags</name>
  <files>index.html</files>
  <read_first>index.html, .planning/phases/09-pwa-install-manifest/09-RESEARCH.md, .planning/phases/09-pwa-install-manifest/09-PATTERNS.md</read_first>
  <acceptance_criteria>
    - link rel manifest href /manifest.webmanifest present
    - meta name theme-color content #0d1117 present (D-20)
    - apple-mobile-web-app-capable yes, status-bar-style black-translucent, title Habit Tracker
    - link rel apple-touch-icon href /icons/apple-touch-icon.png sizes 180x180
    - No script registering service worker; no vite-plugin-pwa virtual imports
    - html lang en and class dark unchanged
  </acceptance_criteria>
  <action>
    Append PWA head tags inside index.html head before closing head tag per RESEARCH Pattern 1 and D-20. Keep existing favicon.svg link. Do not add inline SW registration or workbox scripts — offline shell is Phase 10 scope.
  </action>
  <verify>
    <automated>grep -c 'manifest.webmanifest' index.html | grep -q '^1$' && grep -c 'theme-color' index.html | grep -q '^1$' && grep -c 'apple-touch-icon' index.html | grep -q '^1$'</automated>
  </verify>
  <done>Browser discovers manifest and theme colors on first paint without JS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: platform/install.ts module and unit tests</name>
  <files>src/platform/install.ts, src/platform/install.test.ts, src/platform/manifest.test.ts, src/test/setup.ts</files>
  <read_first>src/domain/schedule.ts, src/domain/backupSchema.test.ts, src/test/setup.ts, .planning/phases/09-pwa-install-manifest/09-RESEARCH.md, .planning/phases/09-pwa-install-manifest/09-PATTERNS.md, .planning/phases/09-pwa-install-manifest/09-VALIDATION.md</read_first>
  <acceptance_criteria>
    - install.ts exports isStandaloneDisplayMode, isIosSafari, shouldShowIosInstallFlow, shouldShowChromiumInstallFlow
    - install.ts exports recordSessionVisit, markFirstCheckIn, isEngaged (visits ge 2 OR hasCheckedIn), dismissInstallBanner, isDismissed (7-day snooze per D-09)
    - install.ts exports subscribeInstallPrompt, getDeferredPrompt, triggerInstallPrompt with module-scope beforeinstallprompt and appinstalled listeners (preventDefault on BIP)
    - localStorage keys namespaced ht_pwa_* only; no Dexie usage
    - install.test.ts covers standalone matchMedia, engagement visit count, dismiss timers with fake timers, iOS branch
    - manifest.test.ts reads public/manifest.webmanifest via fs, asserts required fields and icon purposes
    - setup.ts extended with configurable matchMedia helper and localStorage clear pattern for tests
  </acceptance_criteria>
  <behavior>
    - isEngaged false when visitCount 1 and no check-in; true when visitCount 2 or markFirstCheckIn called
    - isDismissed true when dismissedUntil timestamp in future
    - isStandaloneDisplayMode true when display-mode standalone or navigator.standalone true
    - shouldShowIosInstallFlow true on iOS UA when not standalone
    - manifest.test asserts display standalone and theme_color #0d1117
  </behavior>
  <action>
    Create src/platform/install.ts as first platform-tier module mirroring domain/ pure-export style. Initialize window listeners at module load — not only in useEffect — per RESEARCH Pattern 2. Engagement trigger per D-06: implement visit count ge 2 OR hasCheckedIn flag; recordSessionVisit increments once per session. dismissInstallBanner stores Date.now plus 7 days. Extend src/test/setup.ts with matchMedia mock utilities and document beforeinstallprompt test helper. Write install.test.ts and manifest.test.ts per VALIDATION wave-0 map. Do not import React in install.ts.
  </action>
  <verify>
    <automated>npm test -- src/platform/install.test.ts src/platform/manifest.test.ts -x</automated>
  </verify>
  <done>Platform install module is tested and ready for UI consumers in Plans 02–03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static manifest (public/) | Served as-is; no user input |
| localStorage (ht_pwa_*) | Client-only ephemeral UI flags; no secrets |
| beforeinstallprompt handler | Browser-trusted install API; user gesture required for prompt() |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | IosInstallModal (Plan 02) | medium | mitigate | Static Spanish strings only in UI tasks; no HTML injection APIs |
| T-09-02 | Spoofing | triggerInstallPrompt | low | mitigate | Call native prompt() only on explicit user button click |
| T-09-03 | Information disclosure | localStorage keys | low | accept | Namespaced ht_pwa_* keys; no PII stored |
| T-09-04 | Tampering | manifest.webmanifest | low | mitigate | manifest.test.ts asserts required fields at build time |
| T-09-SC | Tampering | npm installs | high | mitigate | Zero new packages in Phase 9; RESEARCH Package Legitimacy Audit defers vite-plugin-pwa to Phase 10 |

</threat_model>

<verification>
- npm test -- src/platform/install.test.ts src/platform/manifest.test.ts -x passes
- npm run build succeeds with no SW output in dist/ (no sw.js or registerSW in bundle)
- DevTools Application tab shows manifest with 192 and 512 icons after npm run dev
</verification>

<success_criteria>
- PWA-01 static installability prerequisites shipped without service worker
- All D-01 through D-05, D-19, D-20 decisions implemented
- platform/install.ts exports consumed by Plans 02–03
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Plan |
|---------------|------|------|
| public/manifest.webmanifest | static config | 01 |
| public/icons/icon-source.svg | design source | 01 |
| public/icons/icon-192.png | static asset | 01 |
| public/icons/icon-512.png | static asset | 01 |
| public/icons/maskable-192.png | static asset | 01 |
| public/icons/maskable-512.png | static asset | 01 |
| public/icons/apple-touch-icon.png | static asset | 01 |
| recordSessionVisit | function export | 01 |
| markFirstCheckIn | function export | 01 |
| isEngaged | function export | 01 |
| dismissInstallBanner | function export | 01 |
| isDismissed | function export | 01 |
| isStandaloneDisplayMode | function export | 01 |
| isIosSafari | function export | 01 |
| shouldShowIosInstallFlow | function export | 01 |
| shouldShowChromiumInstallFlow | function export | 01 |
| subscribeInstallPrompt | function export | 01 |
| getDeferredPrompt | function export | 01 |
| triggerInstallPrompt | function export | 01 |
| BeforeInstallPromptEvent | type export | 01 |

## Multi-Source Coverage Audit

| Source | Item | Plan | Status |
|--------|------|------|--------|
| GOAL | Installable standalone app with manifest and icons | 01 | COVERED |
| GOAL | Contextual install prompt (logic foundation) | 01 (install.ts) + 02 (UI) | COVERED |
| GOAL | iOS guidance (detection foundation) | 01 + 02 | COVERED |
| REQ PWA-01 | manifest, icons, standalone display | 01 | COVERED |
| RESEARCH | Static manifest, no vite-plugin-pwa | 01 | COVERED |
| RESEARCH | Module-scope BIP capture | 01 | COVERED |
| RESEARCH | localStorage engagement/dismiss | 01 | COVERED |
| CONTEXT D-01–D-05 | Icons and manifest branding | 01 Task 1 | COVERED |
| CONTEXT D-19–D-20 | Manifest theme + index meta | 01 Tasks 1–2 | COVERED |
| CONTEXT D-06, D-09, D-11 | Engagement/dismiss/iOS detection | 01 Task 3 | COVERED |
| CONTEXT D-07–D-08, D-10, D-12–D-18, D-21 | Install UI copy and flows | 02, 03 | COVERED (downstream) |

<output>
Create `.planning/phases/09-pwa-install-manifest/09-01-SUMMARY.md` when done
</output>
