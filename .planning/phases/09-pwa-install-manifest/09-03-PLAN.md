---
phase: 09-pwa-install-manifest
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/pages/SettingsPage.tsx
  - src/pages/SettingsPage.test.tsx
autonomous: true
requirements:
  - PWA-01
must_haves:
  truths:
    - Settings shows Instalar app section above Copia de seguridad when not standalone (D-15, D-16)
    - Section entirely hidden in standalone/display-mode — no empty placeholder (D-16, UI empty state)
    - Android with deferred prompt shows Instalar button triggering triggerInstallPrompt (D-17)
    - Android without prompt shows fallback browser menu instructions in Spanish (D-17, D-21)
    - iOS Safari shows Cómo instalar opening shared IosInstallModal (D-18)
    - Settings description Instala la app para acceder sin conexión y más rápido per UI-SPEC
  artifacts:
    - src/pages/SettingsPage.tsx
    - src/pages/SettingsPage.test.tsx
  key_links:
    - SettingsInstallSection inline above backup section uses same section pattern as Copia de seguridad
    - IosInstallModal imported from Plan 02 shared component
    - isStandaloneDisplayMode guard wraps entire install section
  ui_considerations:
    - empty settings-install-section — section omitted when standalone; queryByText Instalar app null in tests
    - populated settings-install-section — heading, description, platform CTA per variant
    - zero-one-many settings-install-section — three variants hidden standalone, Android Instalar plus fallback, iOS Cómo instalar
    - overflow ios-install-modal — N/A; reuses Plan 02 modal
    - long-text install-banner — N/A Plan 03
    - long-text settings-install-section — Android fallback Abre el menú del navegador wraps at 320px without clipping CTA (backstop)
  prohibitions:
    - Must not show install section when already installed standalone
    - Must not call browser notification permission APIs
    - Must not add vite-plugin-pwa or service worker
    - Must not duplicate IosInstallModal — import from components/pwa
---

<objective>
Add permanent Instalar app Settings section as fallback when banner is dismissed, with platform-specific install actions and shared iOS modal.

Purpose: Users who dismiss the 7-day banner can still install from Ajustes; installed users see no redundant install UI.
Output: SettingsPage install section + extended tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-pwa-install-manifest/09-CONTEXT.md
@.planning/phases/09-pwa-install-manifest/09-UI-SPEC.md
@.planning/phases/09-pwa-install-manifest/09-PATTERNS.md
@.planning/phases/09-pwa-install-manifest/09-VALIDATION.md
@src/pages/SettingsPage.tsx
@src/pages/SettingsPage.test.tsx
@src/components/pwa/IosInstallModal.tsx
@src/platform/install.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SettingsPage install section tests (RED)</name>
  <files>src/pages/SettingsPage.test.tsx</files>
  <read_first>src/pages/SettingsPage.test.tsx, .planning/phases/09-pwa-install-manifest/09-VALIDATION.md, .planning/phases/09-pwa-install-manifest/09-PATTERNS.md</read_first>
  <acceptance_criteria>
    - vi.mock platform/install with toggleable isStandaloneDisplayMode
    - When standalone true — queryByText Instalar app is null (D-16 empty state)
    - When not standalone — getByText Instalar app heading present
    - When not standalone — install section appears before Copia de seguridad in DOM order (D-15)
    - iOS mock shouldShowIosInstallFlow true — Cómo instalar button visible; click opens modal with step text
    - Android mock with deferred prompt — Instalar button visible
    - Android mock without prompt — fallback instruction text visible
    - Existing backup export/import tests remain green
  </acceptance_criteria>
  <behavior>
    - Standalone hides install section completely
    - Non-standalone shows Spanish section heading
    - iOS Cómo instalar opens modal with recordatorios intro text
  </behavior>
  <action>
    Extend existing SettingsPage.test.tsx per PATTERNS mock style. Add platform/install mock alongside existing sonner and backupService mocks. Write failing tests for install section visibility, DOM order above backup, and platform branches before any SettingsPage.tsx changes. Run tests and confirm new install-section cases fail (RED). Do not break existing Spanish settings chrome tests.
  </action>
  <verify>
    <automated>npm test -- src/pages/SettingsPage.test.tsx -x; test $? -ne 0</automated>
  </verify>
  <done>Failing install-section tests committed; RED state confirmed before UI implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings install section UI (GREEN)</name>
  <files>src/pages/SettingsPage.tsx</files>
  <read_first>src/pages/SettingsPage.tsx, .planning/phases/09-pwa-install-manifest/09-UI-SPEC.md, .planning/phases/09-pwa-install-manifest/09-PATTERNS.md, src/components/pwa/IosInstallModal.tsx, src/platform/install.ts</read_first>
  <acceptance_criteria>
    - New section inserted above Copia de seguridad with mb-8 separation (D-15)
    - Entire section wrapped in NOT isStandaloneDisplayMode guard (D-16)
    - h2 Instalar app with text-xs font-semibold text-muted-foreground matching backup section style
    - p description Instala la app para acceder sin conexión y más rápido (D-21)
    - When shouldShowIosInstallFlow — Button Cómo instalar min-h-11 w-full opens IosInstallModal (D-18)
    - When Chromium and getDeferredPrompt available — Button Instalar min-h-11 w-full calls triggerInstallPrompt (D-17)
    - When Chromium without deferred prompt — show fallback text Abre el menú del navegador (⋮) y selecciona «Instalar app» o «Añadir a la pantalla de inicio» as text-sm text-muted-foreground (D-17, D-21)
    - iosModalOpen state and IosInstallModal sibling to existing ConfirmDialog
    - Desktop unsupported browser — fallback text only, no primary button
  </acceptance_criteria>
  <action>
    Implement SettingsPage install section to satisfy Task 1 tests. Follow existing Copia de seguridad section pattern per PATTERNS. Import isStandaloneDisplayMode, shouldShowIosInstallFlow, getDeferredPrompt, triggerInstallPrompt, subscribeInstallPrompt from platform/install. Import IosInstallModal from components/pwa. Use useSyncExternalStore or local state synced via subscribeInstallPrompt to react when deferred prompt becomes available. Keep inline section — do not extract SettingsInstallSection file unless it improves readability without expanding scope. Spanish copy exactly from UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>npm test -- src/pages/SettingsPage.test.tsx -x</automated>
  </verify>
  <done>Settings permanent install entry live per D-15 through D-18; all install-section tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Phase scope fence verification</name>
  <files>package.json, vite.config.ts</files>
  <read_first>package.json, vite.config.ts, .planning/phases/09-pwa-install-manifest/09-CONTEXT.md, .planning/phases/09-pwa-install-manifest/09-RESEARCH.md</read_first>
  <acceptance_criteria>
    - package.json has no vite-plugin-pwa or workbox dependency added
    - vite.config.ts unchanged from pre-phase or has no VitePWA plugin import
    - Full test suite passes
    - Production build dist/ contains manifest.webmanifest and icons but no service worker registration script
    - No browser notification permission API calls in src/ from Phase 9 changes
  </acceptance_criteria>
  <action>
    Run full npm test and npm run build. Grep src/ for notification permission request patterns and service worker registration patterns introduced in this phase — expect zero matches in new PWA code. Confirm dist/ output includes manifest and icon assets. Document any manual UAT items (real iOS device, Lighthouse installability) for phase verification — do not implement Phase 10 offline scope.
  </action>
  <verify>
    <automated>npm test && npm run build && test ! -f dist/sw.js && ! grep -r registerSW dist/ && test -f dist/manifest.webmanifest</automated>
  </verify>
  <done>Phase 9 scope fence verified — manifest and install UX only; SW deferred to Phase 10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SettingsPage install section | Renders static Spanish instructions; no user HTML input |
| triggerInstallPrompt from Settings | User gesture on Instalar button only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | Settings fallback copy | low | mitigate | Static string literals for Android menu instructions |
| T-09-02 | Spoofing | Instalar button | low | mitigate | Native prompt API only when browser provides deferred event |
| T-09-SC | Tampering | npm installs | high | mitigate | Scope fence task confirms no new PWA plugin deps |

</threat_model>

<verification>
- npm test full suite green
- npm run build && test -f dist/manifest.webmanifest
- Settings install section hidden when display-mode standalone mocked
- Phase 9 ROADMAP success criteria 1–4 addressable via combined Plans 01–03
</verification>

<success_criteria>
- PWA-01 complete via manifest + install UX + Settings fallback
- D-15 through D-18 and D-21 implemented
- UI Considerations zero-one-many settings variants covered
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Plan |
|---------------|------|------|
| SettingsPage | modified — install section | 03 |
| SettingsPage.test.tsx | extended tests | 03 |

## Multi-Source Coverage Audit

| Source | Item | Plan | Status |
|--------|------|------|--------|
| GOAL | Permanent install fallback in Settings | 03 | COVERED |
| REQ PWA-01 | Home screen install path via Settings | 03 | COVERED |
| CONTEXT D-15–D-18 | Settings section placement and platform CTAs | 03 | COVERED |
| CONTEXT D-16 | Hide when standalone | 03 Task 1–2 | COVERED |
| CONTEXT D-21 | Spanish copy | 03 | COVERED |
| UI-SPEC empty settings-install-section | Hidden in standalone | 03 Task 1 | COVERED |
| UI-SPEC populated settings-install-section | Heading + CTA variants | 03 Task 2 | COVERED |
| UI-SPEC zero-one-many | Android/iOS/hidden variants | 03 | COVERED |
| UI-SPEC long-text settings backstop | 320px fallback wrap | 03 Task 3 manual note | COVERED |
| RESEARCH | No vite-plugin-pwa Phase 9 | 03 Task 3 | COVERED |
| DEFERRED | SW, offline, notifications | — | EXCLUDED |

<output>
Create `.planning/phases/09-pwa-install-manifest/09-03-SUMMARY.md` when done
</output>
