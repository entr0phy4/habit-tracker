---
phase: 09-pwa-install-manifest
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/components/pwa/InstallBanner.tsx
  - src/components/pwa/IosInstallModal.tsx
  - src/components/pwa/InstallBanner.test.tsx
  - src/components/pwa/IosInstallModal.test.tsx
  - src/components/layout/MainLayout.tsx
  - src/App.tsx
  - src/hooks/useToggleCompletion.ts
  - src/pages/TodayPage.tsx
  - src/pages/DashboardPage.tsx
autonomous: true
requirements:
  - PWA-01
must_haves:
  truths:
    - Engaged eligible users on tabbed routes see bottom install banner above BottomTabBar with Spanish copy per UI-SPEC (D-07, D-08, D-21)
    - Banner hidden when standalone, not engaged, or within 7-day dismiss window (D-06, D-09, D-16)
    - Android Instalar app triggers deferred beforeinstallprompt on user tap (D-10)
    - iOS Instalar app opens numbered-step modal with reminder prerequisite copy (D-11, D-12, D-13, D-14)
    - Ahora no dismisses banner for 7 days via dismissInstallBanner (D-09)
    - recordSessionVisit runs once per app session on mount (D-06)
  artifacts:
    - src/components/pwa/InstallBanner.tsx
    - src/components/pwa/IosInstallModal.tsx
    - src/components/pwa/InstallBanner.test.tsx
    - src/components/pwa/IosInstallModal.test.tsx
  key_links:
    - MainLayout mounts InstallBanner only on routes with BottomTabBar (slash and dashboard)
    - App.tsx or main.tsx side-effect imports platform/install for early BIP capture
    - InstallBanner uses useSyncExternalStore with subscribeInstallPrompt for Chromium prompt availability
    - Banner bottom offset calc(3.5rem + env(safe-area-inset-bottom)) clears z-40 tab bar
  ui_considerations:
    - empty settings-install-section — N/A Plan 02 (Plan 03)
    - populated install-banner — headline Instala para usar sin conexión, body Accede más rápido desde tu pantalla de inicio, CTAs Instalar app and Ahora no
    - populated ios-install-modal — title Instalar en la pantalla de inicio, intro mentions recordatorios, three numbered steps, Entendido close
    - zero-one-many settings-install-section — N/A Plan 02
    - overflow ios-install-modal — max-w-sm panel with p-4 viewport inset; step 2 guillemet text wraps at text-sm
    - long-text install-banner — verify no horizontal scroll at 320px viewport (backstop)
    - long-text settings-install-section — N/A Plan 02
  prohibitions:
    - Must not show banner on Settings, habit forms, or history routes
    - Must not show blocking first-visit modal
    - Must not call browser notification permission APIs
    - Must not add vite-plugin-pwa or service worker
---

<objective>
Deliver contextual install banner and shared iOS install modal on tabbed routes, wired to platform/install.ts engagement and prompt APIs.

Purpose: Users discover install value after engagement without blocking first visit; iOS users get step-by-step Add to Home Screen guidance including future reminders context.
Output: InstallBanner, IosInstallModal, MainLayout mount, App session init, component tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-pwa-install-manifest/09-CONTEXT.md
@.planning/phases/09-pwa-install-manifest/09-UI-SPEC.md
@.planning/phases/09-pwa-install-manifest/09-PATTERNS.md
@.planning/phases/09-pwa-install-manifest/09-VALIDATION.md
@src/components/habits/ConfirmDialog.tsx
@src/components/habits/FloatingAddButton.tsx
@src/components/layout/BottomTabBar.tsx
@src/components/layout/MainLayout.tsx
@src/App.tsx
@src/hooks/useToggleCompletion.ts
@src/pages/TodayPage.tsx
@src/pages/DashboardPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IosInstallModal component and tests</name>
  <files>src/components/pwa/IosInstallModal.tsx, src/components/pwa/IosInstallModal.test.tsx</files>
  <read_first>src/components/habits/ConfirmDialog.tsx, .planning/phases/09-pwa-install-manifest/09-UI-SPEC.md, .planning/phases/09-pwa-install-manifest/09-PATTERNS.md</read_first>
  <acceptance_criteria>
    - Props open boolean and onClose callback
    - When open false renders null; when true renders dialog overlay matching ConfirmDialog shell (z-50, bg-black/60, max-w-sm bg-card)
    - role dialog with aria-labelledby and aria-describedby
    - Title Instalar en la pantalla de inicio; intro Para recibir recordatorios de hábitos, necesitas instalar la app en tu pantalla de inicio (D-13, D-21)
    - Ordered list three steps with Spanish copy from UI-SPEC Copywriting Contract; step 1 includes inline lucide Share icon after Compartir with aria-hidden (D-12)
    - Single full-width Entendido button min-h-11 closes modal (D-21)
    - Backdrop click and Escape key call onClose
    - No dangerouslySetInnerHTML; static strings only (T-09-01)
  </acceptance_criteria>
  <behavior>
    - Render shows all three step texts when open
    - Click Entendido calls onClose
    - Share icon present in step 1
  </behavior>
  <action>
    Create IosInstallModal mirroring ConfirmDialog overlay pattern but informational single-action layout per UI-SPEC Layout — iOS Install Modal. Import Share from lucide-react. Use semantic ol with aria-label Pasos para instalar. Export from src/components/pwa/IosInstallModal.tsx. Write IosInstallModal.test.tsx asserting Spanish copy and close behavior.
  </action>
  <verify>
    <automated>npm test -- src/components/pwa/IosInstallModal.test.tsx -x</automated>
  </verify>
  <done>Shared iOS modal ready for InstallBanner and SettingsPage per D-14 and D-18.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: InstallBanner component and tests</name>
  <files>src/components/pwa/InstallBanner.tsx, src/components/pwa/InstallBanner.test.tsx</files>
  <read_first>src/components/habits/FloatingAddButton.tsx, src/components/layout/BottomTabBar.tsx, .planning/phases/09-pwa-install-manifest/09-UI-SPEC.md, src/platform/install.ts</read_first>
  <acceptance_criteria>
    - Fixed bottom banner z-50 above tab bar with bottom calc(3.5rem + env(safe-area-inset-bottom)) per D-07
    - role region aria-label Instalar aplicación
    - Headline Instala para usar sin conexión; body Accede más rápido desde tu pantalla de inicio (D-08, D-21)
    - Primary Instalar app min-h-11; ghost/outline Ahora no min-h-11 (D-09)
    - Hidden when isStandaloneDisplayMode OR NOT isEngaged OR isDismissed (D-06, D-09, D-16)
    - iOS branch opens IosInstallModal on Instalar app tap (D-11, D-14)
    - Chromium branch gates Instalar app on shouldShowChromiumInstallFlow(); when Chromium eligible but no deferred prompt show inline fallback text Abre el menú del navegador (⋮) y selecciona «Instalar app» or «Añadir a la pantalla de inicio» instead of dead Instalar CTA — D-17 parity with Settings (D-10, D-17)
    - When shouldShowChromiumInstallFlow true, Instalar app calls triggerInstallPrompt on tap; on accepted outcome call dismissInstallBanner with long snooze (D-10)
    - Ahora no calls dismissInstallBanner(7) and hides banner
    - Optional 32px icon preview from /icons/icon-192.png
  </acceptance_criteria>
  <behavior>
    - Banner not in document when not engaged
    - Spanish headline visible when engaged and eligible
    - Click Ahora no invokes dismissInstallBanner
    - iOS flow opens modal state
    - Chromium without deferred prompt shows fallback menu instructions, no Instalar button
    - Chromium with deferred prompt shows Instalar button and hides fallback text
  </behavior>
  <action>
    Create InstallBanner with internal iosModalOpen state for IosInstallModal. Gate visibility using platform/install exports. Import shouldShowChromiumInstallFlow alongside shouldShowIosInstallFlow. Branch Chromium CTA on shouldShowChromiumInstallFlow() per D-17 Settings parity — render fallback instruction text when Chromium but getDeferredPrompt null; only show Instalar app button when shouldShowChromiumInstallFlow returns true. Use useSyncExternalStore with subscribeInstallPrompt for Chromium deferred prompt reactivity. Surface bg-card border-t border-border shadow per UI-SPEC. Write InstallBanner.test.tsx with vi.mock on platform/install controlling engagement, dismiss, standalone, and shouldShowChromiumInstallFlow flags. Assert Spanish strings from Copywriting Contract.
  </action>
  <verify>
    <automated>npm test -- src/components/pwa/InstallBanner.test.tsx -x</automated>
  </verify>
  <done>Contextual non-blocking install banner implemented per D-06 through D-10 and D-14.</done>
</task>

<task type="auto">
  <name>Task 3: Mount banner, session visit, and content padding</name>
  <files>src/components/layout/MainLayout.tsx, src/App.tsx, src/hooks/useToggleCompletion.ts, src/pages/TodayPage.tsx, src/pages/DashboardPage.tsx</files>
  <read_first>src/App.tsx, src/components/layout/MainLayout.tsx, src/hooks/useToggleCompletion.ts, src/pages/TodayPage.tsx, .planning/phases/09-pwa-install-manifest/09-PATTERNS.md</read_first>
  <acceptance_criteria>
    - MainLayout renders InstallBanner sibling after Outlet and before BottomTabBar so banner only appears on tabbed routes
    - App.tsx imports platform/install as side-effect at top level for early BIP listener registration
    - App calls recordSessionVisit once on mount via useEffect
    - useToggleCompletion optionally calls markFirstCheckIn on first successful toggle per RESEARCH discretion
    - TodayPage and/or DashboardPage list padding increased when needed (pb-36 or equivalent) so banner does not obscure habit list above tab bar
    - No changes to vite.config.ts; no SW registration
  </acceptance_criteria>
  <action>
    Mount InstallBanner in MainLayout per PATTERNS recommendation — cleaner than global App mount. Add side-effect import import '@/platform/install' in App.tsx or main.tsx. Add useEffect in App calling recordSessionVisit once. In useToggleCompletion after successful toggle check localStorage hasCheckedIn flag and call markFirstCheckIn if not set — low-cost additive engagement path per RESEARCH. Tune pb-* on tabbed page content if banner height overlaps list items. Do not reposition Toaster in this phase.
  </action>
  <verify>
    <automated>npm test -- src/components/pwa/InstallBanner.test.tsx src/components/pwa/IosInstallModal.test.tsx -x && npm run build && test ! -f dist/sw.js && ! grep -r registerSW dist/</automated>
  </verify>
  <done>Install UX live on Hoy and Panel routes; session engagement tracked; build has no service worker artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InstallBanner / IosInstallModal | User-facing UI; copy is static Spanish strings |
| triggerInstallPrompt | Invoked only on button click user gesture |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | IosInstallModal.tsx | medium | mitigate | Static string literals only; forbid HTML injection APIs in component |
| T-09-02 | Spoofing | InstallBanner CTA | low | mitigate | Use browser native install prompt API; do not mimic OS system dialogs |
| T-09-05 | Denial of Service | Banner z-index | low | accept | z-50 above tab bar; Sonner toasts remain above via portal default |
| T-09-SC | Tampering | npm installs | high | mitigate | No new packages; reuse lucide-react and shadcn Button |

</threat_model>

<verification>
- npm test -- src/components/pwa/ -x passes
- npm test full suite green
- Manual smoke on npm run dev — banner absent first visit; appears after reload (2nd visit) on Hoy route
</verification>

<success_criteria>
- Roadmap success criteria 3 and 4 partially met — contextual banner and iOS modal with reminders copy
- D-06 through D-14 and D-21 implemented in install prompt UX
- PWA-01 install prompt path complete for tabbed routes
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Plan |
|---------------|------|------|
| InstallBanner | React component | 02 |
| IosInstallModal | React component | 02 |
| InstallBanner.test.tsx | test | 02 |
| IosInstallModal.test.tsx | test | 02 |
| MainLayout | modified — mounts InstallBanner | 02 |
| App | modified — side-effect import + recordSessionVisit | 02 |
| useToggleCompletion | modified — markFirstCheckIn hook-up | 02 |

## Multi-Source Coverage Audit

| Source | Item | Plan | Status |
|--------|------|------|--------|
| GOAL | Contextual install prompt not blocking first visit | 02 | COVERED |
| GOAL | iOS home screen guidance with reminders mention | 02 Task 1 | COVERED |
| REQ PWA-01 | Install prompt UX | 02 | COVERED |
| CONTEXT D-06–D-10 | Engagement banner Android | 02 | COVERED |
| CONTEXT D-17 | Chromium fallback when no BIP | 02 Task 2 | COVERED |
| CONTEXT D-11–D-14 | iOS flow banner + modal | 02 | COVERED |
| CONTEXT D-21 | Spanish UI copy | 02 | COVERED |
| UI-SPEC populated install-banner | Layout + copy | 02 Task 2 | COVERED |
| UI-SPEC populated ios-install-modal | Steps + intro | 02 Task 1 | COVERED |
| UI-SPEC long-text install-banner backstop | 320px verify in human check | 02 | COVERED |

<output>
Create `.planning/phases/09-pwa-install-manifest/09-02-SUMMARY.md` when done
</output>
