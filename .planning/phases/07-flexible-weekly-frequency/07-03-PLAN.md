---
phase: 07-flexible-weekly-frequency
plan: 03
type: tdd
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - src/hooks/useTodayHabits.ts
  - src/hooks/useTodayHabits.test.ts
  - src/components/habits/HabitForm.tsx
  - src/components/habits/HabitForm.test.tsx
  - src/components/habits/WeekQuotaChip.tsx
  - src/components/habits/HabitRow.tsx
  - src/components/habits/HabitRow.test.tsx
  - src/integration/walkingSkeleton.test.ts
autonomous: true
requirements:
  - ENH-04
must_haves:
  truths:
    - useTodayHabits filters with isHabitDueOnDate and week completions; hides when weekly quota met (D-05, D-06)
    - HabitForm can create/edit times_per_week via Schedule mode + 1–7 control; default create remains daily (D-03, D-17, D-19)
    - HabitRow shows WeekQuotaChip for times_per_week and WeekDayDots for daily/weekly (D-18)
    - Export/import path already covered by Plan 01 Zod; UI round-trips new shape through form submit
  artifacts:
    - src/hooks/useTodayHabits.ts
    - src/components/habits/HabitForm.tsx
    - src/components/habits/WeekQuotaChip.tsx
    - src/components/habits/HabitRow.tsx
  key_links:
    - useTodayHabits loads Mon–Sun week completions then isHabitDueOnDate
    - HabitRow receives weekCompletions (or equivalent) for chip done count
    - UI-SPEC copy: Schedule / Specific days / Times per week; chip aria "{done} of {times} this week"
  prohibitions:
    - Must not fake WeekDayDots for times_per_week
    - Must not add streak freeze UI
    - Must not install new npm packages / new shadcn components beyond existing ToggleGroup
    - Must not change Panel DashboardCard schedule chrome (no schedule there today)
---

## Phase Goal

**As a** habit tracker user, **I want** to pick “Times per week” when creating a habit and see remaining weekly progress on Hoy, **so that** flexible schedules stay one-tap and glanceable.

<objective>
Wire ENH-04 into application + UI: quota-aware Today hook, HabitForm schedule modes, WeekQuotaChip on HabitRow. Align with 07-UI-SPEC.

Purpose: Satisfy ROADMAP success criteria 1–2 (create/edit + due-today) and surface streak/rate domain from Plan 02 via existing hooks.
Output: Updated hooks/components + green component/hook tests; full suite green at wave end.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-flexible-weekly-frequency/07-CONTEXT.md
@.planning/phases/07-flexible-weekly-frequency/07-UI-SPEC.md
@.planning/phases/07-flexible-weekly-frequency/07-RESEARCH.md
@.planning/phases/07-flexible-weekly-frequency/07-PATTERNS.md
@src/hooks/useTodayHabits.ts
@src/components/habits/HabitForm.tsx
@src/components/habits/HabitRow.tsx
@src/components/habits/WeekDayDots.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Today quota + HabitForm + HabitRow tests</name>
  <files>src/hooks/useTodayHabits.test.ts, src/components/habits/HabitForm.test.tsx, src/components/habits/HabitRow.test.tsx</files>
  <read_first>src/hooks/useTodayHabits.ts, src/hooks/useTodayHabits.test.ts, src/components/habits/HabitForm.tsx, src/components/habits/HabitForm.test.tsx, src/components/habits/HabitRow.tsx, .planning/phases/07-flexible-weekly-frequency/07-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - useTodayHabits.test.ts: times_per_week habit with completionsThisWeek &lt; times appears in ready list; when &gt;= times it is absent
    - useTodayHabits.test.ts: daily/weekly due behavior unchanged
    - HabitForm.test.tsx: selecting Times per week + times 3 submits { type: 'times_per_week', times: 3 }
    - HabitForm.test.tsx: Specific days all-seven still submits daily; mode switch mutual exclusion covered
    - HabitRow.test.tsx created/extended: times_per_week renders text matching /2\/3/ (or done/times); daily still renders WeekDayDots scheduled days chrome
    - RED until Task 2 implements wiring
  </acceptance_criteria>
  <behavior>
    - Today hide when quota met; form emits new Frequency; chip replaces dots for X/week
  </behavior>
  <action>
    Extend/create RED tests for useTodayHabits quota filter, HabitForm times_per_week submit, and HabitRow WeekQuotaChip vs WeekDayDots. Create HabitRow.test.tsx if missing (Wave 0). Do not implement UI/hook production changes yet if that keeps RED clear.
  </action>
  <verify>
    <automated>npm test -- src/hooks/useTodayHabits.test.ts src/components/habits/HabitForm.test.tsx src/components/habits/HabitRow.test.tsx 2>&1 | grep -qE 'FAIL|Error|Unable to find|is not a function'; test $? -eq 0</automated>
  </verify>
  <done>Failing UI/hook tests document ENH-04 surface contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Today hook, HabitForm modes, WeekQuotaChip, HabitRow</name>
  <files>src/hooks/useTodayHabits.ts, src/components/habits/HabitForm.tsx, src/components/habits/WeekQuotaChip.tsx, src/components/habits/HabitRow.tsx, src/integration/walkingSkeleton.test.ts</files>
  <read_first>src/hooks/useTodayHabits.test.ts, src/components/habits/HabitForm.test.tsx, src/components/habits/HabitRow.test.tsx, src/domain/schedule.ts, src/domain/dates.ts, .planning/phases/07-flexible-weekly-frequency/07-UI-SPEC.md, .planning/phases/07-flexible-weekly-frequency/07-PATTERNS.md</read_first>
  <acceptance_criteria>
    - useTodayHabits uses isHabitDueOnDate with week completion Sets; exposes weekCompletions (or equivalent) for chip
    - HabitForm implements Schedule mode per UI-SPEC; default create daily; edit infers times_per_week mode
    - WeekQuotaChip shows `{done}/{times}` with aria `{done} of {times} this week` and habit accent
    - HabitRow branches chip vs WeekDayDots; flame aria uses week streak wording for times_per_week when practical
    - walkingSkeleton.test.ts Today filter uses isHabitDueOnDate if it previously used isDueOnDate alone
    - Targeted tests pass; npm test full suite passes
  </acceptance_criteria>
  <action>
    Implement hook week-completion load + filter, HabitForm mode UI + submit mapping, WeekQuotaChip component, HabitRow branch, and fix walking skeleton due filter. Follow UI-SPEC copy and 44px targets. No new npm packages.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useTodayHabits.test.ts src/components/habits/HabitForm.test.tsx src/components/habits/HabitRow.test.tsx -x && npm test</automated>
  </verify>
  <done>ENH-04 user-facing create/edit + Today quota behavior shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form input → Frequency | User-controlled times 1–7 via ToggleGroup only |
| Dexie read in useTodayHabits | Existing QUERY_ERROR containment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | HabitForm times | low | mitigate | UI only emits 1–7; repo/Zod still validate on import |
| T-07-02 | Information Disclosure | Today errors | low | accept | Keep existing soft error status; no new exception text |
</threat_model>

<verification>
- npx vitest run src/hooks/useTodayHabits.test.ts src/components/habits/HabitForm.test.tsx src/components/habits/HabitRow.test.tsx -x
- npm test
- rg "isHabitDueOnDate" src/hooks/useTodayHabits.ts — present
- rg "times_per_week|WeekQuotaChip|Times per week" src/components/habits — present
</verification>

<success_criteria>
- ROADMAP SC1: create/edit X×/week
- ROADMAP SC2: Today due by remaining weekly quota
- SC3/SC4 domain already in Plans 01–02; UI does not regress export (Zod) or streaks display via existing hooks
</success_criteria>
