---
phase: 07-flexible-weekly-frequency
plan: 02
type: tdd
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/domain/streak.ts
  - src/domain/streak.test.ts
  - src/domain/stats.ts
  - src/domain/stats.test.ts
autonomous: true
requirements:
  - ENH-04
must_haves:
  truths:
    - times_per_week current streak counts consecutive hit weeks; in-progress unmet week does not break (D-09–D-13)
    - countScheduledCompletions for times_per_week uses per-week scheduled+=times and completed+=min(n,times) (D-14)
    - getWeekDayState for times_per_week never returns missed (D-16)
    - daily/weekly streak and rate behavior remains unchanged
  artifacts:
    - src/domain/streak.ts
    - src/domain/stats.ts
  key_links:
    - Week helpers from Plan 07-01 (countCompletionsInCalendarWeek / iterateCalendarWeeksInRange)
    - calculateCompletionRate and calculateOverallCompletionRate compose updated counts
  prohibitions:
    - Must not implement HabitForm / HabitRow / useTodayHabits in this plan
    - Must not add streak freeze / skip semantics
    - Must not day-walk times_per_week rates via isDueOnDate true every day
---

## Phase Goal

**As a** habit tracker user, **I want** streaks and completion rates to respect weekly quotas, **so that** “3× per week” progress is honest and motivating.

<objective>
Branch streak + stats domain for times_per_week (TDD): week-hit streaks, week-capped scheduled counts, heatmap day states without red misses.

Purpose: Lock D-09–D-16 compute-on-read math used by hooks/Panel/History.
Output: Updated streak.ts + stats.ts with green tests; Phase 6 overall pool still works for X/week inputs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-flexible-weekly-frequency/07-CONTEXT.md
@.planning/phases/07-flexible-weekly-frequency/07-RESEARCH.md
@.planning/phases/07-flexible-weekly-frequency/07-PATTERNS.md
@src/domain/streak.ts
@src/domain/stats.ts
@src/domain/schedule.ts
@src/domain/dates.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — week streak + week-cap stats tests</name>
  <files>src/domain/streak.test.ts, src/domain/stats.test.ts</files>
  <read_first>src/domain/streak.ts, src/domain/stats.ts, .planning/phases/07-flexible-weekly-frequency/07-RESEARCH.md, .planning/phases/07-flexible-weekly-frequency/07-CONTEXT.md</read_first>
  <acceptance_criteria>
    - streak.test.ts: times:3 with two consecutive past hit weeks + current unmet → current streak 2 (grace)
    - streak.test.ts: past week miss breaks current streak to 0 (or only weeks after miss)
    - streak.test.ts: current week already hit counts toward current streak
    - streak.test.ts: longest streak = max consecutive hit weeks; new habit 0 before first hit
    - streak.test.ts: existing MWF/daily cases still intended to pass after GREEN
    - stats.test.ts: times:3 over one full past week with 2 completions → {completed:2, scheduled:3}; with 5 completions → {completed:3, scheduled:3} cap
    - stats.test.ts: getWeekDayState times_per_week empty past day → 'not-scheduled' not 'missed'; completed day → 'completed'
    - stats.test.ts: calculateOverallCompletionRate with one X/week habit uses capped week counts
    - Suite shows RED for new X/week expectations until Task 2
  </acceptance_criteria>
  <behavior>
    - Week unit streaks; rate week caps; heatmap never missed for X/week
  </behavior>
  <action>
    Add RED fixtures/tests for times_per_week streak and stats/heatmap behaviors per D-09–D-16 and RESEARCH. Keep daily/MWF tests; do not change production until Task 2 if RED clarity needs missing branch behavior.
  </action>
  <verify>
    <automated>npm test -- src/domain/streak.test.ts src/domain/stats.test.ts 2>&1 | grep -qE 'FAIL|Error|expected'; test $? -eq 0</automated>
  </verify>
  <done>Failing tests document week-quota streak/rate contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — branch streak + stats for times_per_week</name>
  <files>src/domain/streak.ts, src/domain/stats.ts</files>
  <read_first>src/domain/streak.test.ts, src/domain/stats.test.ts, src/domain/dates.ts, src/domain/schedule.ts, .planning/phases/07-flexible-weekly-frequency/07-RESEARCH.md</read_first>
  <acceptance_criteria>
    - calculateCurrentStreak / calculateLongestStreak implement week-hit branch for times_per_week
    - countScheduledCompletions week-cap branch; daily/weekly day loop unchanged
    - getWeekDayState never returns missed for times_per_week
    - npx vitest run src/domain/streak.test.ts src/domain/stats.test.ts passes
    - Optional isWeekHit helper exported only if used by tests/clarity
  </acceptance_criteria>
  <action>
    Implement times_per_week branches in streak.ts and stats.ts per RESEARCH algorithms. Reuse countCompletionsInCalendarWeek / iterateCalendarWeeksInRange from Plan 07-01. Do not alter hook/UI layers.
  </action>
  <verify>
    <automated>npx vitest run src/domain/streak.test.ts src/domain/stats.test.ts -x</automated>
  </verify>
  <done>Domain streak/rate ready for Today + form wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain pure functions | Completion Sets from trusted app queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Denial of Service | week iteration | low | mitigate | Iterate only weeks from habit start → today; no unbounded loops |
</threat_model>

<verification>
- npx vitest run src/domain/streak.test.ts src/domain/stats.test.ts -x
- rg "times_per_week" src/domain/streak.ts src/domain/stats.ts — present
</verification>

<success_criteria>
- ENH-04 streak + rate + heatmap state rules complete for X/week
- Phase 6 pooled overall remains correct via shared counts
- No UI/hook changes in this wave
</success_criteria>
