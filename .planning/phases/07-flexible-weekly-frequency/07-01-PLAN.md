---
phase: 07-flexible-weekly-frequency
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/types.ts
  - src/domain/dates.ts
  - src/domain/dates.test.ts
  - src/domain/schedule.ts
  - src/domain/schedule.test.ts
  - src/domain/backupSchema.ts
  - src/domain/backupSchema.test.ts
autonomous: true
requirements:
  - ENH-04
must_haves:
  truths:
    - Frequency includes { type: 'times_per_week', times: 1..7 } alongside daily and weekly (D-01, D-02)
    - countCompletionsInCalendarWeek counts Mon–Sun week membership (D-04)
    - isDueOnDate returns false for times_per_week; isHabitDueOnDate implements quota due (D-05, D-06, D-08)
    - Backup version stays 1; Zod accepts times_per_week and rejects times outside 1..7 (D-20, D-21)
  artifacts:
    - src/domain/types.ts
    - src/domain/dates.ts
    - src/domain/schedule.ts
    - src/domain/backupSchema.ts
  key_links:
    - isHabitDueOnDate uses countCompletionsInCalendarWeek for times_per_week
    - getCalendarWeekDates / weekStartsOn 1 remains week boundary source
  prohibitions:
    - Must not change daily/weekly due semantics
    - Must not bump backup version to 2
    - Must not implement streak/stats/UI in this plan
    - Must not treat times:7 as daily
---

## Phase Goal

**As a** habit tracker user, **I want** habits that are “X times per week” stored and evaluated for due-today with a weekly quota, **so that** flexible schedules work without inventing fake weekdays.

<objective>
Ship ENH-04 foundation (TDD): extend `Frequency`, week completion counting, quota-aware due helper, and additive backup Zod — before streak/rate branches and UI.

Purpose: Lock D-01–D-08 / D-20–D-21 data + due contracts.
Output: Updated domain types/dates/schedule/backup + green targeted tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-flexible-weekly-frequency/07-CONTEXT.md
@.planning/phases/07-flexible-weekly-frequency/07-RESEARCH.md
@.planning/phases/07-flexible-weekly-frequency/07-PATTERNS.md
@.planning/phases/07-flexible-weekly-frequency/07-VALIDATION.md
@src/domain/types.ts
@src/domain/dates.ts
@src/domain/schedule.ts
@src/domain/backupSchema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — types, week count, due helper, backup tests</name>
  <files>src/domain/dates.test.ts, src/domain/schedule.test.ts, src/domain/backupSchema.test.ts</files>
  <read_first>src/domain/types.ts, src/domain/dates.ts, src/domain/schedule.ts, src/domain/backupSchema.ts, .planning/phases/07-flexible-weekly-frequency/07-RESEARCH.md, .planning/phases/07-flexible-weekly-frequency/07-CONTEXT.md</read_first>
  <acceptance_criteria>
    - dates.test.ts covers countCompletionsInCalendarWeek: Mon–Sun membership; completion outside week ignored; boundary Sun/Mon fixtures
    - schedule.test.ts: isDueOnDate({type:'times_per_week', times:3}, anyDate) === false
    - schedule.test.ts: isHabitDueOnDate times:3 with 2 week completions → true; with 3 → false; with 4 → false (over-complete still not due)
    - schedule.test.ts: isHabitDueOnDate for daily/weekly matches isDueOnDate (completedDates ignored)
    - schedule.test.ts: isDaily(times_per_week times:7) === false
    - backupSchema.test.ts: accepts times_per_week 1..7; rejects times 0 and 8; version remains 1; old daily/weekly payloads still ok
    - Suite fails RED until Task 2 implements missing exports/branches
  </acceptance_criteria>
  <behavior>
    - Week boundary Mon–Sun via existing WEEK_OPTS
    - Quota due: completionsThisWeek &lt; times
  </behavior>
  <action>
    Extend dates.test.ts, schedule.test.ts, and backupSchema.test.ts with RED cases for countCompletionsInCalendarWeek, isHabitDueOnDate, isDueOnDate X/week false, isDaily X/week false, and Zod times_per_week accept/reject. Prefer missing exports for classic RED. Do not implement production helpers yet if that keeps RED clear.
  </action>
  <verify>
    <automated>npm test -- src/domain/dates.test.ts src/domain/schedule.test.ts src/domain/backupSchema.test.ts 2>&1 | grep -qE 'FAIL|Error|Cannot find|is not a function|does not provide'; test $? -eq 0</automated>
  </verify>
  <done>Failing tests document ENH-04 foundation contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Frequency, dates helpers, schedule, Zod</name>
  <files>src/domain/types.ts, src/domain/dates.ts, src/domain/schedule.ts, src/domain/backupSchema.ts</files>
  <read_first>src/domain/dates.test.ts, src/domain/schedule.test.ts, src/domain/backupSchema.test.ts, .planning/phases/07-flexible-weekly-frequency/07-RESEARCH.md, .planning/phases/07-flexible-weekly-frequency/07-PATTERNS.md</read_first>
  <acceptance_criteria>
    - types.ts Frequency includes times_per_week variant
    - dates.ts exports countCompletionsInCalendarWeek; optionally iterateCalendarWeeksInRange if useful for later plans
    - schedule.ts exports isHabitDueOnDate; isDueOnDate returns false for times_per_week; daily/weekly unchanged
    - backupSchema frequencySchema includes times int min1 max7; version literal 1
    - npx vitest run src/domain/dates.test.ts src/domain/schedule.test.ts src/domain/backupSchema.test.ts passes
    - No React/Dexie imports in domain files touched
  </acceptance_criteria>
  <action>
    Implement Frequency extension, countCompletionsInCalendarWeek (and iterateCalendarWeeksInRange if included), isHabitDueOnDate + isDueOnDate/isDaily X/week rules, and Zod union member per RESEARCH API. Keep existing daily/weekly tests green.
  </action>
  <verify>
    <automated>npx vitest run src/domain/dates.test.ts src/domain/schedule.test.ts src/domain/backupSchema.test.ts -x</automated>
  </verify>
  <done>Domain foundation ready for streak/stats branches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backup JSON → Zod | Untrusted import payload |
| Domain pure functions | In-memory Sets from app code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | backupSchema frequency | medium | mitigate | Zod rejects times outside 1..7 and unknown shapes; version stays 1 |
| T-07-00 | Tampering | schedule/dates math | low | accept | Local compute only |
</threat_model>

<verification>
- npx vitest run src/domain/dates.test.ts src/domain/schedule.test.ts src/domain/backupSchema.test.ts -x
- rg "times_per_week" src/domain/types.ts src/domain/backupSchema.ts — present
- rg "isHabitDueOnDate|countCompletionsInCalendarWeek" src/domain — present
</verification>

<success_criteria>
- ENH-04 frequency shape + due quota helpers + backup round-trip foundation complete
- No streak/stats/UI changes in this wave
</success_criteria>
