---
phase: 08-streak-freeze
plan: 03
type: tdd
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/hooks/useTodayHabits.ts
  - src/hooks/useTodayHabits.test.ts
  - src/hooks/useStreak.ts
  - src/hooks/useStreak.test.ts
  - src/hooks/useHabitStats.ts
  - src/hooks/useHabitStats.test.ts
  - src/hooks/useHeatmapData.ts
  - src/hooks/useHeatmapData.test.ts
  - src/hooks/useToggleFreeze.ts
  - src/hooks/useDashboardHabits.ts
  - src/components/habits/HabitRow.tsx
  - src/components/habits/HabitRow.test.tsx
  - src/components/heatmap/ContributionHeatmap.tsx
  - src/components/heatmap/ContributionHeatmap.test.tsx
  - src/pages/TodayPage.tsx
autonomous: true
requirements:
  - ENH-05
must_haves:
  truths:
    - User can Omitir today from HabitRow; frozen today habits hide from Hoy (D-06, D-08)
    - History heatmap cycles empty → completed → frozen → empty and shows distinct frozen visuals + Omitido (D-07, D-18–D-20)
    - Hooks pass frozenDates into streak/stats/heatmap so SC2–SC4 hold after restore/read
    - Panel DashboardCard remains navigation-only (D-09)
  artifacts:
    - src/hooks/useTodayHabits.ts
    - src/hooks/useHeatmapData.ts
    - src/components/habits/HabitRow.tsx
    - src/components/heatmap/ContributionHeatmap.tsx
  key_links:
    - TodayPage wires onSkip → freezeRepository/useToggleFreeze for today
    - useHeatmapData.cycle uses mutual-exclusion repos
    - UI-SPEC: aria Omitir, tooltip Omitido, ice #58a6ff dashed
  prohibitions:
    - Must not replace one-tap complete with Skip
    - Must not add freeze editing on DashboardCard
    - Must not install new npm packages / new shadcn components
    - Must not add monthly freeze cap UI
---

## Phase Goal

**As a** habit tracker user, **I want** an obvious Skip on today and a clear freeze mark on history, **so that** I can protect my streak without confusing it with a check-in.

<objective>
Wire ENH-05 into application + UI: freeze-aware hooks, HabitRow Omitir, ContributionHeatmap three-state cycle + frozen style. Align with 08-UI-SPEC.

Purpose: Satisfy ROADMAP SC1–SC3 (mark freeze, streak/rate honesty visible, heatmap distinction); SC4 domain/backup already in Plans 01–02.
Output: Updated hooks/components/pages + green tests; full suite green at wave end.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-streak-freeze/08-CONTEXT.md
@.planning/phases/08-streak-freeze/08-UI-SPEC.md
@.planning/phases/08-streak-freeze/08-RESEARCH.md
@.planning/phases/08-streak-freeze/08-PATTERNS.md
@src/hooks/useTodayHabits.ts
@src/hooks/useHeatmapData.ts
@src/components/habits/HabitRow.tsx
@src/components/heatmap/ContributionHeatmap.tsx
@src/pages/TodayPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Today hide/Omitir + heatmap cycle tests</name>
  <files>src/hooks/useTodayHabits.test.ts, src/hooks/useHeatmapData.test.ts, src/hooks/useStreak.test.ts, src/hooks/useHabitStats.test.ts, src/components/habits/HabitRow.test.tsx, src/components/heatmap/ContributionHeatmap.test.tsx</files>
  <read_first>src/hooks/useTodayHabits.ts, src/hooks/useHeatmapData.ts, src/components/habits/HabitRow.tsx, src/components/heatmap/ContributionHeatmap.tsx, .planning/phases/08-streak-freeze/08-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - useTodayHabits.test.ts: habit frozen today is absent from ready list even if due
    - HabitRow.test.tsx: renders button/name matching /Omitir/; calling it invokes onSkip; row tap still calls onToggle
    - ContributionHeatmap.test.tsx (or useHeatmapData.test.ts): cycling advances empty→completed→frozen→empty; frozen cell exposes Omitido tooltip or frozen state styling hook
    - useStreak/useHabitStats tests updated for freeze load if they assert domain inputs/outputs
    - RED until Task 2 implements wiring
  </acceptance_criteria>
  <behavior>
    - Hide frozen today; secondary Omitir; heatmap three-state cycle
  </behavior>
  <action>
    Extend/create RED tests for Today freeze hide, HabitRow Omitir control, heatmap/useHeatmapData cycle + frozen distinction, and hook freeze wiring. Do not implement production UI/hook changes yet if that keeps RED clear.
  </action>
  <verify>
    <automated>npm test -- src/hooks/useTodayHabits.test.ts src/hooks/useHeatmapData.test.ts src/components/habits/HabitRow.test.tsx src/components/heatmap/ContributionHeatmap.test.tsx 2>&1 | grep -qE 'FAIL|Error|Unable to find|is not a function'; test $? -eq 0</automated>
  </verify>
  <done>Failing UI/hook tests document ENH-05 surface contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — hooks, Omitir, heatmap cycle + frozen style</name>
  <files>src/hooks/useTodayHabits.ts, src/hooks/useStreak.ts, src/hooks/useHabitStats.ts, src/hooks/useHeatmapData.ts, src/hooks/useToggleFreeze.ts, src/hooks/useDashboardHabits.ts, src/components/habits/HabitRow.tsx, src/components/heatmap/ContributionHeatmap.tsx, src/pages/TodayPage.tsx</files>
  <read_first>src/hooks/useTodayHabits.test.ts, src/components/habits/HabitRow.test.tsx, src/components/heatmap/ContributionHeatmap.test.tsx, src/infrastructure/freezeRepository.ts, .planning/phases/08-streak-freeze/08-UI-SPEC.md, .planning/phases/08-streak-freeze/08-PATTERNS.md</read_first>
  <acceptance_criteria>
    - useTodayHabits loads freezes and hides frozen-today habits
    - useStreak / useHabitStats / useDashboardHabits (if applicable) pass frozenDates into domain
    - useHeatmapData loads freezes and exposes cycle per UI-SPEC; X/week past/today not-scheduled cells interactive
    - HabitRow Snowflake Omitir button ≥44px before History; TodayPage wires onSkip
    - ContributionHeatmap frozen visual uses ice #58a6ff dashed; tooltips show Omitido
    - DashboardCard not given freeze controls
    - Targeted tests pass; npm test full suite passes
  </acceptance_criteria>
  <action>
    Implement freeze-aware hooks, optional useToggleFreeze wrapper, HabitRow Omitir, TodayPage wiring, heatmap cycle + frozen styles per UI-SPEC/PATTERNS. Keep one-tap complete primary. No new npm packages.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useTodayHabits.test.ts src/hooks/useHeatmapData.test.ts src/hooks/useStreak.test.ts src/hooks/useHabitStats.test.ts src/components/habits/HabitRow.test.tsx src/components/heatmap/ContributionHeatmap.test.tsx -x && npm test</automated>
  </verify>
  <done>ENH-05 user-facing freeze mark + History distinction shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI click → repository | User-initiated explicit freeze only |
| Dexie read in hooks | Existing QUERY_ERROR containment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Tampering / integrity | UI visibility | medium | mitigate | Frozen always shown as Omitido on heatmap; no silent freeze |
| T-08-01 | Tampering | N/A in UI | low | accept | Writes go through validated repos from Plan 01 |
</threat_model>

<verification>
- npx vitest run src/hooks/useTodayHabits.test.ts src/hooks/useHeatmapData.test.ts src/components/habits/HabitRow.test.tsx src/components/heatmap/ContributionHeatmap.test.tsx -x
- npm test
- rg "Omitir|Snowflake|onSkip" src/components/habits/HabitRow.tsx — present
- rg "frozen|Omitido|cycle" src/components/heatmap/ContributionHeatmap.tsx src/hooks/useHeatmapData.ts — present
</verification>

<success_criteria>
- ROADMAP SC1: mark skip/freeze today or past from habit UI
- ROADMAP SC2: frozen does not break streak; frozen ≠ done for rate (domain + wired hooks)
- ROADMAP SC3: heatmap/history distinguishes freeze
- ROADMAP SC4: export/import freezes already in Plan 01; UI does not regress
</success_criteria>
