---
phase: 08-streak-freeze
plan: 02
type: tdd
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/domain/streak.ts
  - src/domain/streak.test.ts
  - src/domain/stats.ts
  - src/domain/stats.test.ts
  - src/domain/heatmap.ts
  - src/domain/heatmap.test.ts
autonomous: true
requirements:
  - ENH-05
must_haves:
  truths:
    - Frozen due days bridge streaks without incrementing; freeze never starts a streak (D-10–D-13)
    - times_per_week uses effectiveTimes = max(0, times - freezeCountInWeek) for hits and rates (D-14, D-16)
    - Frozen due days excluded from daily/weekly rate numerator and denominator (D-15)
    - WeekDayState includes frozen; heatmap tooltip uses Omitido (D-18–D-21)
  artifacts:
    - src/domain/streak.ts
    - src/domain/stats.ts
    - src/domain/heatmap.ts
  key_links:
    - Domain calculators accept frozenDates Set alongside completedDates
    - calculateCompletionRate / calculateOverallCompletionRate compose updated counts
  prohibitions:
    - Must not implement HabitRow / heatmap UI / hooks in this plan
    - Must not count freeze as a completion
    - Must not add silent auto-freeze rules
---

## Phase Goal

**As a** habit tracker user, **I want** skips to preserve my earned streak without faking completions, **so that** motivation stays honest.

<objective>
Branch streak + stats + heatmap domain for freezes (TDD): bridge-without-increment, effectiveTimes for X/week, rate exclusion, `'frozen'` day state + Omitido tooltip.

Purpose: Lock D-10–D-21 compute-on-read math used by hooks/Panel/History.
Output: Updated streak.ts / stats.ts / heatmap.ts with green tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-streak-freeze/08-CONTEXT.md
@.planning/phases/08-streak-freeze/08-RESEARCH.md
@.planning/phases/08-streak-freeze/08-PATTERNS.md
@src/domain/streak.ts
@src/domain/stats.ts
@src/domain/heatmap.ts
@src/domain/dates.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — freeze streak + rate + heatmap tests</name>
  <files>src/domain/streak.test.ts, src/domain/stats.test.ts, src/domain/heatmap.test.ts</files>
  <read_first>src/domain/streak.ts, src/domain/stats.ts, src/domain/heatmap.ts, .planning/phases/08-streak-freeze/08-RESEARCH.md, .planning/phases/08-streak-freeze/08-CONTEXT.md</read_first>
  <acceptance_criteria>
    - streak.test.ts: complete Mon + freeze Tue + complete Wed (daily) → current streak 2 (bridge, no increment on freeze)
    - streak.test.ts: freeze-only on new habit → streak 0
    - streak.test.ts: today frozen after yesterday complete → streak includes yesterday (D-13 bridge)
    - streak.test.ts: due miss (neither complete nor frozen) still breaks
    - streak.test.ts: times:3 with 1 freeze + 2 completions → week hit; past week unmet effectiveTimes breaks
    - streak.test.ts: existing daily/MWF/X/week cases updated for frozenDates param and still intended to pass after GREEN
    - stats.test.ts: 2 due days, 1 complete, 1 frozen → completed 1 scheduled 1 (100%)
    - stats.test.ts: times:3, 1 freeze, 2 completions → scheduled 2 completed 2
    - stats.test.ts: getWeekDayState frozen date → 'frozen'; X/week empty still not 'missed'
    - heatmap.test.ts: frozen state tooltip includes Omitido; buildHeatmapActivities marks frozen cells
    - Suite shows RED for new freeze expectations until Task 2
  </acceptance_criteria>
  <behavior>
    - Bridge streaks; frozen≠done rates; frozen heatmap state
  </behavior>
  <action>
    Add RED fixtures/tests for freeze bridge/effectiveTimes/rate exclusion/WeekDayState frozen/Omitido per D-10–D-21 and RESEARCH. Update call signatures in tests to pass frozenDates. Do not change production until Task 2 if RED clarity needs missing param behavior.
  </action>
  <verify>
    <automated>npm test -- src/domain/streak.test.ts src/domain/stats.test.ts src/domain/heatmap.test.ts 2>&1 | grep -qE 'FAIL|Error|expected|Argument'; test $? -eq 0</automated>
  </verify>
  <done>Failing tests document freeze streak/rate/heatmap contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — freeze-aware streak, stats, heatmap</name>
  <files>src/domain/streak.ts, src/domain/stats.ts, src/domain/heatmap.ts</files>
  <read_first>src/domain/streak.test.ts, src/domain/stats.test.ts, src/domain/heatmap.test.ts, src/domain/dates.ts, .planning/phases/08-streak-freeze/08-RESEARCH.md</read_first>
  <acceptance_criteria>
    - calculateCurrentStreak / calculateLongestStreak accept frozenDates and implement bridge + X/week effectiveTimes
    - countScheduledCompletions excludes frozen due days / uses effectiveTimes for X/week
    - WeekDayState includes 'frozen'; getWeekDayState + buildHeatmapActivities + formatHeatmapTooltip honor it (Omitido)
    - npx vitest run src/domain/streak.test.ts src/domain/stats.test.ts src/domain/heatmap.test.ts passes
  </acceptance_criteria>
  <action>
    Implement freeze-aware algorithms in streak.ts, stats.ts, heatmap.ts per RESEARCH. Reuse countCompletionsInCalendarWeek / week helpers; count freezes in week similarly. Do not alter hook/UI layers (call-site compile fixes belong in Plan 08-03 if signatures break hooks — prefer updating hook signatures minimally only if required to keep `npm test` typecheck; otherwise leave hooks to Plan 03 with temporary test-only domain API completeness).
  </action>
  <verify>
    <automated>npx vitest run src/domain/streak.test.ts src/domain/stats.test.ts src/domain/heatmap.test.ts -x</automated>
  </verify>
  <done>Domain freeze math ready for hook/UI wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain pure functions | Completion/freeze Sets from trusted app queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Tampering / integrity | streak-rate honesty | medium | mitigate | Frozen never increments streak or rate numerator; always visible as distinct state |
| T-08-04 | Denial of Service | week iteration | low | mitigate | Iterate only habit start → today; bounded loops |
</threat_model>

<verification>
- npx vitest run src/domain/streak.test.ts src/domain/stats.test.ts src/domain/heatmap.test.ts -x
- rg "frozenDates|'frozen'|Omitido|effectiveTimes" src/domain/streak.ts src/domain/stats.ts src/domain/heatmap.ts — present
</verification>

<success_criteria>
- ENH-05 streak + rate + heatmap state rules complete
- Phase 6 pooled overall remains correct via shared counts
- No UI/hook user-facing changes in this wave (except compile fixes if unavoidable)
</success_criteria>
