---
phase: 08-streak-freeze
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/types.ts
  - src/domain/backupSchema.ts
  - src/domain/backupSchema.test.ts
  - src/infrastructure/db.ts
  - src/infrastructure/db.test.ts
  - src/infrastructure/freezeRepository.ts
  - src/infrastructure/freezeRepository.test.ts
  - src/infrastructure/completionRepository.ts
  - src/infrastructure/completionRepository.test.ts
  - src/infrastructure/backupService.ts
  - src/infrastructure/backupService.test.ts
autonomous: true
requirements:
  - ENH-05
must_haves:
  truths:
    - Freeze entity exists parallel to Completion; BackupPayload may include freezes (D-01, D-02, D-23)
    - Dexie schema version 2 adds freezes store keyed [habitId+date] without wiping habits/completions (D-22)
    - Writing freeze clears completion and writing completion clears freeze for same day (D-03)
    - Future freezes rejected; export/import round-trips freezes; old backups without freezes still import (D-04, D-23, D-24)
  artifacts:
    - src/domain/types.ts
    - src/infrastructure/db.ts
    - src/infrastructure/freezeRepository.ts
    - src/domain/backupSchema.ts
    - src/infrastructure/backupService.ts
  key_links:
    - freezeRepository and completionRepository share transactional mutual exclusion
    - backupService import clears/bulkAdds freezes in same transaction as habits/completions
  prohibitions:
    - Must not overload Completion with a status field
    - Must not bump backup version to 2
    - Must not implement streak/stats/heatmap/UI in this plan
    - Must not add npm packages
---

## Phase Goal

**As a** habit tracker user, **I want** skip/freeze days stored separately from completions and preserved in backups, **so that** frozen ≠ done and restores stay honest.

<objective>
Ship ENH-05 data foundation (TDD): `Freeze` type, Dexie v2 `freezes` store, freezeRepository with mutual exclusion, additive backup Zod + export/import.

Purpose: Lock D-01–D-04 / D-22–D-24 persistence contracts before domain math and UI.
Output: Updated types/db/repos/backup + green targeted tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-streak-freeze/08-CONTEXT.md
@.planning/phases/08-streak-freeze/08-RESEARCH.md
@.planning/phases/08-streak-freeze/08-PATTERNS.md
@.planning/phases/08-streak-freeze/08-VALIDATION.md
@src/domain/types.ts
@src/infrastructure/db.ts
@src/infrastructure/completionRepository.ts
@src/domain/backupSchema.ts
@src/infrastructure/backupService.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Freeze types, Dexie v2, repo exclusion, backup tests</name>
  <files>src/domain/backupSchema.test.ts, src/infrastructure/db.test.ts, src/infrastructure/freezeRepository.test.ts, src/infrastructure/completionRepository.test.ts, src/infrastructure/backupService.test.ts</files>
  <read_first>src/domain/types.ts, src/domain/backupSchema.ts, src/infrastructure/db.ts, src/infrastructure/completionRepository.ts, src/infrastructure/backupService.ts, .planning/phases/08-streak-freeze/08-RESEARCH.md, .planning/phases/08-streak-freeze/08-CONTEXT.md</read_first>
  <acceptance_criteria>
    - backupSchema.test.ts: accepts payload with freezes[]; defaults missing freezes to []; version remains 1; rejects malformed freeze dates
    - db.test.ts: HabitTrackerDB exposes freezes table at schema version 2 (or opens with freezes store)
    - freezeRepository.test.ts created: set freeze clears existing completion; toggle off clears freeze; future date rejected
    - completionRepository.test.ts: toggling completion on clears existing freeze for same day
    - backupService.test.ts: export includes freezes; import round-trips freezes; old payload without freezes still imports
    - Suite fails RED until Task 2 implements missing pieces
  </acceptance_criteria>
  <behavior>
    - Separate Freeze entity; transactional mutual exclusion; additive backup v1
  </behavior>
  <action>
    Add/extend RED tests for Freeze backup Zod, Dexie v2 freezes store, freezeRepository mutual exclusion + future reject, completionRepository clearing freezes, and backupService round-trip. Create freezeRepository.test.ts (Wave 0). Prefer missing exports for classic RED; do not implement production yet if that keeps RED clear.
  </action>
  <verify>
    <automated>npm test -- src/domain/backupSchema.test.ts src/infrastructure/db.test.ts src/infrastructure/freezeRepository.test.ts src/infrastructure/completionRepository.test.ts src/infrastructure/backupService.test.ts 2>&1 | grep -qE 'FAIL|Error|Cannot find|is not a function|Cannot find module'; test $? -eq 0</automated>
  </verify>
  <done>Failing tests document ENH-05 persistence contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — types, Dexie v2, freezeRepository, backup</name>
  <files>src/domain/types.ts, src/domain/backupSchema.ts, src/infrastructure/db.ts, src/infrastructure/freezeRepository.ts, src/infrastructure/completionRepository.ts, src/infrastructure/backupService.ts</files>
  <read_first>src/domain/backupSchema.test.ts, src/infrastructure/freezeRepository.test.ts, src/infrastructure/completionRepository.test.ts, src/infrastructure/backupService.test.ts, .planning/phases/08-streak-freeze/08-RESEARCH.md, .planning/phases/08-streak-freeze/08-PATTERNS.md</read_first>
  <acceptance_criteria>
    - types.ts exports Freeze; BackupPayload includes optional freezes
    - db.ts version(2) adds freezes store mirroring completions indexes
    - freezeRepository implements set/clear/toggle/getByHabitInRange with txn mutual exclusion and future rejection
    - completionRepository toggle-on clears freeze in same txn
    - backupSchema optional freezes default []; backupService export/import includes freezes transactionally
    - npx vitest run on Task 1 files passes
  </acceptance_criteria>
  <action>
    Implement Freeze type, Dexie v2, freezeRepository, completionRepository freeze clear, Zod freezes, and backupService export/import per RESEARCH/PATTERNS. Do not change streak/stats/UI layers.
  </action>
  <verify>
    <automated>npx vitest run src/domain/backupSchema.test.ts src/infrastructure/db.test.ts src/infrastructure/freezeRepository.test.ts src/infrastructure/completionRepository.test.ts src/infrastructure/backupService.test.ts -x</automated>
  </verify>
  <done>Persistence ready for freeze-aware domain math.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backup JSON → Zod → Dexie | Untrusted file input validated before transactional write |
| Repository writes | App-trusted habitId/date; still reject futures |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | backup freezes | medium | mitigate | Zod shape + date regex; transactional replace; mutual exclusion on write |
| T-08-02 | Tampering | orphan freezes | low | accept | Import replaces all; unused orphans harmless on read |
</threat_model>

<verification>
- npx vitest run src/domain/backupSchema.test.ts src/infrastructure/db.test.ts src/infrastructure/freezeRepository.test.ts src/infrastructure/completionRepository.test.ts src/infrastructure/backupService.test.ts -x
- rg "freezes" src/infrastructure/db.ts src/domain/backupSchema.ts — present
- rg "version\\(2\\)" src/infrastructure/db.ts — present
</verification>

<success_criteria>
- ENH-05 persistence + backup round-trip foundation complete
- Mutual exclusion enforced at repository layer
- No domain streak/UI changes in this wave
</success_criteria>
