---
phase: 03-dashboard-progress-visualization
plan: 04
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/hooks/useHeatmapData.ts
  - src/components/heatmap/ContributionHeatmap.tsx
  - src/components/heatmap/ContributionHeatmap.test.tsx
  - src/pages/HabitHistoryPage.tsx
  - src/pages/HabitHistoryPage.test.tsx
  - src/components/habits/HistoryDotGrid.tsx
  - src/components/habits/HistoryDotGrid.test.tsx
autonomous: true
requirements:
  - VIZ-01
must_haves:
  truths:
    - History page shows 52-week GitHub-style contribution grid below stat cards (VIZ-01, D-09, D-10, D-13)
    - Section subtitle Historial replaces Esta semana above heatmap (D-11)
    - Non-scheduled days very dim opacity-20 and not tappable; missed scheduled days tappable with destructive ring (D-14, D-16)
    - Tap toggles completion on today and past scheduled days; future dates disabled (D-16)
    - Tooltips show Spanish date plus status per D-15 via formatHeatmapTooltip
    - Heatmap wrapped in overflow-x-auto for mobile horizontal scroll (VIZ-01 responsive)
    - Back button uses navigate(-1) — not hardcoded Link to / (D-12)
    - HistoryDotGrid component and tests deleted — no coexistence with heatmap (D-09)
    - ContributionHeatmap returns null while loading — matches Phase 2 dot grid pattern (UI-SPEC loading)
    - Toggle failure shows sonner toast Couldn't update. Try again.
    - statement: Invalid completion dates in IndexedDB skipped silently without error UI
      verification: backstop
  artifacts:
    - src/components/heatmap/ContributionHeatmap.tsx
    - src/hooks/useHeatmapData.ts
    - src/pages/HabitHistoryPage.tsx
  key_links:
    - useHeatmapData queries 52-week range via getHeatmapDateRange and buildHeatmapActivities
    - ContributionHeatmap renderBlock cloneElement attaches onClick only for completed and missed states
    - Cell tap calls useToggleCompletion.toggle(habitId, date) — same path as Phase 2 dots
    - HabitHistoryPage composes StatCards then Historial subtitle then ContributionHeatmap
  prohibitions:
    - Must not use eventHandlers prop on ActivityCalendar — use renderBlock per v3 API
    - Must not keep HistoryDotGrid alongside heatmap (D-09)
    - Must not use Link to="/" for back navigation (D-12)
---

## Phase Goal

**As a** habit tracker user, **I want to** see all streaks at a glance and explore each habit's full history on a GitHub-style contribution grid, **so that** I stay motivated by visual progress I cannot ignore.

<objective>
Ship VIZ-01 vertical slice — user views per-habit 52-week heatmap on history page with tap-to-toggle, replacing calendar-week dot grid.

Purpose: GitHub-style contribution visualization with schedule-aware cells completes Phase 3 visualization requirement (D-09 through D-16).
Output: useHeatmapData, ContributionHeatmap, updated HabitHistoryPage, HistoryDotGrid removed.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-progress-visualization/03-CONTEXT.md
@.planning/phases/03-dashboard-progress-visualization/03-UI-SPEC.md
@.planning/phases/03-dashboard-progress-visualization/03-PATTERNS.md
@.planning/phases/03-dashboard-progress-visualization/03-01-SUMMARY.md
@.planning/phases/03-dashboard-progress-visualization/03-02-SUMMARY.md
@src/domain/heatmap.ts
@src/components/habits/HistoryDotGrid.tsx
@src/pages/HabitHistoryPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: useHeatmapData hook</name>
  <files>src/hooks/useHeatmapData.ts</files>
  <read_first>src/hooks/useStreak.ts, src/domain/heatmap.ts, .planning/phases/03-dashboard-progress-visualization/03-PATTERNS.md</read_first>
  <acceptance_criteria>
    - useHeatmapData(habitId, frequency, todayKey?) returns activities, cellStates, isLoading, toggle
    - useLiveQuery fetches completionRepository.getByHabitInRange for 52-week window from getHeatmapDateRange
    - toggle delegates to useToggleCompletion for habitId and date
    - activities defaults to empty array; cellStates to empty Map while loading
    - isLoading true when result undefined
    - npm run build passes
  </acceptance_criteria>
  <action>
    Create src/hooks/useHeatmapData.ts following 03-RESEARCH.md Pattern 4 and 03-PATTERNS.md hook shape.

    Import buildHeatmapActivities and getHeatmapDateRange from domain/heatmap. Wire useToggleCompletion inside hook.

    Dependency array includes habitId, frequency, start, end, today from getHeatmapDateRange.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Reactive 52-week heatmap data and toggle function ready for ContributionHeatmap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContributionHeatmap component and tests</name>
  <files>src/components/heatmap/ContributionHeatmap.tsx, src/components/heatmap/ContributionHeatmap.test.tsx</files>
  <read_first>src/components/habits/HistoryDotGrid.tsx, .planning/phases/03-dashboard-progress-visualization/03-UI-SPEC.md, .planning/phases/03-dashboard-progress-visualization/03-RESEARCH.md</read_first>
  <acceptance_criteria>
    - ActivityCalendar with colorScheme dark, weekStart 1, blockSize 12, blockMargin 3 per UI-SPEC
    - theme dark array uses #21262d empty and #3fb950 completed (level 4 mapping)
    - minLevel 0 maxLevel 4; showColorLegend false; showTotalCount false
    - renderBlock via cloneElement — opacity 0.15 for not-scheduled, pointer cursor only for completed and missed
    - Missed scheduled cells get ring-2 ring-destructive wrapper; today gets ring-primary when applicable
    - overflow-x-auto wrapper with data-testid contribution-heatmap
    - Import react-activity-calendar/tooltips.css; tooltips.activity.text uses formatHeatmapTooltip
    - Toggle error shows toast.error on catch — same message as Phase 2 dot grid
    - Test: click scheduled missed date calls toggle; future date does not call toggle
    - Test: wrapper has overflow-x-auto class
    - npm test -- src/components/heatmap/ContributionHeatmap.test.tsx -x passes
  </acceptance_criteria>
  <behavior>
    - Returns null while isLoading
    - Tappable cells wrapped for 44px touch targets via renderBlock outer wrapper min-h-11 min-w-11 flex center per UI-SPEC
  </behavior>
  <action>
    Create ContributionHeatmap accepting habitId and frequency props.

    Migrate toggle and schedule guard semantics from HistoryDotGrid — only completed and missed states invoke toggle (D-16).

    Use renderBlock pattern from 03-RESEARCH.md — do not use removed v3 eventHandlers prop.

    Create ContributionHeatmap.test.tsx mocking useHeatmapData with MWF fixture patterns from HistoryDotGrid.test.tsx.
  </action>
  <verify>
    <automated>npm test -- src/components/heatmap/ContributionHeatmap.test.tsx -x</automated>
  </verify>
  <done>52-week heatmap component with schedule-aware interaction and Spanish tooltips ready for history page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HabitHistoryPage integration and HistoryDotGrid removal</name>
  <files>src/pages/HabitHistoryPage.tsx, src/pages/HabitHistoryPage.test.tsx, src/components/habits/HistoryDotGrid.tsx, src/components/habits/HistoryDotGrid.test.tsx</files>
  <read_first>src/pages/HabitHistoryPage.tsx, .planning/phases/03-dashboard-progress-visualization/03-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - HabitHistoryContent subtitle text is Historial not Esta semana (D-11)
    - HistoryDotGrid import and usage removed; ContributionHeatmap rendered with habit.id and habit.frequency (D-09, D-10)
    - Back control is button with onClick navigate(-1) — no Link to="/" (D-12)
    - Loading shell back link also uses navigate(-1)
    - Spacing: StatCards to subtitle 16px (mt-4); subtitle to heatmap 32px (mt-8)
    - Delete src/components/habits/HistoryDotGrid.tsx and HistoryDotGrid.test.tsx after ContributionHeatmap tests cover equivalent behaviors
    - HabitHistoryPage.test.tsx asserts navigate(-1) on back click and Historial subtitle present
    - grep confirms HistoryDotGrid not imported anywhere in src/
    - npm test passes full suite
  </acceptance_criteria>
  <action>
    Update HabitHistoryPage: replace Link imports with useNavigate for back button per D-12.

    Replace HistoryDotGrid block with ContributionHeatmap below Historial subtitle.

    Preserve HabitHistoryContent sub-component and StatCards wiring unchanged.

    Create HabitHistoryPage.test.tsx with navigate mock — assert back calls navigate(-1).

    Delete HistoryDotGrid.tsx and HistoryDotGrid.test.tsx per D-09 — do not keep unused reference copy.

    Remove any HistoryDotGrid exports from barrel files if present.
  </action>
  <verify>
    <automated>npm test -x</automated>
  </verify>
  <done>VIZ-01 complete — user views and toggles 52-week heatmap on history; dot grid fully removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Heatmap cell click → Dexie | Date strings cross from UI to completionRepository.toggle |
| Habit name on history page | Rendered from local Dexie — trusted single-user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-07 | Tampering | Heatmap cell toggle | medium | mitigate | renderBlock only invokes toggle for completed and missed states; isFutureDate guard via getWeekDayState |
| T-3-08 | Spoofing | Habit name in history header | low | mitigate | React text escaping — no dangerouslySetInnerHTML |
| T-3-09 | Denial of Service | 52-week grid render | low | accept | Memoized domain builder; horizontal scroll container; dashboard excludes heatmap per Pitfall 5 |
</threat_model>

<verification>
- npm test -x full suite green
- npm run build succeeds
- Manual: history page shows heatmap; toggle updates stats and dashboard streaks; back from Panel returns to Panel
</verification>

<success_criteria>
- VIZ-01 requirement satisfied — GitHub-style contribution grid per habit with tap-to-toggle
- All locked decisions D-09 through D-16 implemented
- HistoryDotGrid deleted with no remaining imports
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-progress-visualization/03-04-SUMMARY.md` when done
</output>
