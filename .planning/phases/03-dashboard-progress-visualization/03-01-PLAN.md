---
phase: 03-dashboard-progress-visualization
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/domain/heatmap.ts
  - src/domain/heatmap.test.ts
autonomous: false
requirements:
  - VIZ-01
must_haves:
  truths:
    - 52-week heatmap date range spans HEATMAP_WEEKS (52) from today backward using noon-local parse (D-13, VIZ-01)
    - Completed scheduled days map to Activity level 4; missed/not-scheduled/future map to level 0 (D-14, UI-SPEC)
    - formatHeatmapTooltip returns Spanish status labels per D-15 (Completado, Perdido, No programado, Futuro)
    - buildHeatmapActivities reuses getWeekDayState from stats.ts — no duplicate schedule logic
    - statement: IndexedDB read failure on dashboard list
      verification: backstop
  artifacts:
    - src/domain/heatmap.ts
    - src/domain/heatmap.test.ts
    - package.json
  key_links:
    - getHeatmapDateRange calls subWeeks with T12:00:00 parse then getLocalDateString
    - buildHeatmapActivities iterates via iterateDaysInRange from dates.ts
    - cellStates Map drives ContributionHeatmap renderBlock in Plan 04
  prohibitions:
    - Must not use eventHandlers prop on ActivityCalendar (removed in v3)
    - Must not use toISOString().slice(0,10) for day keys
---

## Phase Goal

**As a** habit tracker user, **I want to** see all streaks at a glance and explore each habit's full history on a GitHub-style contribution grid, **so that** I stay motivated by visual progress I cannot ignore.

<objective>
Install react-activity-calendar and implement schedule-aware heatmap domain functions with TDD coverage — pure foundation for ContributionHeatmap in Plan 04.

Purpose: VIZ-01 requires 52-week Activity[] builder with schedule-aware cell states before any React heatmap wiring (D-13, D-14, D-15).
Output: heatmap.ts, heatmap.test.ts, react-activity-calendar@3.2.1 in package.json.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-progress-visualization/03-CONTEXT.md
@.planning/phases/03-dashboard-progress-visualization/03-RESEARCH.md
@.planning/phases/03-dashboard-progress-visualization/03-PATTERNS.md
@.planning/phases/03-dashboard-progress-visualization/03-UI-SPEC.md
@.planning/phases/03-dashboard-progress-visualization/03-VALIDATION.md
@src/domain/stats.ts
@src/domain/dates.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify react-activity-calendar package legitimacy</name>
  <files>package.json</files>
  <what-built>Package legitimacy audit flagged react-activity-calendar@3.2.1 as [SUS] (too-new v3.2.1 release). Locked STACK.md version requires human confirmation before install.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/react-activity-calendar — confirm publisher grubersjoe, version 3.2.1, ~50K weekly downloads
    2. Open https://github.com/grubersjoe/react-activity-calendar — confirm official repo, no postinstall scripts, Storybook docs present
    3. Confirm package matches locked decision in STACK.md (not an alternative like react-calendar-heatmap)
  </how-to-verify>
  <action>Pause execution until human verifies npm and GitHub sources for react-activity-calendar@3.2.1 per RESEARCH Package Legitimacy Audit [SUS] disposition. Do not run npm install until checkpoint approved.</action>
  <verify>
    <human-check>Human confirms package legitimacy on npm and GitHub; resume with approved signal</human-check>
  </verify>
  <done>Human approved react-activity-calendar@3.2.1 install; executor may proceed to Task 3 npm install.</done>
  <resume-signal>Type "approved" to proceed with npm install react-activity-calendar@3.2.1</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — failing heatmap domain tests</name>
  <files>src/domain/heatmap.test.ts</files>
  <read_first>src/domain/stats.test.ts, src/domain/stats.ts, .planning/phases/03-dashboard-progress-visualization/03-RESEARCH.md</read_first>
  <acceptance_criteria>
    - heatmap.test.ts imports from ./heatmap and fails because module does not exist yet
    - Fixed today fixture fixedToday = '2026-07-19' with daily and monWedFri = { type: 'weekly', days: [1, 3, 5] } frequencies
    - Test: getHeatmapDateRange('2026-07-19') returns start ~52 weeks before end inclusive
    - Test: buildHeatmapActivities daily habit with one completion → that date has level 4 and state completed
    - Test: buildHeatmapActivities missed scheduled day → level 0 and state missed in cellStates
    - Test: buildHeatmapActivities MWF habit → Tuesday cellStates has not-scheduled
    - Test: formatHeatmapTooltip('2026-07-14', 'completed') contains Completado
    - Test: formatHeatmapTooltip with missed state contains Perdido
    - npm test -- src/domain/heatmap.test.ts fails with import/module-not-found, not assertion pass
  </acceptance_criteria>
  <behavior>
    - getHeatmapDateRange(today) returns { start, end } where end === today
    - buildHeatmapActivities returns { activities: Activity[], cellStates: Map<string, WeekDayState> }
    - formatHeatmapTooltip uses date-fns format with es locale and STATUS_LABELS per D-15
  </behavior>
  <action>
    Create src/domain/heatmap.test.ts with all cases from 03-VALIDATION.md Nyquist table for VIZ-01 domain layer.

    Use completed(...dates) Set helper pattern from stats.test.ts. Assert activities array length matches iterateDaysInRange day count for 52-week window (spot-check range boundaries, not every cell).

    Do not create heatmap.ts yet — RED commit must fail on missing implementation.
  </action>
  <verify>
    <automated>npm test -- src/domain/heatmap.test.ts 2>&1 | grep -qE 'FAIL|Error|Cannot find'; test $? -eq 0</automated>
  </verify>
  <done>Failing heatmap test suite documents D-13 through D-16 domain predicates before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — heatmap.ts and package install</name>
  <files>src/domain/heatmap.ts, package.json, package-lock.json</files>
  <read_first>src/domain/heatmap.test.ts, .planning/phases/03-dashboard-progress-visualization/03-PATTERNS.md, .planning/phases/03-dashboard-progress-visualization/03-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - npm install react-activity-calendar@3.2.1 completes successfully after checkpoint approval
    - HEATMAP_WEEKS = 52 exported constant
    - getHeatmapDateRange uses subWeeks from date-fns/subWeeks with noon-local parse
    - buildHeatmapActivities delegates every cell to getWeekDayState from stats.ts
    - Completed cells: count 1, level 4 per UI-SPEC binary mapping; non-completed level 0
    - formatHeatmapTooltip exported with Spanish STATUS_LABELS per D-15
    - No React or Dexie imports in domain layer
    - npm test -- src/domain/heatmap.test.ts passes
  </acceptance_criteria>
  <action>
    Run npm install react-activity-calendar@3.2.1 only after checkpoint approval.

    Create src/domain/heatmap.ts exporting HEATMAP_WEEKS, getHeatmapDateRange, buildHeatmapActivities, formatHeatmapTooltip.

    Import Activity type from react-activity-calendar for activities array typing. Import getWeekDayState and WeekDayState from ./stats; iterateDaysInRange and getLocalDateString from ./dates.

    Use per-function date-fns imports (subWeeks, format) and es locale from date-fns/locale — match dates.ts convention.
  </action>
  <verify>
    <automated>npm test -- src/domain/heatmap.test.ts -x</automated>
  </verify>
  <done>Heatmap domain module green; react-activity-calendar installed for Plan 04 component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package.json | External package crosses trust boundary before install |
| domain/heatmap.ts | Pure functions — no I/O; trusted internal logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Tampering | npm install react-activity-calendar | high | mitigate | Package legitimacy checkpoint before install; slopcheck on package.json diff |
| T-3-02 | Tampering | heatmap date range | medium | mitigate | Reuse getLocalDateString and noon-local T12:00:00 parse — no UTC slice |
| T-3-SC | Tampering | npm/pip/cargo installs | high | mitigate | Human checkpoint for [SUS] react-activity-calendar@3.2.1 per RESEARCH audit |
</threat_model>

<verification>
- npm test -- src/domain/heatmap.test.ts -x passes
- npm run build succeeds with react-activity-calendar type imports
</verification>

<success_criteria>
- All heatmap domain tests green
- Package installed at locked version 3.2.1
- cellStates Map available for renderBlock guards in Plan 04
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-progress-visualization/03-01-SUMMARY.md` when done
</output>
