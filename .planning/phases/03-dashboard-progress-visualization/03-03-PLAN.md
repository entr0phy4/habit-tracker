---
phase: 03-dashboard-progress-visualization
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/components/dashboard/DashboardCard.tsx
  - src/components/dashboard/DashboardCard.test.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/DashboardPage.test.tsx
autonomous: true
requirements:
  - DASH-01
must_haves:
  truths:
    - User opens Panel tab and sees every active habit current streak at a glance (DASH-01, D-05)
    - Dashboard cards show habit name plus Flame icon plus streak count only — no rate or week dots (D-05)
    - Highest streak habit appears first in list (D-06)
    - Tapping card navigates to /habits/:id/history (D-07)
    - Zero active habits shows Spanish empty state No hay hábitos activos with body copy per UI-SPEC
    - Dashboard returns null while useDashboardHabits isLoading — no skeleton (UI-SPEC loading)
    - statement: Streak count above 999 does not break card layout
      verification: backstop
  artifacts:
    - src/components/dashboard/DashboardCard.tsx
    - src/pages/DashboardPage.tsx
  key_links:
    - DashboardPage calls useDashboardHabits and maps items to DashboardCard
    - DashboardCard onClick navigates via useNavigate to /habits/${habit.id}/history
    - Flame badge pattern matches HabitRow — text-primary icon, text-foreground count
  prohibitions:
    - Must not show completion rate or week dots on dashboard cards (D-05, deferred ENH-03)
    - Must not include archived habits (D-08)
    - Must not render full heatmap on dashboard (PITFALLS Pitfall 5)
---

## Phase Goal

**As a** habit tracker user, **I want to** see all streaks at a glance and explore each habit's full history on a GitHub-style contribution grid, **so that** I stay motivated by visual progress I cannot ignore.

<objective>
Ship DASH-01 vertical slice — user opens Panel tab, sees streak-sorted habit cards, taps through to history.

Purpose: Dashboard is the glanceable streak leaderboard per D-05 through D-08; first user-visible Phase 3 capability after navigation shell.
Output: DashboardCard, DashboardPage with tests replacing Plan 02 stub.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-progress-visualization/03-CONTEXT.md
@.planning/phases/03-dashboard-progress-visualization/03-UI-SPEC.md
@.planning/phases/03-dashboard-progress-visualization/03-PATTERNS.md
@.planning/phases/03-dashboard-progress-visualization/03-02-SUMMARY.md
@src/components/habits/HabitRow.tsx
@src/pages/TodayPage.tsx
@src/hooks/useDashboardHabits.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing DashboardPage and DashboardCard tests</name>
  <files>src/pages/DashboardPage.test.tsx, src/components/dashboard/DashboardCard.test.tsx</files>
  <read_first>src/pages/TodayPage.test.tsx, src/components/habits/HabitRow.test.tsx, .planning/phases/03-dashboard-progress-visualization/03-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - DashboardPage.test.tsx imports DashboardPage and fails until implementation complete
    - Mock useDashboardHabits returns two habits streaks 7 and 3 — test asserts 7-streak habit appears before 3-streak
    - Mock empty items → screen shows No hay hábitos activos
    - DashboardCard.test.tsx asserts habit name and streak number visible
    - DashboardCard click calls navigate with /habits/{id}/history (D-07)
    - Flame aria-label includes streak count
    - npm test -- src/pages/DashboardPage.test.tsx src/components/dashboard/DashboardCard.test.tsx fails before Task 2
  </acceptance_criteria>
  <behavior>
    - DashboardPage loading state returns null when isLoading true
    - DashboardCard is full-width button min-h-11 with truncate name and Flame badge
  </behavior>
  <action>
    Create test files with vi.mock for useDashboardHabits and useNavigate following TodayPage.test.tsx and HabitRow.test.tsx patterns.

    Use MemoryRouter wrapper for DashboardPage tests.

    Do not create production components yet — tests may import from paths that fail until Task 2.
  </action>
  <verify>
    <automated>npm test -- src/pages/DashboardPage.test.tsx src/components/dashboard/DashboardCard.test.tsx 2>&1 | grep -qE 'FAIL|Error|Cannot find'; test $? -eq 0</automated>
  </verify>
  <done>Failing dashboard component tests define DASH-01 acceptance criteria before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — DashboardCard and DashboardPage</name>
  <files>src/components/dashboard/DashboardCard.tsx, src/pages/DashboardPage.tsx</files>
  <read_first>src/components/habits/HabitRow.tsx, src/pages/ManageHabitsPage.tsx, .planning/phases/03-dashboard-progress-visualization/03-PATTERNS.md</read_first>
  <acceptance_criteria>
    - DashboardPage uses AppShell title Panel with hasTabBar
    - Renders ul gap-2 list of DashboardCard from useDashboardHabits items — order preserved from hook sort (D-06)
    - Empty state heading No hay hábitos activos and body Toca + en Hoy para crear tu primer hábito. per UI-SPEC
    - DashboardCard shows habit.name truncate, Flame h-4 w-4 text-primary, count text-xs font-semibold text-foreground
    - Zero streak shows Flame plus 0 — not hidden
    - Returns null while isLoading
    - npm test -- src/pages/DashboardPage.test.tsx src/components/dashboard/DashboardCard.test.tsx -x passes
  </acceptance_criteria>
  <action>
    Replace Plan 02 DashboardPage stub with full implementation.

    Create DashboardCard as button element per 03-PATTERNS.md — navigate to /habits/:id/history on click (D-07).

    Echo HabitRow Flame badge markup for visual consistency (D-05). No completion rate or week dots.

    List uses pb-20 or adequate bottom padding inside AppShell hasTabBar layout.
  </action>
  <verify>
    <automated>npm test -- src/pages/DashboardPage.test.tsx src/components/dashboard/DashboardCard.test.tsx -x</automated>
  </verify>
  <done>User can open Panel, see sorted streak cards, tap to history — DASH-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client → Dexie | Habit names rendered from local IndexedDB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-05 | Spoofing/Tampering | DashboardCard habit name | medium | mitigate | React text content only — no dangerouslySetInnerHTML |
| T-3-06 | Information Disclosure | Streak counts | low | accept | Local-only data; single-user v1 |
</threat_model>

<verification>
- npm test -- src/pages/DashboardPage.test.tsx src/components/dashboard/DashboardCard.test.tsx src/hooks/useDashboardHabits.test.ts -x passes
- Manual: Panel tab shows habits sorted by streak; tap card opens history
</verification>

<success_criteria>
- DASH-01 requirement satisfied — dashboard displays all current streaks at a glance
- All locked decisions D-05 through D-08 implemented on dashboard surface
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-progress-visualization/03-03-SUMMARY.md` when done
</output>
