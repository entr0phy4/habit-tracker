---
phase: 03-dashboard-progress-visualization
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/hooks/useDashboardHabits.ts
  - src/hooks/useDashboardHabits.test.ts
  - src/components/layout/BottomTabBar.tsx
  - src/components/layout/BottomTabBar.test.tsx
  - src/components/layout/MainLayout.tsx
  - src/components/layout/AppShell.tsx
  - src/pages/TodayPage.tsx
  - src/components/habits/FloatingAddButton.tsx
  - src/App.tsx
  - src/pages/DashboardPage.tsx
autonomous: true
requirements:
  - DASH-01
must_haves:
  truths:
    - Bottom tab bar shows Spanish labels Hoy and Panel with icons — not English, not icon-only (D-01, D-02)
    - Today remains home at /; dashboard at /dashboard (D-03)
    - Tab bar visible on /, /dashboard, and /habits/:id/history; hidden on form and manage routes (D-04, UI-SPEC tab bar on history)
    - useDashboardHabits returns active habits only sorted by currentStreak descending (D-06, D-08)
    - FAB on Today sits above tab bar; list has pb-28 clearance (UI-SPEC spacing)
    - statement: Heatmap horizontal scroll shows recent weeks without manual scroll
      verification: backstop
  artifacts:
    - src/hooks/useDashboardHabits.ts
    - src/components/layout/BottomTabBar.tsx
    - src/components/layout/MainLayout.tsx
  key_links:
    - MainLayout renders Outlet plus BottomTabBar for tab-bar routes
    - useDashboardHabits batch useLiveQuery computes streak via calculateCurrentStreak per habit
    - AppShell hasTabBar prop applies pb-20 bottom padding on tab-bar pages
    - App.tsx nests /, /dashboard, /habits/:id/history under MainLayout; form routes outside
  prohibitions:
    - Must not use per-card useStreak on dashboard parent — batch hook required for sort (RESEARCH Pitfall 4)
    - Must not hardcode Link to="/" for history back — Plan 04 handles navigate(-1) per D-12
---

## Phase Goal

**As a** habit tracker user, **I want to** see all streaks at a glance and explore each habit's full history on a GitHub-style contribution grid, **so that** I stay motivated by visual progress I cannot ignore.

<objective>
Ship navigation shell and dashboard data hook — user can switch between Hoy and Panel tabs; streak-sorted habit data ready for dashboard cards in Plan 03.

Purpose: DASH-01 requires persistent bottom navigation (D-01–D-04) and batch streak query (D-06, D-08) before dashboard UI slice.
Output: useDashboardHabits, BottomTabBar, MainLayout, updated App.tsx routing, Today FAB clearance.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-progress-visualization/03-CONTEXT.md
@.planning/phases/03-dashboard-progress-visualization/03-UI-SPEC.md
@.planning/phases/03-dashboard-progress-visualization/03-PATTERNS.md
@.planning/phases/03-dashboard-progress-visualization/03-01-SUMMARY.md
@src/hooks/useStreak.ts
@src/App.tsx
@src/components/layout/AppShell.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useDashboardHabits batch hook and tests</name>
  <files>src/hooks/useDashboardHabits.ts, src/hooks/useDashboardHabits.test.ts</files>
  <read_first>src/hooks/useTodayHabits.test.ts, src/hooks/useHabitStats.ts, .planning/phases/03-dashboard-progress-visualization/03-PATTERNS.md</read_first>
  <acceptance_criteria>
    - useDashboardHabits(todayKey?) returns { items: { habit, currentStreak }[], isLoading }
    - Filters db.habits where archived is false (D-08)
    - Sorts items by currentStreak descending (D-06)
    - Uses completionRepository.getByHabitInRange and calculateCurrentStreak per habit
    - isLoading true when useLiveQuery result is undefined
    - Test: two habits with streaks 3 and 7 → 7 appears first
    - Test: archived habit excluded from items
    - npm test -- src/hooks/useDashboardHabits.test.ts -x passes
  </acceptance_criteria>
  <behavior>
    - Batch query pattern mirrors useHabitStats range fetch but across all active habits
    - todayKey defaults to getLocalDateString(new Date())
  </behavior>
  <action>
    Create src/hooks/useDashboardHabits.ts following batch hook shape in 03-RESEARCH.md Pattern 2.

    Create src/hooks/useDashboardHabits.test.ts with Dexie integration setup from useTodayHabits.test.ts (vi.setSystemTime, db.delete, db.open, clear tables in beforeEach).

    Seed habits and completions to verify sort order and archived exclusion per D-06 and D-08.
  </action>
  <verify>
    <automated>npm test -- src/hooks/useDashboardHabits.test.ts -x</automated>
  </verify>
  <done>Reactive sorted dashboard habit list ready for DashboardPage consumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BottomTabBar, MainLayout, and App routing</name>
  <files>src/components/layout/BottomTabBar.tsx, src/components/layout/BottomTabBar.test.tsx, src/components/layout/MainLayout.tsx, src/App.tsx</files>
  <read_first>.planning/phases/03-dashboard-progress-visualization/03-UI-SPEC.md, src/App.tsx, .planning/phases/03-dashboard-progress-visualization/03-PATTERNS.md</read_first>
  <acceptance_criteria>
    - BottomTabBar renders NavLink to / with label Hoy and to /dashboard with label Panel (D-02)
    - Icons CalendarCheck and LayoutGrid from lucide-react per UI-SPEC (20×20px)
    - Each tab min-h-11 touch target; aria-label Navegación principal on nav
    - MainLayout renders Outlet then BottomTabBar fixed at bottom
    - App.tsx nests /, /dashboard, /habits/:id/history under MainLayout element
    - Form routes (/habits/new, /habits/manage, /habits/:id/edit) remain outside MainLayout — no tab bar
    - BottomTabBar.test.tsx asserts Hoy and Panel labels visible
    - npm test -- src/components/layout/BottomTabBar.test.tsx -x passes
  </acceptance_criteria>
  <action>
    Create BottomTabBar with fixed bottom positioning, max-w-[480px] mx-auto, border-t border-border bg-background per UI-SPEC.

    Create MainLayout with Outlet from react-router wrapping page content.

    Update App.tsx to nest /, /dashboard, and /habits/:id/history under MainLayout. Form routes stay outside.

    Create src/pages/DashboardPage.tsx stub: AppShell title Panel with hasTabBar and empty main — Plan 03 replaces with full dashboard list.

    Keep HabitHistoryPage inside MainLayout so tab bar shows on history per UI-SPEC.
  </action>
  <verify>
    <automated>npm test -- src/components/layout/BottomTabBar.test.tsx -x</automated>
  </verify>
  <done>User can switch Hoy/Panel tabs; history route shows tab bar; form routes hide tab bar.</done>
</task>

<task type="auto">
  <name>Task 3: AppShell tab-bar padding and Today FAB clearance</name>
  <files>src/components/layout/AppShell.tsx, src/pages/TodayPage.tsx, src/components/habits/FloatingAddButton.tsx</files>
  <read_first>.planning/phases/03-dashboard-progress-visualization/03-UI-SPEC.md, src/pages/TodayPage.tsx, src/components/habits/FloatingAddButton.tsx</read_first>
  <acceptance_criteria>
    - AppShell accepts optional hasTabBar boolean prop; when true main uses pb-20
    - TodayPage passes hasTabBar={true} to AppShell
    - Today list ul uses pb-28 instead of pb-12 for FAB plus tab bar clearance
    - FloatingAddButton bottom position raised to bottom-[4.5rem] (72px) above tab bar
    - npm run build passes
  </acceptance_criteria>
  <action>
    Extend AppShell with hasTabBar?: boolean prop defaulting false. When true, apply pb-20 to main element per UI-SPEC.

    Update TodayPage all AppShell usages to pass hasTabBar. Increase list bottom padding to pb-28.

    Update FloatingAddButton className bottom-4 to bottom-[4.5rem] so FAB clears 56px tab bar.

    Dashboard stub page should also pass hasTabBar when using AppShell.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Tab bar and FAB do not overlap Today content; shell padding consistent across tab-bar routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client | Tab navigation and Dexie reads — single-user local SPA |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-03 | Spoofing | Dashboard habit names in future cards | low | accept | React text escaping; names rendered as text nodes |
| T-3-04 | Tampering | useDashboardHabits archived filter | medium | mitigate | Filter !habit.archived in query — archived habits never surface (D-08) |
</threat_model>

<verification>
- npm test -- src/hooks/useDashboardHabits.test.ts src/components/layout/BottomTabBar.test.tsx -x passes
- npm run build succeeds
- Manual: visit / and /dashboard — tab bar visible; visit /habits/new — tab bar hidden
</verification>

<success_criteria>
- Tab navigation functional with Spanish labels
- useDashboardHabits returns sorted active habits
- Today FAB and list clear fixed tab bar
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-progress-visualization/03-02-SUMMARY.md` when done
</output>
