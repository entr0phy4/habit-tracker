---
phase: 01-habit-management-daily-logging
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/components/habits/HistoryDotGrid.tsx
  - src/components/habits/HistoryDotGrid.test.tsx
  - src/pages/HabitHistoryPage.tsx
  - src/hooks/useCompletions.ts
  - src/App.tsx
  - src/infrastructure/completionRepository.test.ts
  - src/components/habits/HabitRow.tsx
autonomous: true
requirements:
  - LOG-02
  - UI-02
must_haves:
  truths:
    - User navigates to /habits/:id/history from row CalendarDays button (D-14)
    - History screen shows last 7 calendar days inclusive of today as dot grid (D-15, D-16)
    - Tapping a dot toggles completion for that date; re-tap undoes (LOG-02 idempotency)
    - Future dates cannot be marked complete — grid only renders past+today (D-17)
    - Today's dot shows green 2px ring whether complete or incomplete (UI-SPEC)
    - Toggle on past day updates IndexedDB immediately via useLiveQuery (LOG-02 concurrency — last write wins)
    - statement: Failed history dot toggle shows sonner toast "Couldn't update. Try again."
      verification: backstop
    - statement: Long habit name on history header wraps max 2 lines with ellipsis
      verification: backstop
  artifacts:
    - src/components/habits/HistoryDotGrid.tsx
    - src/pages/HabitHistoryPage.tsx
    - src/hooks/useCompletions.ts
  key_links:
    - useCompletions queries completions between getLast7Days range for habitId
    - HistoryDotGrid calls useToggleCompletion with explicit date per dot tap
    - HabitRow CalendarDays navigates to /habits/:id/history
  prohibitions:
    - Must not render future dates in the 7-day grid
    - Must not allow completionRepository.toggle on future dates
---

<objective>
Ship the 7-day history screen so users can toggle completions for past days per habit.

Purpose: Satisfy LOG-02 and locked decisions D-14 through D-17 — the final slice of Phase 1 core loop.
Output: HabitHistoryPage, HistoryDotGrid, useCompletions hook, wired navigation from Today rows.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-habit-management-daily-logging/01-CONTEXT.md
@.planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md
@.planning/phases/01-habit-management-daily-logging/01-PATTERNS.md
@.planning/phases/01-habit-management-daily-logging/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: useCompletions hook and repository range tests</name>
  <files>src/hooks/useCompletions.ts, src/infrastructure/completionRepository.ts, src/infrastructure/completionRepository.test.ts</files>
  <read_first>src/infrastructure/completionRepository.ts, src/domain/dates.ts, .planning/phases/01-habit-management-daily-logging/01-PATTERNS.md</read_first>
  <acceptance_criteria>
    - useCompletions(habitId) returns dates from getLast7Days and completedDates Set via useLiveQuery
    - completionRepository.getByHabitInRange returns dates within inclusive start/end
    - completionRepository.test.ts covers toggle on yesterday, toggle off idempotency, future date rejected
    - npx vitest run src/infrastructure/completionRepository.test.ts passes
  </acceptance_criteria>
  <action>
    Implement useCompletions per PATTERNS.md using getLast7Days and Dexie between query on compound key range.

    Ensure completionRepository.getByHabitInRange is fully implemented if stubbed in Plan 01.

    Extend completionRepository.test.ts: seed completions for days in 7-day window; assert toggle past day twice returns to original state (LOG-02 idempotency). Assert isFutureDate guard prevents tomorrow writes.

    Hook must not import React components — infrastructure access only through repository from hook layer.
  </action>
  <verify>
    <automated>npx vitest run src/infrastructure/completionRepository.test.ts</automated>
  </verify>
  <done>Completion range queries and past-day toggle logic tested.</done>
</task>

<task type="auto">
  <name>Task 2: HistoryDotGrid and HabitHistoryPage</name>
  <files>src/components/habits/HistoryDotGrid.tsx, src/components/habits/HistoryDotGrid.test.tsx, src/pages/HabitHistoryPage.tsx, src/App.tsx</files>
  <read_first>.planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md, .planning/phases/01-habit-management-daily-logging/01-CONTEXT.md</read_first>
  <acceptance_criteria>
    - HistoryDotGrid renders 7 columns with day letter labels and 32x32 touch targets (UI-SPEC WeekDotGrid)
    - Incomplete dots #30363d, complete #3fb950, today gets ring-2 ring-primary
    - Tap dot calls toggle with that date string; disabled logic skips isFutureDate (D-17)
    - HabitHistoryPage shows Back to /, habit name heading with break-words max 2 lines truncate, subtitle "Last 7 days"
    - Route /habits/:id/history registered in App.tsx
    - Grid contains exactly 7 days — no eighth future day (D-15)
  </acceptance_criteria>
  <action>
    Create HistoryDotGrid.tsx composing useCompletions and useToggleCompletion. Each dot is a button with aria-pressed for completion state.

    Create HabitHistoryPage loading habit by :id via useLiveQuery; render HistoryDotGrid centered. Handle missing habit with navigate to /.

    Apply UI-SPEC spacing 16px between columns. Pressed state scale(0.9).

    Per D-16: tap individual dots toggles that day. Per D-17: only past and today in grid — getLast7Days already excludes future beyond today.

    On toggle failure catch, toast.error("Couldn't update. Try again.") per unresolved UI-SPEC assumption.

    Write src/components/habits/HistoryDotGrid.test.tsx with @testing-library/react: renders exactly 7 dot buttons; today dot has ring class; tap fires toggle with correct date string.
  </action>
  <verify>
    <automated>npx vitest run src/components/habits/HistoryDotGrid.test.tsx src/infrastructure/completionRepository.test.ts src/domain/dates.test.ts</automated>
  </verify>
  <done>History screen renders 7-day grid with working dot toggles.</done>
</task>

<task type="auto">
  <name>Task 3: Wire history navigation and phase verification gate</name>
  <files>src/components/habits/HabitRow.tsx, src/App.tsx</files>
  <read_first>src/components/habits/HabitRow.tsx, src/App.tsx</read_first>
  <acceptance_criteria>
    - HabitRow CalendarDays button navigates to /habits/:id/history with correct habit id
    - Full vitest suite passes: npx vitest run exits 0
    - npm run build exits 0
    - All Phase 1 requirement IDs have automated or documented manual coverage in 01-VALIDATION.md tasks
  </acceptance_criteria>
  <action>
    Confirm HabitRow history button uses useNavigate to /habits/${habit.id}/history. Verify stopPropagation still prevents accidental toggle.

    Run full test suite and production build as phase gate.

    Update nothing in 01-VALIDATION.md (orchestrator owns) — executor runs full suite only.

    Manual smoke checklist: create habit → toggle today → open history → toggle yesterday → refresh → states persist.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <done>Phase 1 vertical slices complete; LOG-02 satisfied end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Date selection → completionRepository | Past/today dates only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | HistoryDotGrid date param | medium | mitigate | isFutureDate guard in repository; grid only renders getLast7Days |
| T-01-10 | Elevation of Privilege | N/A | low | accept | No multi-user scope |
</threat_model>

<verification>
- npx vitest run && npm run build — both green
- Manual: history dot toggle for 3 different past days, verify Today row reflects today's state
</verification>

<success_criteria>
- LOG-02 complete: past-day toggling from dedicated history screen
- D-14 through D-17 implemented
- Full Phase 1 test suite and build pass
</success_criteria>

## Artifacts this phase produces

| Symbol / Path | Kind | Plan |
|---------------|------|------|
| `HistoryDotGrid` | component | 01-04 |
| `HabitHistoryPage` | page | 01-04 |
| `useCompletions` | hook | 01-04 |

<output>
Create `.planning/phases/01-habit-management-daily-logging/01-04-SUMMARY.md` when done
</output>
