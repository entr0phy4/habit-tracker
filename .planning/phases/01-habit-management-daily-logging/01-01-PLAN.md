---
phase: 01-habit-management-daily-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - components.json
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/lib/utils.ts
  - src/test/setup.ts
  - src/domain/types.ts
  - src/domain/dates.ts
  - src/domain/schedule.ts
  - src/domain/dates.test.ts
  - src/domain/schedule.test.ts
  - src/infrastructure/db.ts
  - src/infrastructure/habitRepository.ts
  - src/infrastructure/completionRepository.ts
  - src/infrastructure/db.test.ts
  - src/infrastructure/habitRepository.test.ts
  - src/infrastructure/completionRepository.test.ts
  - src/hooks/useTodayHabits.ts
  - src/hooks/useToggleCompletion.ts
  - src/pages/TodayPage.tsx
  - src/pages/HabitNewPage.tsx
  - src/components/layout/AppShell.tsx
  - src/components/habits/HabitRow.tsx
  - src/components/habits/FloatingAddButton.tsx
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/sonner.tsx
  - src/integration/walkingSkeleton.test.ts
autonomous: true
requirements:
  - DATA-01
  - HABT-01
  - LOG-01
  - LOG-03
  - UI-01
must_haves:
  truths:
    - User can create a habit with a name and daily frequency via /habits/new and land back on Today with a toast (D-08 partial — toast wired in Plan 02)
    - User sees habits due today in a flat list on / (D-01); habits not due today are hidden (D-02)
    - User can toggle today's completion by tapping a habit row on desktop (D-10 partial — swipe in Plan 03)
    - Completed row shows strikethrough and muted styling (D-11)
    - Toggling completion twice returns to incomplete state (LOG-02 idempotency probe — same-day toggle is upsert/delete)
    - Empty habit name is rejected on create (HABT-01 empty probe)
    - Habit and completion data survive simulated browser reopen via IndexedDB (DATA-01)
    - App renders with dark theme on html.dark root (UI-01)
    - statement: Today view shows centered error copy when IndexedDB is unavailable
      verification: backstop
  artifacts:
    - src/infrastructure/db.ts
    - src/infrastructure/habitRepository.ts
    - src/infrastructure/completionRepository.ts
    - src/pages/TodayPage.tsx
    - src/integration/walkingSkeleton.test.ts
  key_links:
    - useTodayHabits useLiveQuery reads habits filtered by isDueOnDate and joins today's completions
    - useToggleCompletion calls completionRepository.toggle with getLocalDateString at interaction time
    - HabitNewPage calls habitRepository.create then navigate('/')
  prohibitions:
    - Must not use localStorage as primary persistence store
    - Must not use toISOString().slice(0,10) for calendar day keys
    - Must not render habit names via dangerouslySetInnerHTML
---

## Phase Goal

**As a** habit tracker user, **I want to** create a daily habit, see it on Today, and check in with one tap, **so that** my progress survives browser restarts.

<objective>
Deliver the walking skeleton: greenfield Vite + React + Dexie stack with a failing-then-passing integration test proving create → today list → toggle → persistence.

Purpose: Establish architectural backbone (see 01-SKELETON.md) and the thinnest end-to-end habit loop before UX polish slices.
Output: Runnable `npm run dev` app with / and /habits/new routes, Dexie v1 schema, domain + repository layers with tests.

Note: The greenfield 43-file `files_modified` scope is intentional — Plan 01-01 delivers the full walking-skeleton stack (scaffold, domain, repositories, hooks, pages, integration test) in one plan per 01-SKELETON.md, not a thin stub deferred to later waves.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-habit-management-daily-logging/01-SKELETON.md
@.planning/phases/01-habit-management-daily-logging/01-CONTEXT.md
@.planning/phases/01-habit-management-daily-logging/01-RESEARCH.md
@.planning/phases/01-habit-management-daily-logging/01-PATTERNS.md
@.planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold app shell and failing walking-skeleton test</name>
  <files>package.json, vite.config.ts, index.html, src/index.css, src/App.tsx, src/main.tsx, src/test/setup.ts, src/integration/walkingSkeleton.test.ts, src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx, src/components/ui/sonner.tsx</files>
  <read_first>.planning/phases/01-habit-management-daily-logging/01-RESEARCH.md, .planning/phases/01-habit-management-daily-logging/01-PATTERNS.md, .planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md, .planning/research/STACK.md</read_first>
  <acceptance_criteria>
    - npx shadcn@latest init -t vite completes with dark theme, zinc base, new-york style; index.html has class="dark" on html element (UI-01)
    - npm install dexie@4.4.4 dexie-react-hooks@4.4.0 react-router@8.2.0 date-fns@4.4.0 sonner@2.0.7 and dev deps vitest@4.1.10 @testing-library/react@16.3.2 fake-indexeddb@6.2.5 jsdom per RESEARCH.md Package Legitimacy Audit (official SUS packages — no human checkpoint required)
    - npx shadcn@latest add button input label sonner succeeds
    - vite.config.ts includes test block with environment jsdom and setupFiles ./src/test/setup.ts
    - src/test/setup.ts wires fake-indexeddb into Dexie.dependencies per PATTERNS.md
    - src/index.css applies GitHub dark @theme tokens from UI-SPEC.md
    - src/integration/walkingSkeleton.test.ts exists and FAILS describing: create habit, appears in today due list, toggle completion, reopen db retains both records
    - npm run build exits 0
  </acceptance_criteria>
  <action>
    Initialize the greenfield project in repo root using shadcn Vite template (A3: if init conflicts with existing .planning files, scaffold into root with defaults). Install Phase 1 runtime and test dependencies from RESEARCH.md installation block — versions must match audit table.

    Configure Vitest in vite.config.ts with path alias @ → src. Create src/test/setup.ts with fake-indexeddb auto import and Dexie dependency injection.

    Apply UI-SPEC color tokens in src/index.css @theme block. Set html class="dark" in index.html.

    Create minimal src/App.tsx with BrowserRouter, Routes for / and /habits/new (stub pages OK), and Toaster from shadcn sonner.

    Write src/integration/walkingSkeleton.test.ts FIRST (RED): use fake-indexeddb, import repositories once they exist as stubs that throw, or test at repository+domain level until Task 2 fills implementations. Test must assert: habitRepository.create with daily frequency → useTodayHabits-equivalent query returns habit when today matches schedule → completionRepository.toggle → second db instance read still has habit + completion. Do not implement repositories in this task beyond empty stubs if needed to compile.

    Per D-05: habit creation route is /habits/new (not modal).
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/integration/walkingSkeleton.test.ts; [ $? -ne 0 ]</automated>
  </verify>
  <done>Project scaffolds, builds, and walking-skeleton integration test exists in failing (RED) state.</done>
</task>

<task type="auto">
  <name>Task 2: Domain layer and Dexie repositories with tests</name>
  <files>src/domain/types.ts, src/domain/dates.ts, src/domain/schedule.ts, src/domain/dates.test.ts, src/domain/schedule.test.ts, src/infrastructure/db.ts, src/infrastructure/habitRepository.ts, src/infrastructure/completionRepository.ts, src/infrastructure/db.test.ts, src/infrastructure/habitRepository.test.ts, src/infrastructure/completionRepository.test.ts</files>
  <read_first>src/domain/types.ts, src/infrastructure/db.ts, .planning/phases/01-habit-management-daily-logging/01-PATTERNS.md, .planning/research/ARCHITECTURE.md</read_first>
  <acceptance_criteria>
    - src/domain/types.ts exports Habit, Completion, Frequency with 0-6 day convention documented (A2)
    - getLocalDateString uses date-fns format with yyyy-MM-dd token only; dates.test.ts proves no UTC slice pattern in domain layer
    - isDueOnDate returns true for daily and correct weekdays for weekly; schedule.test.ts covers Mon/Wed/Fri fixture
    - isFutureDate rejects tomorrow (D-17 guard for repositories)
    - db.ts schema v1: habits id,archived,createdAt; completions [habitId+date] compound PK
    - habitRepository.create trims name, rejects empty string, max length 100 (T-01-01 / HABT-01 encoding probe)
    - completionRepository.toggle is idempotent upsert/delete per [habitId,date]
    - completionRepository.toggle no-ops on future dates
    - npx vitest run src/domain src/infrastructure exits 0
  </acceptance_criteria>
  <action>
    Implement pure domain modules per PATTERNS.md: types.ts, dates.ts (getLocalDateString, getLast7Days, isFutureDate), schedule.ts (isDueOnDate, isDaily). Use per-function date-fns imports.

    Implement infrastructure: db.ts singleton HabitTrackerDB, habitRepository (create, update, archive, delete with completion cascade in transaction), completionRepository (toggle with future guard, getByHabitInRange stub for Plan 04).

    Write unit tests dates.test.ts and schedule.test.ts. Write integration tests habitRepository.test.ts (create, empty name throws, update, archive), completionRepository.test.ts (toggle on/off idempotency, future date rejected), db.test.ts (data persists across closing and reopening Dexie instance).

    Ban calendar-day extraction via UTC ISO slice in src/domain and src/infrastructure — use local format helper only.

    Do not add streak fields to Habit. Do not use localStorage.
  </action>
  <verify>
    <automated>npx vitest run src/domain src/infrastructure</automated>
  </verify>
  <done>Domain and repository layers pass all unit/integration tests; walking-skeleton test may still fail on UI portions.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Today and Create pages to pass walking-skeleton test</name>
  <files>src/hooks/useTodayHabits.ts, src/hooks/useToggleCompletion.ts, src/pages/TodayPage.tsx, src/pages/HabitNewPage.tsx, src/components/layout/AppShell.tsx, src/components/habits/HabitRow.tsx, src/components/habits/FloatingAddButton.tsx, src/integration/walkingSkeleton.test.ts</files>
  <read_first>src/hooks/useTodayHabits.ts, src/pages/TodayPage.tsx, .planning/phases/01-habit-management-daily-logging/01-UI-SPEC.md, .planning/phases/01-habit-management-daily-logging/01-CONTEXT.md</read_first>
  <acceptance_criteria>
    - useTodayHabits filters !archived habits where isDueOnDate(frequency, today) and maps isCompleted from today's completions (D-01, D-02, LOG-03)
    - useToggleCompletion resolves getLocalDateString inside handler, not mount-only state (Pitfall 2)
    - TodayPage renders HabitRow list with 8px gap, FloatingAddButton fixed bottom-right (D-03), AppShell with "Today" heading
    - HabitRow min-h-11 (44px), row tap toggles on non-touch (D-10), strikethrough + muted when complete (D-11)
    - HabitNewPage: name input + submit creates daily habit (all days), navigates to / (D-07 default daily)
    - npm run dev serves / with dark background; walkingSkeleton.test.ts passes GREEN
    - npx vitest run exits 0
  </acceptance_criteria>
  <action>
    Implement hooks useTodayHabits and useToggleCompletion per PATTERNS.md. Components: AppShell (min-h-dvh bg-background), HabitRow (basic tap toggle — swipe deferred to Plan 03), FloatingAddButton navigates to /habits/new.

    Implement TodayPage composing useTodayHabits + useToggleCompletion. Implement HabitNewPage with shadcn Input + Button, default frequency { type: 'daily' }, calls habitRepository.create on submit, navigate('/') — sonner toast deferred to Plan 02 but navigation required now per D-08 flow.

  Empty state: when zero due habits, show UI-SPEC copy "No habits due today" and body text; FAB remains visible (D discretion).

    Extend walkingSkeleton.test.ts to GREEN. Run full vitest suite.

    Components must not import from infrastructure directly — only hooks call repositories.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Walking skeleton GREEN: user can create daily habit, see on Today, toggle completion, data persists in IndexedDB tests; npm run dev works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → React DOM | Habit names rendered as text nodes |
| npm registry → node_modules | Third-party package install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | habitRepository.create | medium | mitigate | Trim name; reject empty; max 100 chars; React text interpolation only |
| T-01-02 | Information Disclosure | IndexedDB | low | accept | Local-only v1; no encryption per PROJECT.md |
| T-01-03 | Spoofing | N/A | low | accept | No auth surface in Phase 1 |
| T-01-SC | Tampering | npm installs | high | mitigate | RESEARCH.md Package Legitimacy Audit; official packages only; no SLOP verdicts |
</threat_model>

<verification>
- npx vitest run — full suite green
- npm run build — production build succeeds
- npm run dev — manual smoke: create habit, toggle, refresh browser, data remains
</verification>

<success_criteria>
- Walking skeleton integration test passes
- Dexie schema v1 committed with habits + completions tables
- Today route shows due habits only with working tap toggle
- Dark theme visible on load
</success_criteria>

## Artifacts this phase produces

| Symbol / Path | Kind | Plan |
|---------------|------|------|
| `HabitTrackerDB` | class | 01-01 |
| `db` | singleton export | 01-01 |
| `habitRepository` | object (create, update, archive, delete) | 01-01 |
| `completionRepository` | object (toggle, getByHabitInRange) | 01-01 |
| `Frequency`, `Habit`, `Completion` | types | 01-01 |
| `getLocalDateString`, `getLast7Days`, `isFutureDate` | functions | 01-01 |
| `isDueOnDate`, `isDaily` | functions | 01-01 |
| `useTodayHabits`, `useToggleCompletion` | hooks | 01-01 |
| `TodayPage`, `HabitNewPage` | pages | 01-01 |
| `AppShell`, `HabitRow`, `FloatingAddButton` | components | 01-01 |
| `src/integration/walkingSkeleton.test.ts` | test file | 01-01 |
| `src/components/ui/*` | shadcn components | 01-01 |

<output>
Create `.planning/phases/01-habit-management-daily-logging/01-01-SUMMARY.md` when done
</output>
