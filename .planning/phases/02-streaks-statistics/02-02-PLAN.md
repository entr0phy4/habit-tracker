---
phase: 02-streaks-statistics
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/domain/dates.ts
  - src/domain/dates.test.ts
  - src/domain/stats.ts
  - src/domain/stats.test.ts
autonomous: true
requirements:
  - STRK-03
  - STRK-04
must_haves:
  truths:
    - Completion rate uses lifetime scheduled days since creation, rounded whole percent (STRK-03, D-07, D-08)
    - Completion rate denominator excludes future scheduled days (RESEARCH Pitfall 3)
    - Calendar week is Mon–Sun containing today, not rolling last 7 days (STRK-04, D-09)
    - getWeekDayState returns completed | missed | not-scheduled | future for grid rendering (D-10)
    - Missed scheduled days map to missed state; non-scheduled map to not-scheduled (D-10, D-11)
    - Planner assumption: future scheduled days in current week resolve to future state — dimmed disabled dot, not missed (UI-SPEC unresolved)
  artifacts:
    - src/domain/stats.ts
    - src/domain/stats.test.ts
  key_links:
    - calculateCompletionRate uses iterateDaysInRange + isDueOnDate + isFutureDate
    - getCalendarWeekDates uses date-fns startOfWeek/endOfWeek with weekStartsOn 1
    - getWeekDayState composes isDueOnDate, isFutureDate, completedDates.has
  prohibitions:
    - Must not use getLast7Days for history week range in new code paths
---

## Phase Goal

**As a** habit tracker user, **I want to** see streak motivation and completion stats that respect each habit's schedule, **so that** I stay motivated without breaking my chain on rest days or before today's check-in.

<objective>
Implement completion rate and calendar-week state pure functions with Vitest coverage — foundation for history stat cards and weekly dot grid.

Purpose: STRK-03 lifetime rate and STRK-04 calendar-week data layer before UI wiring (D-07–D-12).
Output: `stats.ts`, `getCalendarWeekDates`, passing `stats.test.ts` and calendar week `dates.test.ts` cases.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-streaks-statistics/02-CONTEXT.md
@.planning/phases/02-streaks-statistics/02-RESEARCH.md
@.planning/phases/02-streaks-statistics/02-PATTERNS.md
@.planning/phases/02-streaks-statistics/02-VALIDATION.md
@src/domain/streak.ts
@src/domain/dates.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing stats and calendar-week tests</name>
  <files>src/domain/stats.test.ts, src/domain/dates.test.ts</files>
  <read_first>src/domain/dates.ts, src/domain/streak.ts, .planning/phases/02-streaks-statistics/02-RESEARCH.md, .planning/phases/02-streaks-statistics/02-VALIDATION.md</read_first>
  <acceptance_criteria>
    - stats.test.ts imports calculateCompletionRate and getWeekDayState from ./stats and fails on missing module
    - Test: 0% rate for new habit with no completions
    - Test: 100% when all scheduled days since creation are complete
    - Test: 2/3 scheduled days complete → 67% rounded (D-08)
    - Test: denominator excludes future scheduled days in current week
    - Test: MWF habit Tuesday → getWeekDayState returns not-scheduled
    - Test: scheduled yesterday incomplete → missed
    - Test: scheduled future day in week → future (not missed)
    - dates.test.ts: today Sunday 2026-07-19 → getCalendarWeekDates returns Mon Jul 13 through Sun Jul 19 (7 dates, Mon first)
    - npm test -- src/domain/stats.test.ts fails before implementation exists
  </acceptance_criteria>
  <behavior>
    - WeekDayState type is completed | missed | not-scheduled | future
    - getCalendarWeekDates anchors to Monday per D-09
    - Rate formula: Math.round((completed / scheduled) * 100) with scheduled === 0 → 0
  </behavior>
  <action>
    Create src/domain/stats.test.ts covering STRK-03 and STRK-04 state predicates from 02-VALIDATION.md Nyquist table.

    Extend src/domain/dates.test.ts with calendar week boundary fixture: fixedToday = new Date(2026, 6, 19) (July 19 Sunday). Assert first element is 2026-07-13 and length is 7.

    Do not create stats.ts or getCalendarWeekDates yet.
  </action>
  <verify>
    <automated>npm test -- src/domain/stats.test.ts 2>&1 | grep -qE 'FAIL|Error|Cannot find'; test $? -eq 0</automated>
  </verify>
  <done>Failing stats test suite documents rate and week-state edge predicates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — stats.ts and getCalendarWeekDates</name>
  <files>src/domain/stats.ts, src/domain/dates.ts</files>
  <read_first>src/domain/stats.test.ts, .planning/phases/02-streaks-statistics/02-RESEARCH.md, .planning/phases/02-streaks-statistics/02-PATTERNS.md</read_first>
  <acceptance_criteria>
    - calculateCompletionRate accepts optional today Date defaulting to new Date() for isFutureDate exclusion
    - getWeekDayState exported with WeekDayState type
    - getCalendarWeekDates exported from dates.ts using eachDayOfInterval + startOfWeek/endOfWeek weekStartsOn 1
    - getLast7Days remains in dates.ts for backward compat but history flow will use getCalendarWeekDates in Plan 04
    - npm test -- src/domain/stats.test.ts src/domain/dates.test.ts -x passes
  </acceptance_criteria>
  <action>
    Add to dates.ts: getCalendarWeekDates with per-function imports from date-fns/eachDayOfInterval, date-fns/startOfWeek, date-fns/endOfWeek. Map results through getLocalDateString.

    Create stats.ts with calculateCompletionRate and getWeekDayState. Import isDueOnDate from schedule.ts, isFutureDate and iterateDaysInRange from dates.ts. Export WeekDayState type.

    Keep functions pure — no React or Dexie.
  </action>
  <verify>
    <automated>npm test -- src/domain/stats.test.ts src/domain/dates.test.ts -x</automated>
  </verify>
  <done>Stats and calendar-week domain layer complete and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain ← Set&lt;date&gt; | Rate and week state derived from repository-provided completion set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-03 | Tampering | stats.ts date inputs | low | accept | Dates bounded to habit lifetime and calendar week |
| T-2-04 | Denial of Service | getCalendarWeekDates | low | accept | Fixed 7-day output always |
</threat_model>

<verification>
- npm test -- src/domain/stats.test.ts src/domain/dates.test.ts -x — all green
- grep -c getCalendarWeekDates src/domain/dates.ts returns 1+
</verification>

<success_criteria>
- STRK-03 completion rate algorithm tested with rounding and future-day exclusion
- STRK-04 calendar week and week-day state predicates tested
- Ready for reactive hooks and HistoryDotGrid in Plans 03–04
</success_criteria>

## Artifacts this phase produces

| Symbol / Path | Kind | Plan |
|---------------|------|------|
| `calculateCompletionRate` | function | 02-02 |
| `getWeekDayState` | function | 02-02 |
| `WeekDayState` | type | 02-02 |
| `getCalendarWeekDates` | function | 02-02 |
| `src/domain/stats.ts` | file | 02-02 |
| `src/domain/stats.test.ts` | file | 02-02 |

<output>
Create `.planning/phases/02-streaks-statistics/02-02-SUMMARY.md` when done
</output>
