---
phase: 02-streaks-statistics
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
files_modified:
  - src/hooks/useStreak.ts
  - src/components/habits/HabitRow.tsx
  - src/components/habits/HabitRow.test.tsx
  - src/pages/TodayPage.tsx
autonomous: true
requirements:
  - STRK-01
must_haves:
  truths:
    - User sees current streak inline on Today rows with Flame icon + number — prominent Today view, not history-only (STRK-01, D-01, D-02, D-03)
    - Streak badge visible when row is completed — not muted or hidden (D-04)
    - Streak shows Flame + 0 before first scheduled completion (D-13)
    - Stats update immediately when user toggles today completion via useLiveQuery (STRK-01 reactive)
    - Streak badge has aria-label "{n} day streak" per UI-SPEC accessibility
    - statement: Dexie read failure during useStreak falls back to 0 without error UI
      verification: backstop
  artifacts:
    - src/hooks/useStreak.ts
    - src/components/habits/HabitRow.tsx
  key_links:
    - useStreak calls completionRepository.getByHabitInRange(habit.id, getHabitStartDate(habit), today)
    - useStreak passes Set to calculateCurrentStreak from domain/streak.ts
    - HabitRow calls useStreak(habit, todayKey) between name and WeekDayDots
    - TodayPage threads todayKey to HabitRow for midnight visibilitychange consistency
  prohibitions:
    - Must not use dangerouslySetInnerHTML for habit name or streak count (T-2-01)
    - Must not mute streak badge when isCompleted is true
---

## Phase Goal

**As a** habit tracker user, **I want to** see streak motivation and completion stats that respect each habit's schedule, **so that** I stay motivated without breaking my chain on rest days or before today's check-in.

<objective>
Ship end-to-end current streak on Today view — user sees Flame + count on every due habit row, updating immediately on toggle.

Purpose: STRK-01 user-visible vertical slice; motivation at check-in time per D-01–D-04.
Output: useStreak hook, HabitRow inline badge, extended component tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-streaks-statistics/02-CONTEXT.md
@.planning/phases/02-streaks-statistics/02-UI-SPEC.md
@.planning/phases/02-streaks-statistics/02-PATTERNS.md
@.planning/phases/02-streaks-statistics/02-01-SUMMARY.md
@src/domain/streak.ts
@src/components/habits/HabitRow.tsx
@src/hooks/useCompletions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useStreak reactive hook</name>
  <files>src/hooks/useStreak.ts</files>
  <read_first>src/hooks/useCompletions.ts, src/infrastructure/completionRepository.ts, src/domain/streak.ts, src/domain/dates.ts, .planning/phases/02-streaks-statistics/02-PATTERNS.md</read_first>
  <acceptance_criteria>
    - useStreak(habit, todayKey?) returns { currentStreak, isLoading }
    - useLiveQuery deps include habit.id, habit.frequency, habit.createdAt, today
    - Fetches completions via completionRepository.getByHabitInRange from getHabitStartDate(habit) through today
    - currentStreak defaults to 0 when result is undefined (loading) vs computed number when ready
    - isLoading true when useLiveQuery result is undefined
    - No direct db import — use completionRepository only
  </acceptance_criteria>
  <action>
    Create src/hooks/useStreak.ts following useCompletions useLiveQuery pattern from 02-PATTERNS.md.

    Inside callback: build Set from returned date strings, call calculateCurrentStreak with habit.frequency, today, and getHabitStartDate(habit).

    Accept optional todayKey string; default getLocalDateString(new Date()).

    Include habit.frequency in dependency array so stats refresh when habit schedule is edited.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Reactive current streak hook ready for HabitRow consumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HabitRow inline streak badge and tests</name>
  <files>src/components/habits/HabitRow.tsx, src/components/habits/HabitRow.test.tsx, src/pages/TodayPage.tsx</files>
  <read_first>.planning/phases/02-streaks-statistics/02-UI-SPEC.md, src/components/habits/HabitRow.tsx, src/components/habits/HabitRow.test.tsx, src/pages/TodayPage.tsx</read_first>
  <acceptance_criteria>
    - HabitRow layout: [name] [streak badge] [WeekDayDots] [history button] per UI-SPEC
    - Flame icon h-4 w-4 text-primary with numeric count text-xs font-semibold text-foreground
    - Badge wrapper aria-label="{currentStreak} day streak"; Flame has aria-hidden
    - Badge renders when isCompleted true with full color — not text-muted-foreground (D-04)
    - Badge shows Flame + 0 when useStreak returns 0
    - Optional todayKey prop threaded from TodayPage existing todayKey state
    - HabitRow.test.tsx mocks useStreak; asserts streak number visible and present when isCompleted
    - npm test -- src/components/habits/HabitRow.test.tsx -x passes
  </acceptance_criteria>
  <behavior>
    - Mock useStreak returns { currentStreak: 5, isLoading: false }
    - Completed row still shows "5" streak count
    - Flame icon present via lucide-react
  </behavior>
  <action>
    Modify HabitRow: import Flame from lucide-react and useStreak. Add optional todayKey?: string prop.

    Insert streak badge span between truncated name and WeekDayDots — shrink-0, gap-1, visible during loading only when useStreak resolves (omit badge while isLoading per UI-SPEC).

    Update TodayPage to pass todayKey={todayKey} to each HabitRow.

    Extend HabitRow.test.tsx with vi.mock('@/hooks/useStreak'). Add test cases for streak visible and streak visible when completed.

    Do not change row toggle/swipe behavior.
  </action>
  <verify>
    <automated>npm test -- src/components/habits/HabitRow.test.tsx -x</automated>
  </verify>
  <done>User sees current streak on Today view; STRK-01 UI slice complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HabitRow → DOM | Habit name and streak count rendered as React text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | HabitRow streak badge | medium | mitigate | React text escaping; no dangerouslySetInnerHTML for habit.name or streak count |
| T-2-05 | Spoofing | useStreak data | low | accept | Local-only Dexie; no remote data |
</threat_model>

<verification>
- npm test -- src/components/habits/HabitRow.test.tsx src/domain/streak.test.ts -x
- npm run build exits 0
- Manual: toggle today completion; streak increments without page refresh
</verification>

<success_criteria>
- STRK-01 complete on Today view (D-01–D-04)
- Inline Flame badge reactive via useLiveQuery
- Component tests cover badge render and completed-row visibility
</success_criteria>

## Artifacts this phase produces

| Symbol / Path | Kind | Plan |
|---------------|------|------|
| `useStreak` | hook | 02-03 |
| `src/hooks/useStreak.ts` | file | 02-03 |
| HabitRow streak badge | UI region | 02-03 |

<output>
Create `.planning/phases/02-streaks-statistics/02-03-SUMMARY.md` when done
</output>
