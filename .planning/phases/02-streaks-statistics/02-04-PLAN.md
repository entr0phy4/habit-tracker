---
phase: 02-streaks-statistics
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/hooks/useHabitStats.ts
  - src/hooks/useCompletions.ts
  - src/components/habits/StatCards.tsx
  - src/components/habits/StatCards.test.tsx
  - src/components/habits/HistoryDotGrid.tsx
  - src/components/habits/HistoryDotGrid.test.tsx
  - src/pages/HabitHistoryPage.tsx
autonomous: true
requirements:
  - STRK-02
  - STRK-03
  - STRK-04
must_haves:
  truths:
    - History page shows fuller stats (current, longest, completion rate) in three prominent stat cards below habit name — not history-only stats (STRK-02, STRK-03, D-03, D-05, D-06)
    - Rate formatted as whole-number percent e.g. 85% (D-08)
    - Weekly overview uses calendar week Mon–Sun with subtitle "This week" (STRK-04, D-09, D-12)
    - Completed scheduled days show green fill; missed scheduled show destructive ring outline (D-10, D-11)
    - Non-scheduled days hidden — spacer only, day label remains (D-10 discretion hidden)
    - Dot grid remains tappable to toggle past/today completions (D-12 dual purpose)
    - History dots use 44x44px touch targets h-11 w-11 per UI-SPEC UI-02
    - Stats and grid update immediately on dot toggle via useLiveQuery (all STRK)
    - statement: Stat card values &gt;999 display full integer without abbreviation
      verification: backstop
    - statement: Dexie read failure during stat hooks shows 0 values or blank cards without raw exception text
      verification: backstop
    - Planner assumption: invalid/missing completions yield streak 0 and rate 0% silently (UI-SPEC unresolved)
  artifacts:
    - src/hooks/useHabitStats.ts
    - src/components/habits/StatCards.tsx
    - src/components/habits/HistoryDotGrid.tsx
    - src/pages/HabitHistoryPage.tsx
  key_links:
    - useHabitStats single getByHabitInRange query derives current, longest, rate
    - useCompletions uses getCalendarWeekDates instead of getLast7Days
    - HistoryDotGrid calls getWeekDayState with habit.frequency prop
    - HabitHistoryPage composes StatCards + HistoryDotGrid with frequency prop
  prohibitions:
    - Must not use getLast7Days in useCompletions after this plan
    - Must not render X mark on missed days — ring-destructive only (D-11)
---

## Phase Goal

**As a** habit tracker user, **I want to** see streak motivation and completion stats that respect each habit's schedule, **so that** I stay motivated without breaking my chain on rest days or before today's check-in.

<objective>
Ship history page stats header and calendar-week overview — user sees longest streak, completion rate, and weekly completed/missed grid per habit.

Purpose: Complete STRK-02, STRK-03, STRK-04 on /habits/:id/history (D-05–D-12).
Output: useHabitStats, StatCards, calendar-week HistoryDotGrid, updated HabitHistoryPage.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-streaks-statistics/02-CONTEXT.md
@.planning/phases/02-streaks-statistics/02-UI-SPEC.md
@.planning/phases/02-streaks-statistics/02-PATTERNS.md
@.planning/phases/02-streaks-statistics/02-01-SUMMARY.md
@.planning/phases/02-streaks-statistics/02-02-SUMMARY.md
@src/domain/stats.ts
@src/components/habits/HistoryDotGrid.tsx
@src/pages/HabitHistoryPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: useHabitStats hook and StatCards on history page</name>
  <files>src/hooks/useHabitStats.ts, src/components/habits/StatCards.tsx, src/components/habits/StatCards.test.tsx, src/pages/HabitHistoryPage.tsx</files>
  <read_first>.planning/phases/02-streaks-statistics/02-UI-SPEC.md, src/hooks/useStreak.ts, src/domain/streak.ts, src/domain/stats.ts, src/pages/HabitHistoryPage.tsx</read_first>
  <acceptance_criteria>
    - useHabitStats(habit, todayKey?) returns { current, longest, rate, isLoading }
    - Single useLiveQuery callback computes all three via calculateCurrentStreak, calculateLongestStreak, calculateCompletionRate
    - StatCards renders grid-cols-3 with labels Current, Longest, Rate and values per D-05/D-06
    - StatCards returns null while isLoading
    - Rate value renders as `${rate}%`
    - StatCards uses role="group" aria-label="Habit statistics" or semantic dl/dt/dd
    - HabitHistoryPage renders StatCards between habit name and "This week" subtitle
    - Subtitle text changed from "Last 7 days" to "This week"
    - StatCards.test.tsx asserts three labels and formatted rate
    - npm test -- src/components/habits/StatCards.test.tsx -x passes
  </acceptance_criteria>
  <action>
    Create useHabitStats.ts mirroring useStreak pattern but returning { current, longest, rate } object from one repository fetch.

    Create StatCards.tsx per 02-UI-SPEC: rounded-lg border border-border bg-card, text-xs muted labels, text-xl font-semibold values.

    Wire HabitHistoryPage: import useHabitStats and StatCards; render StatCards when habit loaded and stats not loading.

    Change subtitle copy to "This week".

    Add StatCards.test.tsx with render of sample values { current: 5, longest: 12, rate: 85 }.
  </action>
  <verify>
    <automated>npm test -- src/components/habits/StatCards.test.tsx -x</automated>
  </verify>
  <done>User sees current, longest, and rate stat cards on history page (STRK-02, STRK-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Calendar-week HistoryDotGrid and useCompletions</name>
  <files>src/hooks/useCompletions.ts, src/components/habits/HistoryDotGrid.tsx, src/components/habits/HistoryDotGrid.test.tsx, src/pages/HabitHistoryPage.tsx</files>
  <read_first>.planning/phases/02-streaks-statistics/02-UI-SPEC.md, src/domain/stats.ts, src/components/habits/HistoryDotGrid.tsx, src/components/habits/HistoryDotGrid.test.tsx</read_first>
  <acceptance_criteria>
    - useCompletions uses getCalendarWeekDates() instead of getLast7Days()
    - HistoryDotGrid accepts frequency: Frequency prop from HabitHistoryPage
    - Three visual states: completed green fill, missed ring-2 ring-destructive empty center, not-scheduled hidden dot with w-11 h-11 spacer
    - Future scheduled days: opacity-40, disabled, not counted as missed
    - Dot buttons h-11 w-11 (44px touch target)
    - Day labels remain Mon–Sun single letters for all 7 columns
    - Toggle on completed/missed dots still calls useToggleCompletion; toast on failure unchanged
    - HistoryDotGrid.test.tsx uses getCalendarWeekDates fixture; asserts ring-destructive on missed, no button for not-scheduled
    - npm test -- src/components/habits/HistoryDotGrid.test.tsx src/domain/stats.test.ts -x passes
  </acceptance_criteria>
  <behavior>
    - Fixed today mock 2026-07-19 (Sunday); week spans Jul 13–19
    - MWF habit: Tuesday column has no tappable button
    - Missed scheduled past day has ring-destructive class
    - mockToggle still called on dot tap
  </behavior>
  <action>
    Modify useCompletions to import getCalendarWeekDates from dates.ts. Keep same return shape { dates, completedDates, isLoading }.

    Modify HistoryDotGrid: add frequency prop; use getWeekDayState for each date; branch render per 02-UI-SPEC state table. Upgrade touch targets to h-11 w-11.

    Pass habit.frequency from HabitHistoryPage to HistoryDotGrid.

    Update HistoryDotGrid.test.tsx mocks and assertions per 02-PATTERNS.md. Replace getLast7Days references with getCalendarWeekDates.

    Preserve existing toggle error toast and today primary ring behavior.
  </action>
  <verify>
    <automated>npm test -- src/components/habits/HistoryDotGrid.test.tsx src/domain/stats.test.ts src/domain/dates.test.ts -x</automated>
  </verify>
  <done>User sees calendar-week overview with completed/missed states; STRK-04 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HistoryDotGrid → toggle | Date strings from calendar week only — bounded 7-day set |
| StatCards → DOM | Numeric values only; no user HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-06 | Tampering | HistoryDotGrid date toggle | medium | mitigate | isFutureDate guard preserved; future dots disabled |
| T-2-07 | Denial of Service | useHabitStats range query | low | mitigate | Bounded createdAt → today; acceptable for v1 per RESEARCH A4 |
</threat_model>

<verification>
- npm test — full suite green
- npm run build exits 0
- Manual: open history, verify Mon–Sun week, toggle missed dot, stat cards and grid update without refresh
</verification>

<success_criteria>
- STRK-02 longest streak visible in stat card
- STRK-03 completion rate visible as rounded percent
- STRK-04 calendar-week overview with three dot states
- Phase 2 all four requirements satisfied
</success_criteria>

## Artifacts this phase produces

| Symbol / Path | Kind | Plan |
|---------------|------|------|
| `useHabitStats` | hook | 02-04 |
| `StatCards` | component | 02-04 |
| `src/hooks/useHabitStats.ts` | file | 02-04 |
| `src/components/habits/StatCards.tsx` | file | 02-04 |
| HistoryDotGrid calendar-week mode | component behavior | 02-04 |

<output>
Create `.planning/phases/02-streaks-statistics/02-04-SUMMARY.md` when done
</output>
