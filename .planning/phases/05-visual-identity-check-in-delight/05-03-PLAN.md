---
phase: 05-visual-identity-check-in-delight
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/index.css
  - src/components/habits/HabitRow.tsx
  - src/components/habits/HabitRow.test.tsx
autonomous: true
requirements:
  - ENH-02
must_haves:
  truths:
    - Completing a habit today plays a one-shot CSS pulse ≤200ms with habit-color wash (~20% opacity) (D-13, D-17)
    - Uncomplete does not play celebration animation (D-13)
    - Completed resting state keeps strikethrough/muted text and uses low-opacity habit color wash instead of bare muted alone (D-14)
    - Flame streak badge remains visible when completed; animation does not replace it (D-15)
    - prefers-reduced-motion: reduce skips keyframes but still applies completed wash instantly (D-16)
  artifacts:
    - src/index.css
    - src/components/habits/HabitRow.tsx
    - src/components/habits/HabitRow.test.tsx
  key_links:
    - HabitRow incomplete→complete edge sets local celebrate class then clears on animationend/timeout
    - @keyframes habit-check-in-pulse + .animate-habit-check-in gated by prefers-reduced-motion in index.css
    - Resting wash uses habit.color at ~18% via color-mix or equivalent after normalizeHabitColor
  prohibitions:
    - Must not install framer-motion, Lottie, confetti, or haptic APIs (D-17)
    - Must not animate on uncomplete or on mount when already complete
    - Must not remove or replace the Flame badge
    - Must not animate DashboardCard or heatmap check-ins
---

## Phase Goal

**As a** habit tracker user, **I want** checking in today to flash a brief color-fill reward while my streak flame stays visible, **so that** completing a habit feels satisfying without extra chrome.

<objective>
Add CSS-only check-in delight on HabitRow: keyframes in index.css, complete-only pulse + resting habit wash, reduced-motion skip, Flame retained — fulfills ENH-02.

Purpose: D-13–D-17 on the Today surface after ENH-01 accents from Plan 02.
Output: index.css keyframes + HabitRow celebrate/resting wash with extended HabitRow tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-visual-identity-check-in-delight/05-CONTEXT.md
@.planning/phases/05-visual-identity-check-in-delight/05-UI-SPEC.md
@.planning/phases/05-visual-identity-check-in-delight/05-PATTERNS.md
@.planning/phases/05-visual-identity-check-in-delight/05-VALIDATION.md
@.planning/phases/05-visual-identity-check-in-delight/05-02-SUMMARY.md
@src/index.css
@src/components/habits/HabitRow.tsx
@src/components/habits/HabitRow.test.tsx
@src/domain/habitColors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS keyframes + prefers-reduced-motion in index.css</name>
  <files>src/index.css</files>
  <read_first>src/index.css, .planning/phases/05-visual-identity-check-in-delight/05-UI-SPEC.md, .planning/phases/05-visual-identity-check-in-delight/05-CONTEXT.md</read_first>
  <acceptance_criteria>
    - @keyframes habit-check-in-pulse defined: scale 1 → 1.02 at ~40% → 1 at 100% (D-13)
    - Utility class animate-habit-check-in (or equivalent documented name) runs animation 180ms with ease-out / cubic-bezier(0.22, 1, 0.36, 1) — duration ≤200ms (D-13)
    - @media (prefers-reduced-motion: reduce) sets that class animation: none (D-16)
    - Global --primary / @theme tokens unchanged; no framer-motion imports anywhere (D-17)
    - rg confirms keyframes and reduced-motion rule present in src/index.css
  </acceptance_criteria>
  <action>
    Add CSS-only check-in motion to src/index.css per UI-SPEC Interaction/Motion (D-13, D-16, D-17). Lock names from UI-SPEC: @keyframes habit-check-in-pulse and class animate-habit-check-in. Do not add animation libraries. Keep global primary green for FAB/tabs. Resting/celebration washes remain component-owned (inline/color-mix), not extra keyframe colors unless needed for the pulse wash pairing.
  </action>
  <verify>
    <automated>rg -n "habit-check-in-pulse|animate-habit-check-in|prefers-reduced-motion" src/index.css | grep -q habit-check-in-pulse; rg -n "prefers-reduced-motion" src/index.css | grep -q prefers-reduced-motion</automated>
  </verify>
  <done>CSS micro-animation and reduced-motion gate exist without new dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HabitRow complete-only pulse + resting wash + Flame retained</name>
  <files>src/components/habits/HabitRow.tsx, src/components/habits/HabitRow.test.tsx</files>
  <read_first>src/components/habits/HabitRow.tsx, src/components/habits/HabitRow.test.tsx, src/index.css, .planning/phases/05-visual-identity-check-in-delight/05-UI-SPEC.md, .planning/phases/05-visual-identity-check-in-delight/05-PATTERNS.md</read_first>
  <acceptance_criteria>
    - On transition to complete only: local UI state adds animate-habit-check-in (or habit-checkin class) for ≤180–200ms then removes; uncomplete does not add celebration class (D-13)
    - Do not celebrate on mount if already completed
    - Completed resting background uses habit.color wash ~18% (color-mix preferred) over muted/card — not bare bg-muted alone; name keeps line-through + muted-foreground (D-14)
    - Flame badge still rendered when completed with habit color tint; animation does not replace it (D-15)
    - Under reduced-motion CSS, keyframes skipped; wash still applied when complete (D-16) — document CSS gate; optional matchMedia test if JS-gated
    - Scale/wash applied on inner content so swipe translateX ownership stays clear
    - HabitRow.test.tsx asserts: complete triggers celebration class (or animation class); uncomplete does not; resting wash present when completed; Flame present when completed
    - npx vitest run src/components/habits/HabitRow.test.tsx passes; npm test full suite green
  </acceptance_criteria>
  <behavior>
    - incomplete → complete: celebrate once + settle to resting wash
    - complete → incomplete: wash removes instantly; no reverse pulse
    - already complete on first render: resting wash only, no pulse
  </behavior>
  <action>
    Implement ENH-02 on HabitRow only (D-13–D-17). Use local isCelebrating state (ref edge detect or equivalent) — no Zustand. Apply animate-habit-check-in on complete edge; clear via animationend or ~180ms timeout. Resting wash ~18% habit color; celebration peak ~20%. Keep Flame. Normalize color before style (T-05-01). Extend HabitRow.test.tsx per VALIDATION map. Run full npm test before done.
  </action>
  <verify>
    <automated>npx vitest run src/components/habits/HabitRow.test.tsx -x && npm test</automated>
  </verify>
  <done>ENH-02 complete — check-in pulse + tinted resting state with Flame retained and reduced-motion honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| habit.color → HabitRow wash/pulse styles | Same normalized hex path as Plan 02 accents |
| prefers-reduced-motion | OS accessibility preference must disable non-essential motion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering / CSS injection | HabitRow background wash styles | medium | mitigate | normalizeHabitColor before color-mix / backgroundColor; never inject raw import strings into stylesheets |
| T-05-06 | Denial of Service | rapid toggle celebrate | low | accept | Short 180ms one-shot; local state only; personal device |
| T-05-07 | Elevation / Privacy | prefers-reduced-motion bypass | low | mitigate | CSS @media (prefers-reduced-motion: reduce) forces animation: none on celebrate class (D-16) |
</threat_model>

<verification>
- npx vitest run src/components/habits/HabitRow.test.tsx -x
- npm test — full suite green
- Manual: toggle complete on Today — brief pulse then tinted completed row; Flame visible; OS reduced-motion → wash without pulse
</verification>

<success_criteria>
- ENH-02 satisfied: color-fill + micro-animation on check-in beyond Flame (ROADMAP success criterion 3)
- D-13–D-17 implemented; no animation libraries; Flame retained
- Phase 5 ready for `/gsd-verify-work` after execution
</success_criteria>
