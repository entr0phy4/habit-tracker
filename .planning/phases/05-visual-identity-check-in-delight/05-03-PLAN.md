---
phase: 05-visual-identity-check-in-delight
plan: 03
type: tdd
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/components/habits/HabitRow.tsx
  - src/components/habits/HabitRow.test.tsx
  - src/index.css
autonomous: true
requirements:
  - ENH-02
must_haves:
  truths:
    - Completing a habit today triggers visible color-fill and/or micro-animation on HabitRow (D-14–D-17)
    - Un-completing does not play the reward
    - prefers-reduced-motion disables the pulse/scale reward
    - Flame badge remains visible (D-16)
  artifacts:
    - src/components/habits/HabitRow.tsx
    - src/index.css
  key_links:
    - Reward uses habit.color for fill pulse
  prohibitions:
    - Must not add confetti/sound/haptics libraries
    - Must not require reward on heatmap past-day toggles
---

## Phase Goal

**As a** user, **I want** a satisfying micro-reward when I check in today, **so that** completing a habit feels motivating beyond the flame alone (ENH-02).

<objective>
Add CSS color-fill pulse + short scale animation on HabitRow when toggling to completed (D-14–D-17).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-visual-identity-check-in-delight/05-CONTEXT.md
@.planning/phases/05-visual-identity-check-in-delight/05-UI-SPEC.md
@.planning/phases/05-visual-identity-check-in-delight/05-02-SUMMARY.md
@src/components/habits/HabitRow.tsx
@src/index.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — HabitRow check-in reward tests</name>
  <files>src/components/habits/HabitRow.test.tsx</files>
  <read_first>src/components/habits/HabitRow.tsx, src/components/habits/HabitRow.test.tsx</read_first>
  <acceptance_criteria>
    - Test: when incomplete row is toggled to complete, reward class/data attribute appears (e.g. data-checkin-reward or class habit-checkin-reward)
    - Test: when completed row is toggled off, reward attribute does not appear
    - Tests fail until reward implemented (RED)
  </acceptance_criteria>
  <action>
    Extend HabitRow.test.tsx for ENH-02 reward trigger rules (D-14).
  </action>
  <verify>
    <automated>npm test -- src/components/habits/HabitRow.test.tsx 2>&1 | grep -qE 'FAIL|fail'; test $? -eq 0</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — pulse CSS + HabitRow wiring</name>
  <files>src/components/habits/HabitRow.tsx, src/index.css, src/components/habits/HabitRow.test.tsx</files>
  <read_first>src/index.css, src/components/habits/HabitRow.tsx</read_first>
  <acceptance_criteria>
    - CSS keyframes for habit-checkin-reward using --habit-reward-color custom property
    - HabitRow sets --habit-reward-color to habit.color and toggles reward class for ~250–300ms on complete
    - @media (prefers-reduced-motion: reduce) disables transform/opacity animation
    - Flame still rendered
    - npm test -- src/components/habits/HabitRow.test.tsx passes
  </acceptance_criteria>
  <action>
    Implement D-15–D-17 CSS + HabitRow state. Keep swipe/click toggle behavior intact.
  </action>
  <verify>
    <automated>npm test -- src/components/habits/HabitRow.test.tsx</automated>
  </verify>
</task>

</tasks>

## Verification

```bash
npm test -- src/components/habits/HabitRow.test.tsx
npm test
```

Create `.planning/phases/05-visual-identity-check-in-delight/05-03-SUMMARY.md` when done.
