---
phase: 05-visual-identity-check-in-delight
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/habits/HabitForm.tsx
  - src/components/habits/HabitForm.test.tsx
  - src/hooks/useCreateHabit.ts
  - src/pages/HabitEditPage.tsx
  - src/pages/HabitHistoryPage.tsx
  - src/components/habits/HabitRow.tsx
  - src/components/habits/HabitRow.test.tsx
  - src/components/habits/WeekDayDots.tsx
  - src/components/habits/WeekDayDots.test.tsx
  - src/components/dashboard/DashboardCard.tsx
  - src/components/dashboard/DashboardCard.test.tsx
  - src/components/heatmap/ContributionHeatmap.tsx
  - src/components/heatmap/ContributionHeatmap.test.tsx
  - src/pages/ManageHabitsPage.tsx
  - src/pages/ManageHabitsPage.test.tsx
autonomous: true
requirements:
  - ENH-01
must_haves:
  truths:
    - User can pick a preset color on create/edit via HabitForm Color radiogroup (D-06, D-07, D-08, D-19)
    - Today HabitRow shows left accent bar, habit-colored week dots and swipe strip (D-09)
    - Panel DashboardCard shows left accent bar + Flame tinted with habit.color; border stays border-border (D-10)
    - Historial ContributionHeatmap uses buildHeatmapTheme(habit.color); today stroke = habit color; missed stays destructive (D-11)
    - ManageHabitsPage shows 8px read-only swatch beside habit name including archived (D-12)
  artifacts:
    - src/components/habits/HabitForm.tsx
    - src/components/habits/HabitForm.test.tsx
    - src/components/habits/HabitRow.tsx
    - src/components/dashboard/DashboardCard.tsx
    - src/components/heatmap/ContributionHeatmap.tsx
    - src/pages/ManageHabitsPage.tsx
    - src/pages/ManageHabitsPage.test.tsx
  key_links:
    - HabitFormValues.color → useCreateHabit / habitRepository.create and HabitEditPage → update
    - WeekDayDots accentColor={habit.color} from HabitRow
    - ContributionHeatmap: `const theme = buildHeatmapTheme(normalizeHabitColor(color)); theme={theme}` (helper returns `{ dark: string[] }`)
    - ManageHabitsPage ← habit.color 8px swatch (D-12)
  prohibitions:
    - Must not add free-form hex / color wheel input
    - Must not tint DashboardCard full border with habit color
    - Must not recolor FAB / tab bar / form submit away from --primary
    - Must not implement check-in pulse keyframes here (Plan 05-03 / ENH-02)
    - Must not use Tailwind bg-[${habit.color}] — normalized inline style only
---

## Phase Goal

**As a** habit tracker user, **I want to** assign a preset color when creating or editing a habit and see that color on Today, Panel, Historial, and Manage, **so that** habits are visually distinct at a glance.

<objective>
Wire HabitForm Color radiogroup through create/edit, then paint ENH-01 surface accents on HabitRow, DashboardCard, ContributionHeatmap, and ManageHabitsPage.

Purpose: Complete ENH-01 UX (D-06–D-12, D-19) on top of Plan 01 persistence.
Output: HabitForm + HabitForm.test.tsx, create/edit color wire, accented surfaces with passing component tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-visual-identity-check-in-delight/05-CONTEXT.md
@.planning/phases/05-visual-identity-check-in-delight/05-UI-SPEC.md
@.planning/phases/05-visual-identity-check-in-delight/05-PATTERNS.md
@.planning/phases/05-visual-identity-check-in-delight/05-VALIDATION.md
@.planning/phases/05-visual-identity-check-in-delight/05-01-SUMMARY.md
@src/domain/habitColors.ts
@src/components/habits/HabitForm.tsx
@src/components/habits/HabitRow.tsx
@src/components/habits/WeekDayDots.tsx
@src/components/dashboard/DashboardCard.tsx
@src/components/heatmap/ContributionHeatmap.tsx
@src/pages/ManageHabitsPage.tsx
@src/pages/HabitEditPage.tsx
@src/pages/HabitHistoryPage.tsx
@src/hooks/useCreateHabit.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN — HabitForm Color radiogroup + create/edit wire</name>
  <files>src/components/habits/HabitForm.test.tsx, src/components/habits/HabitForm.tsx, src/hooks/useCreateHabit.ts, src/pages/HabitEditPage.tsx</files>
  <read_first>src/components/habits/HabitForm.tsx, src/components/habits/HabitRow.test.tsx, src/pages/HabitEditPage.tsx, src/hooks/useCreateHabit.ts, src/domain/habitColors.ts, .planning/phases/05-visual-identity-check-in-delight/05-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - Wave 0: HabitForm.test.tsx created; initially fails until form implements Color field
    - HabitFormValues includes color: string; create default '#3fb950'; edit seeds initialValues.color (D-08)
    - Label and radiogroup aria-label are English "Color" (D-07); Spanish chrome elsewhere untouched
    - role="radiogroup" with 8 role="radio" swatches from HABIT_COLOR_PRESETS; aria-label Green/Teal/Blue/Purple/Pink/Orange/Yellow/Red (D-05, D-06)
    - Selected swatch aria-checked=true with ring matching swatch hex; hit area min-h-11 min-w-11 (≥44×44) (D-19); visual circle ~28px
    - Color field placed after "Repeat on", before submit; no free-form hex input
    - Selecting non-default swatch → onSubmit receives that hex; default when no initialValues is '#3fb950'
    - useCreateHabit accepts/passes color; HabitEditPage initialValues + update include color (D-08)
    - npx vitest run src/components/habits/HabitForm.test.tsx passes
  </acceptance_criteria>
  <behavior>
    - Radiogroup selection updates form color state; submit payload includes color
    - Keyboard: radio pattern (arrow keys / space-enter) per UI-SPEC
  </behavior>
  <action>
    Write HabitForm.test.tsx first (Wave 0 RED for Color radiogroup submit), then implement HabitForm Color block per D-06, D-07, D-19 and UI-SPEC. Iterate HABIT_COLOR_PRESETS only. Wire color through useCreateHabit and HabitEditPage update/initialValues (D-08). Use inline style for swatch background and selected ring color after normalize — never Tailwind dynamic class interpolation. Do not add check-in animation.
  </action>
  <verify>
    <automated>npx vitest run src/components/habits/HabitForm.test.tsx -x</automated>
  </verify>
  <done>Users can assign a preset color on create and edit; form tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface accents — HabitRow, WeekDayDots, DashboardCard, heatmap, Manage</name>
  <files>src/components/habits/HabitRow.tsx, src/components/habits/HabitRow.test.tsx, src/components/habits/WeekDayDots.tsx, src/components/habits/WeekDayDots.test.tsx, src/components/dashboard/DashboardCard.tsx, src/components/dashboard/DashboardCard.test.tsx, src/components/heatmap/ContributionHeatmap.tsx, src/components/heatmap/ContributionHeatmap.test.tsx, src/pages/HabitHistoryPage.tsx, src/pages/ManageHabitsPage.tsx, src/pages/ManageHabitsPage.test.tsx</files>
  <read_first>src/components/habits/HabitRow.tsx, src/components/habits/WeekDayDots.tsx, src/components/dashboard/DashboardCard.tsx, src/components/heatmap/ContributionHeatmap.tsx, src/pages/ManageHabitsPage.tsx, src/pages/HabitHistoryPage.tsx, src/domain/habitColors.ts, .planning/phases/05-visual-identity-check-in-delight/05-PATTERNS.md, .planning/phases/05-visual-identity-check-in-delight/05-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - HabitRow: 4px left accent bar habit.color; swipe-reveal strip habit.color at ~20% opacity; WeekDayDots accentColor={habit.color}; Flame tinted habit.color (D-09) — no celebration keyframes yet
    - WeekDayDots: optional accentColor; scheduled dots use it; unscheduled stay border/#30363d; default #3fb950 if omitted
    - DashboardCard: 4px left accent + Flame style color=habit.color; border remains border-border — no full-border tint (D-10)
    - ContributionHeatmap: required color prop; wiring EXACTLY `const theme = buildHeatmapTheme(normalizeHabitColor(color));` then `theme={theme}` where buildHeatmapTheme returns `{ dark: string[] }` — do not nest `theme={{ dark: buildHeatmapTheme(...) }}` (D-11, D-18)
    - Today stroke habit color; missed stroke var(--destructive); empty #21262d
    - HabitHistoryPage passes habit.color into ContributionHeatmap
    - ManageHabitsPage: 8px rounded-full aria-hidden swatch beside name on active and archived rows (D-12)
    - Tests extended: HabitRow accent styles, DashboardCard flame/accent, ContributionHeatmap theme/stroke, WeekDayDots accent, ManageHabitsPage swatch; fixtures include color
    - npx vitest run src/components/habits/HabitRow.test.tsx src/components/habits/WeekDayDots.test.tsx src/components/dashboard/DashboardCard.test.tsx src/components/heatmap/ContributionHeatmap.test.tsx src/pages/ManageHabitsPage.test.tsx passes
  </acceptance_criteria>
  <action>
    Replace hardcoded primary accents with habit.color per D-09–D-12. Remove module-level green heatmapTheme; call buildHeatmapTheme per 05-PATTERNS ContributionHeatmap assignment (`theme={theme}` object with `dark` array). Prefer inline style after normalizeHabitColor (T-05-01). Discretion locks: Manage swatch 8px; DashboardCard left bar + Flame only. Add/extend ManageHabitsPage.test.tsx asserting swatch present. Leave completed resting wash and pulse animation to Plan 05-03 (ENH-02) — incomplete rows may keep existing incomplete bg. Global FAB/tabs/submit stay --primary.
  </action>
  <verify>
    <automated>npx vitest run src/components/habits/HabitForm.test.tsx src/components/habits/HabitRow.test.tsx src/components/habits/WeekDayDots.test.tsx src/components/dashboard/DashboardCard.test.tsx src/components/heatmap/ContributionHeatmap.test.tsx src/pages/ManageHabitsPage.test.tsx -x</automated>
  </verify>
  <done>ENH-01 surfaces reflect each habit's color on Today, Panel, Historial, and Manage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Habit.color → inline style | Runtime hex from IndexedDB applied as CSS color/background/stroke |
| HabitForm swatch selection | User picks only preset hexes from HABIT_COLOR_PRESETS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering / CSS injection | HabitRow, DashboardCard, heatmap, Manage style props | medium | mitigate | Always pass through normalizeHabitColor before style={{ }}; never build CSS selectors from raw color |
| T-05-04 | Spoofing / Confusion | habit red #f85149 vs Delete | low | mitigate | Destructive buttons keep --destructive token classes; habit red used only for identity accents |
| T-05-05 | Denial of Service | 8 swatch radiogroup | low | accept | Fixed 8 presets; no unbounded color list |
</threat_model>

<verification>
- npx vitest run src/components/habits/HabitForm.test.tsx src/components/habits/HabitRow.test.tsx src/components/dashboard/DashboardCard.test.tsx src/components/heatmap/ContributionHeatmap.test.tsx -x
- Manual smoke: create habit with blue → Today accent + Panel flame + Historial heatmap + Manage swatch match
</verification>

<success_criteria>
- ENH-01 complete: pick color on create/edit; Today/Panel/heatmap/Manage reflect habit color (ROADMAP success criteria 1–2, 4 contrast via presets + theme helper)
- D-06–D-12 and D-19 implemented; no ENH-02 animation scope creep
</success_criteria>
