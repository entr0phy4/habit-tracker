---
phase: 04-data-backup-restore
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/pages/SettingsPage.tsx
  - src/pages/SettingsPage.test.tsx
autonomous: true
requirements:
  - DATA-02
  - DATA-03
must_haves:
  truths:
    - Exportar datos downloads JSON via backupService and shows toast Backup exportado (D-05, D-08, DATA-02)
    - Invalid file shows Archivo no válido and does not open ConfirmDialog (D-14)
    - Unsupported version shows Versión de backup no compatible (D-15)
    - Valid file opens destructive ConfirmDialog with habit and completion counts (D-11, D-12)
    - Confirm runs importBackup then toast Backup restaurado and navigate to / (D-13, DATA-03)
    - Import write failure shows No se pudo importar. Inténtalo de nuevo. and leaves user on Settings (D-16)
  artifacts:
    - src/pages/SettingsPage.tsx
    - src/pages/SettingsPage.test.tsx
  key_links:
    - parseBackupJson before setState pending payload / open dialog
    - ConfirmDialog destructive with Cancelar / Reemplazar
    - importBackup only inside onConfirm
  prohibitions:
    - Must not show raw Zod or Dexie error strings in UI
    - Must not call importBackup before user confirms
    - Must not merge-by-ID — full replace only
---

## Phase Goal

**As a** habit tracker user, **I want to** export my data in one tap and restore a backup only after an explicit replace warning, **so that** I trust the app with my streak history.

<objective>
Wire Settings export download and import confirm flow to backupService with Spanish toasts and navigation — completes DATA-02 and DATA-03 UX.

Purpose: D-05–D-16 user-facing trust loop on top of Plan 01 service and Plan 02 chrome.
Output: Fully interactive SettingsPage with tested export/import paths.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-data-backup-restore/04-CONTEXT.md
@.planning/phases/04-data-backup-restore/04-UI-SPEC.md
@.planning/phases/04-data-backup-restore/04-PATTERNS.md
@.planning/phases/04-data-backup-restore/04-01-SUMMARY.md
@.planning/phases/04-data-backup-restore/04-02-SUMMARY.md
@src/pages/SettingsPage.tsx
@src/infrastructure/backupService.ts
@src/domain/backupSchema.ts
@src/components/habits/ConfirmDialog.tsx
@src/pages/HabitEditPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export download + success toast</name>
  <files>src/pages/SettingsPage.tsx, src/pages/SettingsPage.test.tsx</files>
  <read_first>src/infrastructure/backupService.ts, src/pages/HabitNewPage.tsx, .planning/phases/04-data-backup-restore/04-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - Clicking Exportar datos awaits exportBackup, calls downloadBackupJson with buildBackupFilename(), then toast.success('Backup exportado')
    - Test mocks backupService module and sonner toast; asserts exportBackup + downloadBackupJson + toast.success called
    - Empty DB export still shows success toast (D-09)
    - npm test -- src/pages/SettingsPage.test.tsx passes for export cases
  </acceptance_criteria>
  <action>
    Implement handleExport on SettingsPage. Mock `@/infrastructure/backupService` in tests. Follow sonner import pattern from HabitEditPage (`import { toast } from 'sonner'`).
  </action>
  <verify>
    <automated>npx vitest run src/pages/SettingsPage.test.tsx -x</automated>
  </verify>
  <done>Export path fulfills DATA-02 UI success criteria.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Import validate → ConfirmDialog → replace → redirect</name>
  <files>src/pages/SettingsPage.tsx, src/pages/SettingsPage.test.tsx</files>
  <read_first>src/pages/HabitEditPage.tsx, src/domain/backupSchema.ts, .planning/phases/04-data-backup-restore/04-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - Import button triggers hidden file input click
    - On file load, parseBackupJson(text): invalid → toast.error('Archivo no válido') and dialog stays closed
    - unsupported_version → toast.error('Versión de backup no compatible') and dialog stays closed
    - ok → open ConfirmDialog with title ¿Reemplazar todos los datos?, description including habit and completion counts, confirmLabel Reemplazar, cancelLabel Cancelar, destructive
    - onConfirm awaits importBackup(pending); on success toast.success('Backup restaurado') and navigate('/')
    - onConfirm failure → toast.error('No se pudo importar. Inténtalo de nuevo.'); dialog closes or stays per discretion but no navigation
    - onCancel clears pending payload and closes dialog without importBackup
    - Reset file input value after handling so re-selecting same file works
    - Tests cover invalid, unsupported_version, confirm success navigate, and cancel paths (mock parseBackupJson or feed File via userEvent.upload)
    - npm test -- src/pages/SettingsPage.test.tsx passes
  </acceptance_criteria>
  <behavior>
    - Never call importBackup until ConfirmDialog confirm
    - Counts in description come from parsed payload arrays lengths
  </behavior>
  <action>
    Complete SettingsPage import state machine: pendingPayload + dialogOpen. Use ConfirmDialog like HabitEditPage. Prefer `await file.text()` over FileReader for simplicity. Mock navigate via react-router if needed (`vi.mock('react-router')` or MemoryRouter + assert path).
  </action>
  <verify>
    <automated>npx vitest run src/pages/SettingsPage.test.tsx -x && npm test</automated>
  </verify>
  <done>Import trust loop complete — validate, warn with counts, transactional replace, Spanish feedback, redirect to Today.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File picker → UI | Untrusted file; must parse before confirm |
| Confirm → importBackup | User intent gate for destructive replace |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-01 | Tampering | file import | high | mitigate | parseBackupJson before dialog; toast-only errors |
| T-4-02 | Tampering | confirm skip | high | mitigate | importBackup only in onConfirm |
| T-4-04 | Information Disclosure | toasts | medium | mitigate | Fixed Spanish strings only |
</threat_model>

<verification>
- npx vitest run src/pages/SettingsPage.test.tsx -x
- npm test — full suite green
- Manual: export file opens download; import invalid toast; import valid confirm → Today shows restored habits
</verification>

<success_criteria>
- DATA-02 export UX complete
- DATA-03 import UX complete with validation, confirm, replace, redirect
- Phase 4 ready for `/gsd-verify-work` after execution
</success_criteria>
